# 第 0 章 · 鸟瞰:一次调用的完整解剖图

> **我们在解剖图上的位置**：整张图。本章不深入任何一站，只负责把地图交到你手上。
>
> **本章回答的问题**：从你按下回车到屏幕上蹦出第一个字，这几百毫秒里到底发生了哪几件事？哪些发生在"模型里面"，哪些发生在"模型外面"？为什么这本书要按现在这个顺序讲？
>
> **前置依赖**：无。这是全书的起点。

---

## 0.1 一次再普通不过的请求

假设你在某个聊天界面里打下一句「帮我补全这首诗：白日依山尽，黄河入海」，点击发送。你的客户端把这句话连同对话历史打包成一个 HTTP 请求：

```bash
curl https://api.example.com/v1/chat/completions \
  -d '{
    "model": "llama-2-70b",
    "messages": [
      {"role": "system", "content": "你是一位精通古诗的助手"},
      {"role": "user",   "content": "帮我补全这首诗：白日依山尽，黄河入海"}
    ],
    "max_tokens": 32,
    "temperature": 0.7,
    "stream": true
  }'
```

半秒左右，屏幕开始逐字打印：先是「流」，然后「。」，接着「欲」「穷」「千」「里」「目」……一首《登鹳雀楼》被补写完整。

作为工程师，你对这类请求-响应太熟悉了：无非是 HTTP 进、JSON 出。但在这个再普通不过的外表下，服务端刚刚完成了一件规模惊人的事：一个拥有 **700 亿个参数**的神经网络——把它的权重用 FP16 格式存下来约有 **140 GB**，比大多数笔记本的整块硬盘还大——被完整地运转了几十遍；每生成一个字，这 140 GB 的参数就要从显存里被完整地读一遍；整个响应过程累计搬运的数据以 TB 计，累计的浮点运算以万亿次计。

这本书要做的事情只有一件：**把这一次调用完整解剖开**。等你合上书，再看到屏幕上逐字蹦出的回答时，你眼前浮现的将不再是一个黑盒，而是一条每一站你都亲手拆开过的流水线。

> **关于请求格式的说明**：本书使用 Chat API 格式（`messages` 数组）作为主线例子，因为这是 2024 年后绝大多数开发者与 LLM 交互的方式。但请放心——`messages` 在进入模型之前会被拼接成一段扁平的文本（站点 ① 会交代这一步），此后全书讨论的一切机制与用裸 prompt 的 Completions API 完全相同。为聚焦内核，Part I~III 中我们直接以拼接后的 prompt 作为起点。

## 0.2 两层世界：外壳与内核

解剖的第一刀，先把这次调用切成两层。

**内核（算法层）**：模型本体做的事。字符串变成数字，数字变成向量，向量穿过 80 层矩阵运算，最后变成"下一个字是什么"的概率分布。这一层的逻辑写在论文里，是数学。

**外壳（工程层）**：让模型本体在真实世界里跑起来的一切。接收 HTTP 请求、排队、把几十个用户的请求拼在一起算、管理显存、把生成的字实时推回给你。这一层的逻辑写在 vLLM 这类推理引擎的源码里，是系统工程。

```
      ┌────────────────────── 外壳（工程层）──────────────────────┐
      │                                                          │
      │   ① 接入·格式化·排队        ⑦ 显存与调度     ⑧ 反分词·流式  │
      │        │                        ↑              ↑         │
      │        ▼                        │              │         │
      │   ┌─────────────── 内核（算法层）───────────────┐          │
      │   │ ② 分词 → ③ 嵌入 → ④ 80 层车间 → ⑤ 出口采样 │──────────┤
      │   │        ↑______________⑥ 自回归循环_____│   │          │
      │   └─────────────────────────────────────────┘           │
      └──────────────────────────────────────────────────────────┘
```

市面上讲 Transformer 的资料，绝大多数只讲内核，讲到"采样出一个字"就结束了。但一次真实调用里，**至少一半的性能、成本和稳定性问题发生在外壳里**——为什么第一个字等得久？为什么并发一高服务就报显存不足？为什么同样的模型，vLLM 比自己手写的推理脚本快一个数量级？这些问题的答案不在任何一篇模型论文里。

所以本书的承诺是：**内核和外壳，一个不少**。Part I 到 Part III 解剖内核，Part IV 解剖外壳。

## 0.3 八个站点走马观花

现在，跟着你的请求走一遍全程。每一站只停留一段话——细节留给后面的章节，这里只需要你记住"有这么一站，它解决什么问题"。

### 站点 ①：接入、格式化与排队（外壳）

你的 HTTP 请求先到达推理服务的接入层。它做的是任何 Web 服务都做的事：解析 JSON、校验参数、把请求放进等待队列。

但在排队之前，还有一步容易被忽略的**格式转换**：你提交的 `messages` 数组并不是模型能直接消化的东西。服务端会按模型对应的 **Chat Template**（对话模板）把多轮消息拼接成一段扁平文本，并插入特殊标记：

```
你提交的:  messages = [system, user]
              │  应用 Chat Template
              ▼
模型看到的: "<s>[INST] <<SYS>>你是一位精通古诗的助手<</SYS>>
            帮我补全这首诗：白日依山尽，黄河入海 [/INST]"
```

`<s>`、`[INST]`、`<<SYS>>` 这些特殊标记在词表里有正式编号（第 1 章会回到词表），它们告诉模型"这里是系统指令的边界""这里轮到助手回答了"。不同模型的模板格式不同（ChatML、Llama 格式、Qwen 格式……），但本质都一样：**把结构化的多轮对话拍平成一段连续文本**。本书此后讨论的"prompt"，指的就是拼接后的这段文本。

拼接完成后，请求进入等待队列。队列后面的**调度器**不是简单的先来先服务，而是每隔十几毫秒就重新决定一次"这一批让哪些请求上 GPU"。为什么要拼批、怎么拼，是第 10 章·吞吐引擎的主题。此刻你只需要知道：你的诗在队列里可能等了几毫秒，然后被批准进入内核。

### 站点 ②：分词——字符串变整数

模型不认识汉字。它认识的是**词表**（vocabulary）——一张 32000 行的表，每行是一个文本片段。**分词器**（tokenizer）把你的 prompt 切成词表里存在的片段，再把每个片段替换成它的行号：

```
"白日依山尽，黄河入海"  →  [白][日][依][山][尽][，][黄][河][入][海]
                        →  [2312, 1907, 4023, 855, 3241, 30, 5722, 1385, 2076, 3811]
```

十个字，十个整数（这里"一个汉字恰好一个 token"是本书的教学简化，第 1 章会说明它为什么无伤大雅）。这串整数叫 **token 序列**，长度记作 $n$，此刻 $n = 10$。注意：这些整数只是行号，不携带任何含义——「白」是 2312 还是 9999 完全无所谓。

### 站点 ③：嵌入——整数变向量

含义从这一站开始。模型里有一张 $(32000, 8192)$ 的大表——**嵌入矩阵**。每个 token 用自己的行号去查表，取出一行 8192 个浮点数。十个 token 各取一行，摞成一个 $(10, 8192)$ 的矩阵。

为什么是 8192 个数？因为这是模型的**隐藏维度** $d_{\text{model}}$——你可以暂时把它理解为"模型内部表达一个字的含义时使用的坐标轴数量"。这 8192 个数是训练出来的语义坐标：「白」和「皎」的坐标相近，「白」和「跑」的坐标相远。第 1 章会解释这些坐标从何而来，以及一个关键的遗留问题：此刻的「白」还不知道自己身处哪句诗里。

### 站点 ④：80 层车间——向量的精加工

$(10, 8192)$ 的矩阵现在进入模型的主体：**80 层完全相同结构的加工车间**，一层的输出是下一层的输入。每层车间里有两道工序：

- **注意力**（attention）：让十个向量互相交换信息——「海」的向量吸收「黄河」「入」的信息，开始明白自己是"河流入海"的海。这是唯一让字与字发生联系的机制（第 3 章）。字与字的**位置关系**（谁在前谁在后）由一套专门的旋转机制注入这道工序（第 2 章讲透它，包括它具体作用在哪一步），而且这道工序有个精打细算的省显存设计（第 4 章）。
- **前馈网络**（FFN）：每个向量独立地"查一次资料"——从模型参数里注入训练时积累的世界知识，比如"这是五言绝句""登鹳雀楼的下一句是黄河入海流"（第 5 章）。

两道工序之外，还有两件保命装置——**残差连接**和**归一化**——保证信号穿过 80 层不失真、不爆炸（第 6 章）。

走完 80 层，矩阵形状依然是 $(10, 8192)$，但内容已经脱胎换骨：最后一行（「海」位置的向量）此刻凝聚了整句诗的全部语境，它"知道"下一个字该是什么了。

### 站点 ⑤：出口——向量变成字

「海」位置的向量与另一张 $(8192, 32000)$ 的大表做一次矩阵乘法，得到 32000 个分数——词表里每个候选片段各一个分数，这组分数叫 **logits**。「流」的分数最高，「留」「归」次之，「桌」是深深的负分。

分数经过 softmax 变成概率分布，再由**采样**策略从分布里挑出一个——你在请求里写的 `temperature: 0.7` 就是在这一站生效的。挑中的是「流」。第 7 章拆解这一站的每个参数。

### 站点 ⑥：自回归循环——一个字一个字地来

「流」诞生了，但请求还没结束。模型一次只能产出**一个** token——这是它的工作方式，不是实现偷懒。于是「流」被追加到序列末尾，$n$ 从 10 变成 11，整个流程回到站点 ④ 再跑一遍，产出「。」；再跑一遍，产出「欲」……直到模型输出"结束"标记或达到 `max_tokens`。

这个"生成一个、喂回去、再生成"的循环叫**自回归**（autoregressive）。它是 LLM 推理一切工程问题的总根源——聪明的读者马上会问：每轮循环里，前面那些字的计算不是重复了吗？问得好，这正是第 8 章的主题（剧透：有一个叫 KV Cache 的东西让重复计算免掉了，但代价是吃显存）。

### 站点 ⑦：显存与调度（外壳）

回到外壳。刚才的循环每转一圈，GPU 显存里就多出一小块缓存；同一时刻，还有几十个其他用户的请求也在各自转圈、各自占显存。一张 80 GB 的 GPU，140 GB 的权重都放不下（所以需要多卡协作，第 9 章开头讲），剩余空间还要被几十个请求的缓存瓜分——**显存管理是推理服务的第一战场**（第 9 章），而把几十个请求拼成一批来喂饱 GPU，是吞吐量的命脉（第 10 章）。

### 站点 ⑧：反分词与流式返回（外壳）

每当一个 token 在站点 ⑤ 诞生，外壳要完成最后两步转换才能把它交到你手上。

**反分词**（detokenization）：把 token 编号翻译回文字——分词的逆操作。这一步并非总是"查表即得"：第 1 章会讲到，一个汉字可能被 BPE 拆成两三个字节 token；反分词时必须把不完整的 UTF-8 字节**缓冲**起来，凑齐一个完整字符后才能输出。这就是你偶尔在流式输出中看到"半个乱码字一闪而过"的原因。

**流式推送**：反分词得到的文字立刻封装成一个 SSE（Server-Sent Events）数据帧推给你：

```
data: {"choices":[{"delta":{"content":"流"},"finish_reason":null}]}
data: {"choices":[{"delta":{"content":"。"},"finish_reason":null}]}
data: {"choices":[{"delta":{},"finish_reason":"stop"}]}
data: [DONE]
```

这就是你看到"逐字蹦"的直接原因：**字真的是一个一个生成的**，流式传输只是忠实转播了这个节奏。`finish_reason` 告诉你循环为什么停了——`stop`（模型自己说了再见）、`length`（撞到 `max_tokens` 上限）或 `content_filter`（安全过滤触发）。

## 0.4 先记住三个数字

细节可以慢慢学，但有三个数量级建议你现在就记住，全书会反复用到：

| 数字 | 含义 | 为什么重要 |
|------|------|-----------|
| **140 GB** | 70B 参数 × FP16 每参数 2 字节 | 单卡放不下 → 必须多卡；每生成一字要完整读一遍 → 速度被显存带宽卡死 |
| **8192** | 隐藏维度 $d_{\text{model}}$ | 全书几乎所有矩阵的行或列都是它；它是 80 层流水线的"管道直径" |
| **32000** | 词表大小 $V$ | 模型每一步的产出永远是这 32000 个候选上的一个概率分布——不多不少 |

本书全程使用同一个基准模型 **LLaMA-2 70B**：隐藏维度 8192、80 层、64 个查询头、8 个 KV 头、FFN 中间维度 28672、词表 32000（数值与 Meta 官方发布的模型配置一致）。不用现在理解每个名词——它们会在各自的章节登场；重要的是：**全书所有例子共用这一组真实数字**，你在第 2 章算出的形状，到第 9 章的显存账本里还能对上。

## 0.5 路线图：每一章放大哪一站

| 站点 | 放大它的章节 | 你将得到的答案 |
|------|-------------|---------------|
| ① 接入/格式化/调度 | 第 0 章预览，第 10 章放大 | messages 如何变成模型能吃的扁平 prompt？调度器怎么拼批？ |
| ② 分词 + ③ 嵌入 | 第 1 章 | 为什么词表是 3.2 万而不是 300 万？8192 个数凭什么“是”一个字的含义？ |
| ④ 位置 | 第 2 章 | 「白日」和「日白」在模型眼里如何区分？“旋转”怎么就编码了顺序？ |
| ④ 注意力 | 第 3 章 | 「海」怎么知道自己是“入海”的海？那条著名公式的每个符号为什么长那样？ |
| ④ 多头/GQA | 第 4 章 | 为什么 64 个问题只需要 8 套答案？这如何省下 87.5% 的缓存显存？ |
| ④ FFN | 第 5 章 | “鹳雀楼的下一句”这种知识存在哪里？三分之二的参数在干什么？ |
| ④ 残差/归一化 | 第 6 章 | 80 层串联为什么不把信号磨成噪声？ |
| ⑤ 出口采样 | 第 7 章 | temperature、top_p 在数学上到底做了什么？语法约束怎么叠加？ |
| ⑥ 自回归循环 | 第 8 章 | 为什么第一个字慢、后面的字匀速？KV Cache 缓存了什么？多轮对话为什么越来越贵？ |
| ⑦ 显存 | 第 9 章 | 显存明明够，为什么还 OOM？操作系统的分页怎么救了 LLM？ |
| ⑧ 反分词/流式 + 调度 | 第 10 章 | token 如何变回文字并推给你？GPU 算力用不满的真凶是谁？vLLM 凭什么快 10 倍？ |

阅读顺序就是数据流的顺序。如果你只关心某一站，每章开头的"前置依赖"和附录 A 的速查表可以帮你空降。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 两层世界 | 内核（算法：字符串→分布）+ 外壳（工程：接入/格式化/调度/显存/反分词/流式），本书两层都讲 |
| 八个站点 | 接入+格式化 → 分词 → 嵌入 → 80 层车间 → 出口采样 → 自回归循环 → 显存调度 → 反分词+流式返回 |
| 一次一个 | 模型每步只产出一个 token；“逐字蹦”是生成节奏的忠实转播 |
| 三个数字 | 140 GB（权重）、8192（管道直径）、32000（每步的候选面） |
| 一组数值 | 全书共用 LLaMA-2 70B 的真实参数，任何一章的数字都能与其他章对账 |

## 练习

1. 70B 参数用 FP16（每参数 2 字节）存储需要多少 GB？如果换成 INT4（每参数 0.5 字节）呢？算完你就理解了为什么"量化"是显存战争的重要武器（第 9 章见）。
2. 把 0.3 节的八个站点分成两类：哪些主要消耗 CPU，哪些主要消耗 GPU？先凭直觉分类，读完第 10 章·吞吐引擎后回来核对。
3. ★ 站点 ⑥ 说每轮循环都要"回到站点 ④ 再跑一遍"。假设 prompt 有 10 个 token、要生成 32 个 token：最朴素的实现下，第 1 个字和第 32 个字各需要处理多长的序列？总计算量随生成长度如何增长？写下你的估算，第 8 章·自回归与 KV Cache 将给出精确答案。

## 延伸阅读

- Vaswani et al., *Attention Is All You Need* (NeurIPS 2017)——一切的起点，读完本书 Part II 后再读会格外顺畅
- Touvron et al., *LLaMA 2: Open Foundation and Fine-Tuned Chat Models* (2023)——本书基准模型的官方技术报告
- Pope et al., *Efficiently Scaling Transformer Inference* (MLSys 2023)——从系统视角划分推理流水线的经典论文，本章"两层世界"的划分与之呼应

---

下一站，我们在解剖图的站点 ②③ 停下来，把"字符串如何变成 $(10, 8192)$ 矩阵"这件事拆到底——包括一个反直觉的事实：**模型从头到尾没见过一个汉字**。

➡️ [第 1 章 · 分词与嵌入：语言的数字化两级跳](./part1-chapter01-分词与嵌入.md) ｜ [返回目录](./README.md)
