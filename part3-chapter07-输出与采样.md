# 第 7 章 · 输出与采样:分布变成决定

> **Part III · 走出模型** 从这里开始。这一部分（第 7~8 章）回答一个层次的问题：**模型的产出是概率分布而非文字——分布如何变成字，字又如何变成流？**
>
> **我们在解剖图上的位置**：站点 ⑤（出口采样），并在结尾推开站点 ⑥（自回归循环）的门。
>
> **本章回答的问题**：走完 80 层的 $(10, 8192)$ 矩阵，如何坍缩成「流」这一个字？你在 API 里调的 temperature、top_p、repetition_penalty，各自在数学上动了分布的哪个部位？
>
> **前置依赖**：第 6 章·残差与归一化（第 80 层输出的来历与尺度问题）、第 3 章·注意力（因果掩码决定了谁看得到全句）、第 1 章·分词与嵌入（词表与嵌入矩阵）。

---

## 7.1 收尾两连:Final Norm,然后只取一行

### 先兑现第 6 章的 ★ 题

答案就是一个词：**归一化**。主干道经历 160 次未校准的加法后尺度已经漂移，而接下来的 LM Head 是一次巨大的点积运算——第 3 章讲过点积对尺度多敏感。所以第 80 层输出先过一次**最终归一化**（Final Norm，仍是 RMSNorm），给全程自由生长的残差流做一次总校准。这是 LLaMA 结构里容易被忽略但不可省略的一环：Pre-Norm 架构把归一化欠账全部攒到了出口，Final Norm 是最后的结账。

### 十行里只用一行

校准后的矩阵仍是 $(10, 8192)$——十个字各有一个走完全程的向量。预测第 11 个字，用哪一行？

**只用最后一行**，即「海」位置的向量。理由在第 3 章的掩码图里已经画过：位置 9 是唯一能看到全部十个字的位置，它的向量是整句「白日依山尽，黄河入海」的最完整摘要。前九行不是废品——训练时它们各是一道并行练习题（第 3 章的"10 道题"）——但推理此刻，它们能提供的信息都已被位置 9 覆盖。这也是第 3 章练习 4 的正式答案。

取出末行并记为 $h: (1, 8192)$。整个 80 层车间的劳动成果，浓缩在这 8192 个数里。

## 7.2 LM Head:与 32000 个候选者一次对表

$h$ 是语义空间中的一个点。要变成文字，需要回答：词表中 32000 个候选 token，谁与这个点最"合拍"？执行这次全员对表的是 **LM Head**——一个 $(8192, 32000)$ 的矩阵 $W_{\text{out}}$：

$$
\text{logits} = h\, W_{\text{out}}, \qquad \text{logits}: (1, 32000)
$$

几何上，$W_{\text{out}}$ 的每一**列**是词表中一个 token 的"输出方向"；$h$ 与第 $j$ 列的点积衡量当前语境与候选 $j$ 的对齐程度。一次矩阵乘法 = 32000 次点积同时完成。

输出叫 **logits**：32000 个未归一化的分数。一个必须刻进直觉的事实：**这个长度恒为 $V = 32000$**——输入 10 个字是它，输入 10 万字还是它；模型每一步的产出永远是"整个词表上的一张打分表"，与序列长短、层数深浅无关。我们的例子里（数值为示意）：

```
logits 中的几个代表:

  流:  13.6   ← 名句 + 韵脚 + 水流意象三路证据汇聚
  归:   8.9   ← 语义可通，但不合原句
  去:   7.2
  中:   5.8
  桌:  -3.5
  跑:  -5.1
   ⋮        （其余三万余候选大多深埋在负分区）
```

> **一个对称性备注**：LM Head 的形状 $(8192, 32000)$ 恰是第 1 章嵌入矩阵 $(32000, 8192)$ 的转置。有些模型（GPT-2 等）索性让两者共享参数（weight tying）——进门查表和出门对表用同一本字典，省 2.6 亿参数并强制输入输出空间对齐。LLaMA-2 为质量考虑选择各存一份，但"LM Head ≈ 嵌入的逆操作"这个对称直觉值得保留。

## 7.3 softmax:打分表变概率表

logits 有正有负，先经 softmax 变成合法的概率分布（与第 3 章同一个函数，这次作用在词表维度上）：

$$
P[j] = \frac{\exp(\text{logits}[j])}{\sum_{j'=1}^{32000} \exp(\text{logits}[j'])}
$$

```
P（示意）:  流 0.87   归 0.008   去 0.0015   …   其余合计 ≈ 0.12
```

到这里，模型的本职工作**已经全部完成**。请注意这个容易被忽略的分界线：**模型的产出从来不是"一个字"，而是"下一个字在全词表上的概率分布"**。从分布里挑出具体的字，是一个独立的、不含任何神经网络的环节——**解码**（decoding）。你在 API 里调的那一排参数，全部住在这个环节里。

## 7.4 解码:对同一张分布表动的几种手术

### 两个极端先划出光谱

**贪心解码**（greedy）：永远选概率最高的字。确定、可复现，但臭名昭著地容易陷入复读循环（"的的的……"），文本平庸僵硬——研究表明人类文本恰恰不是"步步最优"的（Holtzman et al., 2020）。

**完全随机采样**：严格按分布掷骰子。「流」有 87% 的机会当选，但长尾里三万个低概率字合计还占着百分之十几——偶尔真会抽出一个「桌」，一字毁一句诗。

实用的解码策略全部落在两极之间，本质都是**先给分布做手术，再掷骰子**。手术刀有四把，按生产系统（vLLM 等）的实际执行顺序介绍。

### 手术一:重复惩罚——先清算历史

三个相关参数都在压制"复读机"倾向，作用于 softmax 之前的 logits：

| 参数 | 机制 | 手感 |
|------|------|------|
| repetition_penalty | 已出现过的 token，正分除以 $r$、负分乘以 $r$（$r > 1$） | 按"出现过与否"一刀切 |
| frequency_penalty | 按出现**次数**线性扣分：$\text{logit} \mathrel{-}= c \cdot \text{count}$ | 越常出现扣越狠 |
| presence_penalty | 只要出现过就扣固定分 | 鼓励换话题 |

写诗场景它们几乎不动声色；写长文档时，它们是防止模型三句话绕回同一个短语的主力。

### 手术二:temperature——调节分布的对比度

把 logits 除以温度 $T$ 再做 softmax：

$$
P[j] = \text{softmax}\!\left(\frac{\text{logits}}{T}\right)[j]
$$

回忆第 3 章：softmax 的指数会放大分差。除以 $T < 1$ 等于把分差拉大——分布更尖锐；$T > 1$ 把分差压小——分布更平缓。用我们的例子感受三档（示意值）：

| $T$ | 「流」 | 「归」 | 长尾合计 | 效果 |
|-----|--------|--------|----------|------|
| 0.2 | 0.999 | ~0.001 | ≈ 0 | 近乎贪心，适合代码/事实问答 |
| 0.7 | 0.87 | 0.008 | 0.12 | 默认区间：稳中有变 |
| 1.5 | 0.44 | 0.09 | 0.47 | 长尾抬头，适合头脑风暴，也更易跑偏 |

$T \to 0$ 严格退化为贪心。命名借自统计物理：温度越高，系统越无序。

### 手术三:截断长尾——top-k、top-p 与 min-p

温度调的是形状，截断砍的是资格——把长尾候选直接清零再重新归一：

- **top-k**：只保留分数最高的 $k$ 个（如 50 个）。简单，但 $k$ 是死数：分布尖锐时 50 个太宽松，平缓时又太苛刻。
- **top-p**（核采样）：保留累积概率刚达到 $p$（如 0.9）的最小候选集。分布尖时可能只留两三个，平时留几百个——**候选集大小随分布形状自适应**，这是它成为行业默认的原因。
- **min-p**（2024 年后流行，vLLM 等已内置）：保留所有满足 $P[j] \geq p_{\min} \times P_{\max}$ 的候选——门槛锚定在"当前最高分"上。高温场景下它比 top-p 更稳：即使分布被 $T$ 压得很平，"不到冠军几分之一的候选一律出局"这条规则依然锋利。

我们的例子里（top_p = 0.9）：「流」0.87 + 「归」0.008 + … 只需三四个候选即达 0.9——「桌」「跑」被彻底排除，多高的温度都救不了它们。

### 组合与顺序

生产引擎的执行顺序（以 vLLM 的采样器为准）：**重复惩罚 → temperature → top-k / top-p / min-p → 语法约束 → 掷骰子**。顺序有讲究：惩罚要在温度前（作用在原始分数上语义才稳定），截断要在温度后（截的是最终形状的尾巴），语法约束放在最后（在已经缩小的候选集上做硬屏蔽）。这次掷骰子的结果——不出意外——是「流」。

### 手术五：语法约束——只允许合法 token 参选

四把手术刀都在调节"哪个字概率更高"，但生产环境中有一类需求比"概率"更硬：**输出必须符合某种结构**。比如你要求模型返回合法 JSON、调用特定函数、或按预定义的语法生成——这时“概率低”不够，必须"概率为零"。

**约束解码**（constrained decoding）的做法是在每步采样前，用一个**有限状态机**（或等价的语法解析器）跟踪"到目前为止已生成的 token 序列处于语法的哪个状态"，然后把**当前状态下不合法的 token 的 logits 设为 $-\infty$**：

```
例如要求输出 {"name": "...", "age": ...}：

已生成: {"name": "张
当前状态: 字符串未闭合 → 合法: 任何字符 / 引号
                              不合法: { } [ ] : , 数字…

→ 将不合法 token 的 logits 设为 -∞，它们经 softmax 后权重严格为零
```

这与因果掩码（第 3 章）是同一种手法：借 $e^{-\infty} = 0$ 在 softmax 内部完成硬屏蔽，无需事后归一化。区别在于：掩码是静态的（与生成内容无关），语法约束是**动态的**——每步的合法集取决于已生成的前缀。

主流实现（vLLM 的 guided decoding、Outlines、llama.cpp 的 GBNF 语法）都支持在 API 层传入 JSON Schema 或正则表达式，引擎内部编译成状态机后在每步采样前执行屏蔽。对模型权重零修改、对输出分布的影响是确定性的——**这是纯推理侧的工程能力，与本书定位完美契合**。

我们的写诗场景用不上语法约束（诗不是结构化数据），但你的下一次 API 调用里如果带了 `response_format: {"type": "json_object"}`，背后就是这把手术刀在工作。

## 7.5 代码锚点:完整的采样流水线

```python
import numpy as np

def softmax(s):
    e = np.exp(s - s.max()); return e / e.sum()

def sample(logits, prev_ids=(), rep_pen=1.1, T=0.7, top_p=0.9, seed=0):
    """logits: (V,) → 选出的 token id。顺序：惩罚 → 温度 → top-p → 采样"""
    logits = logits.astype(np.float64).copy()
    for i in set(prev_ids):                             # ① repetition penalty
        logits[i] = logits[i] / rep_pen if logits[i] > 0 else logits[i] * rep_pen
    p = softmax(logits / T)                             # ② temperature
    order = np.argsort(-p)                              # ③ top-p 截断
    keep = order[: np.searchsorted(np.cumsum(p[order]), top_p) + 1]
    p_keep = p[keep] / p[keep].sum()                    #    幸存者重新归一
    return np.random.default_rng(seed).choice(keep, p=p_keep)   # ④ 掷骰子

V = 32000
logits = np.full(V, -8.0)
liu, gui, qu = 5872, 7031, 4409          # 示意编号：流 / 归 / 去
logits[[liu, gui, qu]] = [13.6, 8.9, 7.2]

print(sample(logits))                     # 大概率输出 5872（「流」）
print(sample(logits, T=2.0, seed=7))      # 高温下「归」「去」开始有机会
```

## 7.6 一个字诞生了,但请求还没结束

从「白日依山尽，黄河入海」到「流」，内核的单程旅途全部走完：

```
字符串 ─分词→ 10 个整数 ─嵌入→ (10, 8192)
  ─[80 × (RMSNorm → GQA注意力(含RoPE) → 残差 → RMSNorm → FFN → 残差)]→ (10, 8192)
  ─Final Norm + 取末行→ (1, 8192)
  ─LM Head→ (1, 32000) logits
  ─惩罚/温度/截断/采样→ token id 5872 ─反查词表→ 「流」
```

但你的请求写的是 `max_tokens: 32`，诗也没补完。「流」会被追加到序列末尾，变成 11 个 token 的新输入，整条流水线**从头再跑一遍**，产出「。」；再跑一遍，产出「欲」……这个"生成一个、喂回去、再生成"的循环叫**自回归**（autoregressive），第 0 章站点 ⑥ 预告过它。

在跨进循环之前，用第 0 章练习 3 的视角想一个问题：第二圈的输入里，前 10 个 token 与第一圈**一模一样**——它们在 80 层里的所有计算，难道要原封不动重来一遍？如果你已经想起第 3 章埋的那颗种子（"历史字的 $k$/$v$ 一旦算出永不改变"），你其实已经推开了下一章的门。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| Final Norm | Pre-Norm 的归一化欠账在出口一次结清（第 6 章 ★ 题答案） |
| 只取末行 | 因果掩码下位置 9 独家看到全句，它是预测的唯一依据（第 3 章 ★ 题答案） |
| LM Head | $(8192, 32000)$：与全词表 32000 个输出方向一次性对表 |
| 产出恒为分布 | logits/概率长度永远是 $V$；"选哪个字"是模型之外的解码环节 |
| 四把手术刀 | 重复惩罚（清算历史）→ 温度（调对比度）→ top-k/p、min-p（砍长尾）→ 语法约束（硬屏蔽非法 token）→ 采样 |
| 顺序有讲究 | 惩罚在温度前、截断在温度后、约束在最后——生产引擎的固定管线 |
| 自回归入口 | 新字喂回输入再来一轮；历史计算是否要重做，是下一章的全部悬念 |

## 练习

1. 对 logits $= (3, 1, 0.5)$ 手算 $T = 0.5, 1, 2$ 三档下的 softmax 分布，观察"对比度"的变化方向。
2. 构造一个 5 个候选的分布，使 top-k（$k=3$）与 top-p（$p=0.9$）保留的候选集不同；再构造一个高温压平的分布，说明 min-p（$p_{\min}=0.1$）为什么比 top-p 截得更合理。
3. 7.4 节说惩罚必须在温度**之前**。构造一个例子说明：若顺序颠倒，同一组参数在 $T=0.5$ 和 $T=2$ 下的惩罚力度会不一致。
4. ★ 生成第 12 个字时输入是 11 个 token。问：前 10 个 token 在全部 80 层里的 $k$、$v$ 向量，与上一轮相比有没有任何数值变化？为什么？（钥匙在第 3 章 3.4 节的最后一段——这道题就是第 8 章的门票）

## 延伸阅读

- Holtzman et al., *The Curious Case of Neural Text Degeneration* (ICLR 2020)——top-p 的提出；"人类文本不是步步最优"的实证
- Fan et al., *Hierarchical Neural Story Generation* (ACL 2018)——top-k 采样的出处
- Nguyen et al., *Min-p Sampling for Creative and Coherent LLM Outputs* (2024)——min-p 的提出与高温场景消融
- Press & Wolf, *Using the Output Embedding to Improve Language Models* (2017)——weight tying 的系统研究

---

⬅️ 上一站：[第 6 章 · 残差与归一化](./part2-chapter06-残差与归一化.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 8 章 · 自回归与 KV Cache：循环的经济学](./part3-chapter08-自回归与KVCache.md)
