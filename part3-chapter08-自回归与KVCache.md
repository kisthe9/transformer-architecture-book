# 第 8 章 · 自回归与 KV Cache:循环的经济学

> **我们在解剖图上的位置**：站点 ⑥（自回归循环）——内核的最后一站，也是通往工程外壳的天桥。
>
> **本章回答的问题**：为什么朴素的逐字生成在计算上是三次方增长的灾难？KV Cache 缓存了什么、凭什么敢缓存？为什么用大模型时总是"第一个字等得久，后面的字匀速蹦"？
>
> **前置依赖**：第 3 章·注意力（因果掩码的种子）、第 7 章·输出与采样（每步只产出一个 token）、第 4 章·多头与 GQA（KV 缓存量的账单基数）。

---

## 8.1 朴素循环:一笔三次方的账

第 7 章结尾，「流」被追加进序列。最直白的实现方式是每一步都全量重跑：

```
第 1 步: 输入 [白,日,依,山,尽,，,黄,河,入,海]        → 全量前向 → 流
第 2 步: 输入 [白,日,依,山,尽,，,黄,河,入,海,流]     → 全量前向 → 。
第 3 步: 输入 [白,…,流,。]                          → 全量前向 → 欲
  ⋮
第 22 步: 输入 [白,…,欲,穷,千,里,目,，,更,上,一,层]   → 全量前向 → 楼
```

第 2 步里，前 10 个 token 的嵌入、80 层里的全部投影、注意力、FFN——**与第 1 步的计算逐比特相同**，却被原封不动重算了一遍。第 3 步再重算一遍前 11 个。这就是第 0 章练习 3 让你估算的账，现在正式结清：

- 第 $t$ 步的注意力要算 $(t, t)$ 的分数矩阵，代价约 $O(t^2 d)$；
- 生成 $n$ 个字要跑 $n$ 步，总代价 $\sum_t O(t^2 d) = O(n^3 d)$。

**三次方**。把上下文拉到 4096，这个数字直接宣判朴素方案死刑。省钱的直觉人人都有——老 token 的计算结果存下来复用不就行了？但缓存有一条铁律：**只有不会变的东西才能缓存**。老 token 的中间结果，会随着新 token 的加入而改变吗？

## 8.2 缓存的合法性:历史是只写一次的

### 收割第 3 章的种子

这正是第 3 章 3.4 节末尾埋下、第 7 章 ★ 题让你带进门的那颗种子。因果掩码规定：位置 $i$ 的一切计算只依赖位置 $\leq i$。于是：

> 「海」在每一层的 $k$、$v$，只由「白日依山尽，黄河入海」这十个字决定。后来生成的「流」「。」「欲」……**在数学上不存在任何路径**影响到它们。

换句话说，一个 token 的 $k$/$v$ 在它首次被算出的那一刻就是**终稿**——整个生成过程中一个比特都不会再变。这就是缓存合法性的完整证明，不多一个字。反面教材是双向注意力的 BERT：每个新 token 都会改写所有历史表示，那种架构里根本不存在"可以缓存的东西"。因果掩码当年是为"不许偷看"设计的，却在推理侧意外派发了一份大礼——好的设计总是这样一鱼多吃。

### 为什么不缓存 Q

三件套里 $q$ 为什么没有缓存资格？看它的生命周期：预测下一个字只需要**最后一个位置**的输出（第 7 章），而算这个输出时，历史 token 只以 $k$（被匹配）和 $v$（供内容）的身份出场；它们自己的 $q$——"我想问什么"——在它们自己当预测位的那一步用完就再也没人需要了。**$q$ 是一次性的，$k$/$v$ 是传家的。** 这就是 KV Cache 名字里没有 Q 的原因。

## 8.3 带缓存的循环:每步只算一个新字

缓存就位后，生成第 12 个字的流程脱胎换骨：

```
第 2 步（有 KV Cache）:

  输入: 只有「流」一个 token                    ← 不再是整句
  ① 「流」查嵌入表 → (1, 8192)
  ② 每层内部:
       只为「流」投影 q, k, v（RoPE 按位置 10 旋转）
       k, v 追加进本层缓存（append-only，永不改写旧条目）:
         K_cache: [k白, k日, …, k海, k流]     形状 (11, 8, 128)
         V_cache: [v白, v日, …, v海, v流]
       注意力: q流 · K_cacheᵀ → (1, 11) 预算 → 加权 V_cache → 输出
  ③ 过 FFN → 走完 80 层 → Final Norm → LM Head → 采样 → 「。」
```

每步的注意力从 $(t, t)$ 矩阵塌缩成 $(1, t)$ 的一行；总代价从 $O(n^3 d)$ 降到 $O(n^2 d)$——4096 长度下省掉约三个数量级的计算。**KV Cache 是"用显存换计算"这类交易里回报率最高的一单**，没有任何现代推理引擎不做它。

代价当然是显存，账在第 4 章已经算过一半：GQA-8 下每 token 每层 $2 \times 8 \times 128 \times 2\,\text{B} = 4$ KB，80 层、4096 上下文即 **1.34 GB/请求**——比 MHA 的 10.7 GB 已经文明了 8 倍，但它随并发数线性增长、随上下文线性增长，仍然是推理显存的头号动态支出。这笔钱怎么花得更聪明，是第 9 章的战场。

### 代码锚点:可验证的增量生成

三十行验证"增量计算 ≡ 全量重算"——KV Cache 不是近似，是精确等价：

```python
import numpy as np

def softmax(s):
    e = np.exp(s - s.max(-1, keepdims=True)); return e / e.sum(-1, keepdims=True)

d_k = 16
rng = np.random.default_rng(0)
Wq, Wk, Wv = ((rng.normal(size=(d_k, d_k)) / 4).astype(np.float32) for _ in range(3))
xs = rng.normal(size=(7, d_k)).astype(np.float32)     # 7 个 token 的输入向量

def full_forward(X):
    """全量重算：整段序列过一遍因果注意力，返回最后一行输出"""
    Q, K, V = X @ Wq, X @ Wk, X @ Wv
    t = len(X)
    M = np.triu(np.full((t, t), -np.inf), 1)
    return (softmax(Q @ K.T / np.sqrt(d_k) + M) @ V)[-1]

K_cache = np.zeros((0, d_k), np.float32)              # 增量路径：只带缓存
V_cache = np.zeros((0, d_k), np.float32)
for t in range(1, 8):
    x_new = xs[t-1:t]                                 # 每步只喂 1 个新 token
    q, k, v = x_new @ Wq, x_new @ Wk, x_new @ Wv
    K_cache = np.vstack([K_cache, k])                 # append-only
    V_cache = np.vstack([V_cache, v])
    o_inc = (softmax(q @ K_cache.T / np.sqrt(d_k)) @ V_cache)[0]   # (1, t) 无需掩码
    assert np.allclose(o_inc, full_forward(xs[:t]), atol=1e-5)

print("7 步增量输出与全量重算完全一致 —— KV Cache 是精确等价，不是近似")
```

两个细节值得咂摸：增量路径里**掩码消失了**——新 token 本来就只见得到缓存里的历史，掩码的语义被"缓存里只有过去"天然实现；而 `vstack` 的 append-only 语义正是 8.2 节"终稿"性质的代码化身。

## 8.4 一次生成的两副面孔:Prefill 与 Decode

引入缓存后，一次完整的请求处理自然裂解成性质截然不同的两个阶段。

**Prefill（预填充）**：开局处理整个 prompt。十个字（真实场景可能是几千个）一次性并行前向——每层都是"矩阵 × 矩阵"的大块运算，一举建好全部初始缓存，并产出第一个字「流」。

**Decode（解码）**：此后的循环。每步只处理 1 个新 token——每层退化为"向量 × 矩阵"的细碎运算，追加一条缓存，产出一个字。

| | Prefill | Decode |
|---|---------|--------|
| 输入规模 | 整个 prompt（可达数千 token） | 1 个 token |
| 运算形态 | 矩阵×矩阵，GPU 吃得满 | 向量×矩阵，GPU 吃不饱 |
| 瓶颈 | **算力**（compute-bound） | **显存带宽**（memory-bound）——每步都要把全部权重从显存读一遍，却只算一个 token 的量 |
| 用户体感 | TTFT（Time To First Token，首字延迟） | TPOT（Time Per Output Token，字间间隔） |

这张表解释了每个 LLM 用户都体验过却少有人深究的现象：

> **第一个字等得久——Prefill 正在啃整个 prompt；后面的字匀速往外蹦——Decode 每步的工作量恒定。** prompt 越长首字越慢；蹦字速度却与 prompt 长度几乎无关。

"Decode 是带宽瓶颈"这半句话的分量，怎么强调都不过分：每生成一个字，GPU 要从显存搬运 140 GB 权重外加全部 KV Cache，而真正的计算只有一个 token 的量——**算术单元绝大部分时间在等数据**。把这句话量化成毫秒和 token/s，是第 10 章的开场白（章末 ★ 题请你先算一遍）。

## 8.5 循环的终点与主线的交棒

循环不会永动。每步采样之后，引擎检查三类停止条件，命中即收工：

1. **EOS token**：模型自己输出的“到此为止”标记（训练时学会，词表里有正式编号）；
2. **max_tokens**：调用方设的长度上限（我们的请求是 32）；
3. **stop 字符串**：调用方指定的停止词命中。

第三类有一个容易忽略的工程细节：stop 字符串可能跨越 token 边界。比如 stop = "。"，但分词器可能把"。"与下一个字合并成一个 token——引擎必须在每步维护一个**滑动窗口**，对已生成的 token 做后缀匹配，才能及时截断。这也是为什么 stop 字符串不宜太短（单字符容易误触）也不宜太长（匹配延迟增大）。

我们的请求最终停在：

```
白日依山尽，黄河入海 → 流 → 。 → 欲 → 穷 → 千 → 里 → 目 → ，
                     → 更 → 上 → 一 → 层 → 楼 → 。 → <EOS>
```

《登鹳雀楼》补写完成，这个请求的 KV Cache 随之释放。**算法内核的故事到此全部闭环**。

## 8.6 多轮对话的代价:为什么越聊越贵越慢

真实的用户交互不是一锤子买卖，而是多轮对话。这里有一个对开发者至关重要的事实，必须用本章的语言说清楚：

> **推理服务是无状态的。模型不“记得”上一次对话——所谓“上下文记忆”完全是客户端的错觉。**

每一轮对话，客户端必须把**从第一句到最新一句的完整 messages 数组**重新提交；服务端每次都是“第一次见到”这段对话。用本书的语言说：

```
第 1 轮:  prompt = [system, user1]                         → 10 token → Prefill → 生成 asst1
第 2 轮:  prompt = [system, user1, asst1, user2]            → 35 token → 重新 Prefill 全部 35 个
第 3 轮:  prompt = [system, user1, asst1, user2, asst2, user3] → 70 token → 再次全量 Prefill
```

每一轮都要把全部历史重新走一遍 Prefill——因为服务端上一轮结束后已经释放了所有 KV Cache。三个直接后果：

1. **越聊越贵**：第 $k$ 轮的 Prefill 代价是 $O(k^2)$（序列长度随轮次线性增长，Prefill 计算量是序列长度的平方）；
2. **越聊越慢**：TTFT（首字延迟）随历史长度增长——第 10 轮的首字等待可能是第 1 轮的几十倍；
3. **上下文窗口会溢出**：全部历史必须一次装进上下文窗口（如 128K token），超了就不得不截断早期对话——"失忆"不是模型老了，是装不下了。顺带一问：这个上限本身是谁定的、为什么不同服务商给得不一样？答案在第 2 章 §2.7 的三层天花板。

解药在第 9 章：**前缀缓存**（prefix caching）。多轮对话中前 $k-1$ 轮的内容与上一轮完全相同，其 KV 也逐比特相同（第 8.2 节的缓存合法性）——如果引擎能跨请求识别并复用这段前缀的 KV 块，第 $k$ 轮的 Prefill 只需要处理**新增部分**，TTFT 从 $O(k^2)$ 降到 $O(\text{新增}^2)$。这正是 RadixAttention（SGLang）和 vLLM V1 的近零开销 prefix caching 在做的事——第 9 章会展开。

顺带挂一个当代注脚：2024 年后的“推理模型”（o1、DeepSeek-R1 等）在回答前会先生成一长段用户看不见的“思考”——那不是什么新机制，恰恰就是本章这个循环的**刻意延长**：思考 token 逐个走同样的 80 层、逐个进同样的 KV Cache。它为什么能提高答题正确率、又会把本书哪几本账单翻出来重算，解剖见附录 D·思考的解剖。

回望这条因果链，它是全书骨架的上半段：

> 因果掩码（第 3 章）⟹ 历史 K/V 不变 ⟹ 可以缓存（本章）⟹ 缓存吃显存 ⟹ 必须 GQA（第 4 章）⟹ 还不够——**显存怎么管、GPU 怎么喂**，工程外壳的两大战役接棒。

站点 ⑦ 和 ⑧ 在前方等着：1.34 GB/请求的缓存如何在 80 GB 的卡上安放几十份而不碎（第 9 章·显存战争），以及调度器如何把带宽瓶颈的 Decode 拼成高吞吐的流水线（第 10 章·吞吐引擎）。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 朴素循环 | 每步全量重算，$O(n^3 d)$——三次方增长不可行 |
| 缓存合法性 | 因果掩码 ⟹ 每个 token 的 $k$/$v$ 首算即终稿（第 3 章种子收割） |
| 为什么没有 Q | 预测只用末位输出，历史只以 $k$/$v$ 身份出场；$q$ 一次性，$k$/$v$ 传家 |
| 增量生成 | 每步只算 1 个新 token，注意力 $(t,t) \to (1,t)$；精确等价可代码验证 |
| 代价 | GQA-8 下 1.34 GB/请求（4096 上下文），随并发与长度线性增长 |
| 两副面孔 | Prefill 算力瓶颈（建缓存+首字），Decode 带宽瓶颈（逐字蹦） |
| 体感对照 | 首字慢 = Prefill 在啃 prompt；后字匀速 = Decode 步步等量 |
| 多轮对话 | 服务无状态，每轮重新 Prefill 全部历史；前缀缓存是解药（第 9 章） |

## 练习

1. 完整推导 8.1 节的复杂度：第 $t$ 步注意力为何是 $O(t^2 d)$？对 $t$ 从 1 加到 $n$ 为何是 $O(n^3 d)$ 量级？带缓存后逐步代价与总代价各变成多少？
2. 一场 32K token 的长对话，用本书基准模型（GQA-8, FP16）的 KV Cache 是多少 GB？如果当年没有 GQA（MHA）呢？（对账见附录 A）
3. 8.3 节的代码锚点里，增量路径没有掩码矩阵。如果有人错误地在增量路径也加上 $(1, t)$ 的全 $-\infty$ 上三角……会发生什么？动手改一行验证你的判断。
4. ★ Decode 每步只算 1 个 token 的量，却要从显存读全部权重（140 GB）。假设显存带宽 2 TB/s、忽略计算时间：单步 Decode 的时间**下界**是多少毫秒？由此推出单请求生成速度的上限是多少 token/s？（这两个数字是第 10 章的开场白，先自己算出来，读到时会格外解气）

## 延伸阅读

- Pope et al., *Efficiently Scaling Transformer Inference* (MLSys 2023)——Prefill/Decode 两阶段特性与显存瓶颈的系统分析
- Chen, *Transformer Inference Arithmetic* (2022, 博客)——用纸笔推算推理延迟的经典入门，练习 4 的方法论出处
- Leviathan et al., *Fast Inference from Transformers via Speculative Decoding* (ICML 2023)——挑战"一步一字"铁律的旁门左道，第 10 章正文见
- Xiao et al., *Efficient Streaming Language Models with Attention Sinks* (ICLR 2024)——当缓存长到装不下时"丢哪些 token"的反直觉答案

---

⬅️ 上一站：[第 7 章 · 输出与采样](./part3-chapter07-输出与采样.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 9 章 · 显存战争：分页拯救 KV Cache](./part4-chapter09-显存战争.md)
