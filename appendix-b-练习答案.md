# 附录 B · 练习提示与答案

> 验证型题给完整解；开放型/思考型题给提示与判断要点。★ 题的完整答案通常在其指向的章节正文中，这里给出核对版。

---

## 第 0 章 · 鸟瞰

**0.1** FP16：$70 \times 10^9 \times 2\ \text{B} = 140$ GB。INT4：$70 \times 10^9 \times 0.5\ \text{B} = 35$ GB——刚好能塞进半张 80 GB 卡，这就是量化改变部署格局的原因（第 9、10 章）。

**0.2** 主要耗 CPU：①接入排队、⑧流式返回（还有调度决策、反分词、判停）；主要耗 GPU：③嵌入、④80 层车间、⑤出口对表。②分词在 CPU。核对：第 10 章 10.4 节的控制平面/数据平面表。

**0.3 ★** 第 1 个字处理 10 个 token，第 32 个字处理 41 个。朴素实现每步代价 $\propto t^2$，总量 $\sum t^2 \propto n^3$——三次方增长。精确展开见第 8 章 8.1 节。

## 第 1 章 · 分词与嵌入

**1.1** 接 1.2 节第 3 步之后：第 4 步合并 `(lo, w)` → "low"（频次 7：low×5 + lower×2）；第 5 步合并 `(w, i)`… 注意此时 "wi" 频次为 5（widest×3 + wide×2），若与其他对并列取先出现者。合并后 lower → `low e r`。要点：每一步都要**重新统计**，被合并的对会改变相邻关系。

**1.2** 操作题。预期观察：32000 词表下中文 token 数约为英文 1.5~2 倍；换 128K 词表模型后差距明显收窄。

**1.3** $32000 \times 8192 = 262{,}144{,}000 \approx 2.62$ 亿；FP16 下 $\times 2$ B $= 0.52$ GB；占比 $0.262 / 69 \approx 0.38\%$。与附录 A.2 一致。

**1.4 ★** 无位置机制时注意力置换等变（第 2 章 2.2 节）：每个 token 的输出向量数值不变，只是随行序对调。但预测读取的是"末行"——对调后末行坐着「白」的输出而非「海」的，**预测会变**。这题的精妙处：变化不是因为计算结果变了，而是因为"谁是最后一个"变了。

## 第 2 章 · 位置编码

**2.1** $m\theta = 60°$，$n\theta = 150°$。左边：$R(60°)q = (\cos 60°, \sin 60°) = (0.5, 0.866)$；$R(150°)k = (-\sin 150°, \cos 150°) = (-0.5, -0.866)$；点积 $= -0.25 - 0.75 = -1$。右边：$(n-m)\theta = 90°$，$R(90°)k = (-1, 0)$，$q \cdot (-1, 0) = -1$。相等 ✓。

**2.2** 旋转以 $2\pi$ 为周期：距离差为 $2\pi/\theta = 62.83$ 的两对位置贡献相同。如 $d_1 = 5$ 与 $d_2 = 5 + 62.83 \approx 67.8$（取整数场景即 $d$ 与 $d + 63$ 近似混淆）。多频率设计正是为了让"所有平面同时混淆"几乎不可能发生。

**2.3 ★** 若 $v$ 也旋转，注意力取回的内容向量会随位置系统性偏转——同一个字在位置 3 和位置 30 贡献的"发言内容"不再相同，语义被位置污染（正是第一代加法方案的病）。位置应该只影响"谁相关"，不影响"取什么"。

**2.4** 运行后两个点积相等（差异仅浮点误差级）。这验证了高维 RoPE 的相对性：点积只依赖距离差（+2），与绝对位置 (3,5) 或 (7,9) 无关。

**2.5** 精确公式（第 4 章给出）：GQA-8、FP16 下每 token 每层需存 K 与 V 两份，$2 \times 8\ \text{头} \times 128 \times 2$ B $= 4$ KB，乘 80 层 $= 320$ KB/token，乘 200K $= 320\ \text{KB} \times 200{,}000 \approx 65$ GB（第 9 章 9.1 节对账）。所以正确直觉是**每 token 每层 KB 级**——猜 MB 级会高估一千倍（那接近 MHA 全头缓存的量级：$\times 8 \approx 524$ GB）。估算的关键一步：先数"一个 token 在一层里要留几个数"（$2 \times$ 头数 $\times$ 头维度），再乘层数和长度。

## 第 3 章 · 注意力

**3.1** 自设数值题，自查要点：$S$ 是否对称？（一般不对称——$W_Q \neq W_K$）；掩码后每行和是否为 1；位置 0 的输出是否恰等于 $V[0]$。

**3.2** $\mathrm{Var}(q_j k_j) = E[q_j^2 k_j^2] - (E[q_j k_j])^2 = E[q_j^2]E[k_j^2] - 0 = 1$。独立项方差可加：$\mathrm{Var}(\sum_j q_j k_j) = \sum_j 1 = d_k$。∎

**3.3** 以位置 4 行、缩放后分数全为 0 为例：正确方案 softmax 后前 5 个位置各 0.2、其余 0。"事后清零"方案：先对全部 10 个位置 softmax（各 0.1），再清零后 5 个——剩余权重和只有 0.5。破坏"预算总和为 1"的是后者；输出向量被整体缩小一半，且缩小比例随位置变化——灾难性的系统偏差。

**3.4 ★** 取位置 9（「海」）。它是唯一注意力预算覆盖全部 10 个字的位置，其输出是全句的最完整摘要。正式论证见第 7 章 7.1 节。

## 第 4 章 · 多头与 GQA

**4.1** GQA-8：$W_Q$ 67.1M + $W_K$ 8.4M + $W_V$ 8.4M + $W_O$ 67.1M ≈ **151M**。MHA：$4 \times 67.1 = 268.4$M。节省 $117.4 / 268.4 \approx 44\%$。与附录 A.2 一致。

**4.2** $g = 16$：每 token 每层 $2 \times 16 \times 128 \times 2 = 8$ KB，4096 上下文 ×80 层 = **2.68 GB**。MQA（$g=1$）：0.5 KB/token/层 → **0.168 GB**。

**4.3** 物理复制成 64 份：K/V 显存占用 ×8（从 8 组变 64 组的量），且每步注意力要把这 8 倍数据从 HBM 搬进计算单元——带宽消耗同步 ×8。在 Decode 本就带宽瓶颈的背景下（第 10 章），这等于亲手把 GQA 的收益全部退还。

**4.4 ★** RoPE 的旋转角依赖位置且作用在 K 的每个维度对上（第 2 章）。低秩压缩是跨维度的线性混合——先压缩再解压后，"哪个维度对该转多少度"的对应关系已被搅乱，"解压后再旋转"与"旋转后再压缩"不等价。DeepSeek 的解法（K 拆两段、位置段不压缩）见第 9 章 9.5 节。

## 第 5 章 · FFN

**5.1** $3 \times 8192 \times 28672 = 704{,}643{,}072 \approx 705$M/层；80 层 = 56.37B；$56.37 / 70 \approx 81\%$。✓

**5.2** SiLU 在 $z<0$ 区间输出小负值而非零（最低约 $-0.28$，在 $z \approx -1.28$ 处），且梯度不为零。含义：阀门"虚掩"而非"焊死"——训练中被误关的槽位仍有梯度可以翻身；推理中弱抑制信号也能传递。ReLU 的"死神经元"问题正源于负区间零梯度。

**5.3** $xW_1 = (-1 \cdot 1 + 1 \cdot 0,\ -1 \cdot 0 + 1 \cdot 1,\ -1 \cdot (-1) + 1 \cdot 1) = (-1, 1, 2)$，ReLU 后 $a = (0, 1, 2)$：钥匙 B、C 命中。输出 $= 1 \cdot (0,2) + 2 \cdot (3,3) = (6, 8)$。（已数值验证）

**5.4 ★** 该层 FFN 退化为零函数，但残差使该子层整体成为恒等映射 $x + 0 = x$——模型不会崩溃，只是"白养一层"。完整答案见第 6 章 6.2 节"优雅降级"。

## 第 6 章 · 残差与归一化

**6.1** $0.95^{80} = e^{80 \ln 0.95} \approx e^{-4.10} \approx 0.017$——只剩 1.7%。有残差后每层的梯度传递因子是 $1 + f'$ 而非 $f'$，"连乘全部小于 1 的数"这个前提不再成立，衰减模型失效。

**6.2** 均方根 $= \sqrt{(1 + 4 + 4)/3} = \sqrt{3}$。输出 $= (1/\sqrt{3}, 2/\sqrt{3}, 2/\sqrt{3}) \approx (0.577, 1.155, 1.155)$。验证：输出平方均值 $= (1/3 + 4/3 + 4/3)/3 \cdot 3 = 3/3 = 1$ ✓。

**6.3** 要点：Pre-Norm 的梯度直通路径只经过加法节点（导数 1）；Post-Norm 的直通路径每层必须穿过一次 Norm（其导数依赖当前激活统计量，通常 < 1 且不稳定），80 层累积后深层训练脆弱——这就是 warmup 在 Post-Norm 时代不可或缺的原因。

**6.4 ★** 一个词：**归一化**（Final Norm）。见第 7 章 7.1 节。

## 第 7 章 · 输出与采样

**7.1** logits $(3, 1, 0.5)$：
- $T=1$：softmax ≈ $(0.836, 0.113, 0.069)$
- $T=0.5$：等效 logits $(6, 2, 1)$ → ≈ $(0.976, 0.018, 0.007)$——更尖
- $T=2$：等效 $(1.5, 0.5, 0.25)$ → ≈ $(0.588, 0.216, 0.168)$——更平

**7.2** 例：$P = (0.50, 0.35, 0.06, 0.05, 0.04)$。top-k(3) 留前 3 个（累积 0.91）；top-p(0.9) 需要累积 ≥ 0.9，前 2 个只有 0.85，也要留 3 个……调整：$P = (0.6, 0.32, 0.04, 0.02, 0.02)$：top-p(0.9) 留 2 个（0.92），top-k(3) 留 3 个——集合不同。min-p 优势场景：高温下 $P = (0.12, 0.10, 0.09, \ldots)$ 几乎均匀，top-p(0.9) 会放进几十个杂鱼；min-p(0.1) 只留 $\geq 0.012$ 的候选，仍能有效砍尾。

**7.3** 要点：惩罚作用在 logits 上是"除/减一个量"。若先除温度再惩罚，同一个 $r$ 在 $T=0.5$ 时作用于放大一倍的 logits、在 $T=2$ 时作用于减半的 logits——惩罚的**相对力度**随温度漂移，参数语义不再稳定。先惩罚后温度则力度恒定。

**7.4 ★** 没有任何变化。因果掩码保证每个 token 的 $k$/$v$ 只由它自己及之前的 token 决定（第 3 章 3.4 节），新增的第 11 个 token 不在任何历史 token 的可见范围里。完整展开见第 8 章 8.2 节。

## 第 8 章 · 自回归与 KV Cache

**8.1** 第 $t$ 步：$Q K^\top$ 是 $(t, d_k) \times (d_k, t)$，代价 $O(t^2 d_k)$，全模型各头各层合计 $O(t^2 d)$ 量级。求和 $\sum_{t=1}^{n} t^2 = \frac{n(n+1)(2n+1)}{6} = O(n^3)$。带缓存后每步 $(1, t)$ 点积为 $O(t d)$，总量 $\sum t = O(n^2 d)$。

**8.2** GQA-8：320 KB/token（附录 A.3）× 32768 = **10.7 GB**——一场长对话就吃掉一张卡的八分之一。MHA：×8 = **85.9 GB**，超过整张 A100。GQA 是长上下文时代的入场券。

**8.3** $(1, t)$ 的行向量配全 $-\infty$ 上三角意味着屏蔽掉除第一列外的所有位置（或产生 NaN，取决于实现）——新 token 只能看到最老的一个 token，输出立即偏离全量重算。动手改验证：assert 在第 2 步就失败。

**8.4 ★** $140\ \text{GB} \div 2\ \text{TB/s} = 70$ ms；上限 $1/0.07 = 14.3$ token/s。核对：第 10 章 10.1 节开场白。

## 第 9 章 · 显存战争

**9.1** $\lceil 100/16 \rceil = 7$ 块；最后一块用了 $100 - 96 = 4$ 个坑位，浪费 12 个。最坏情况：生成 97 个 token 时占 7 块、浪费 15 个坑位——浪费上限恒为 block_size − 1。

**9.2** 前 50 个 token 占块 0~3（块 3 用了 2 个坑位）。分叉前两分支共享块 0~3（引用计数 2）。第 51 个 token 要写进块 3 的空位——触发 CoW：复制块 3（16 个 token 的 K/V 数据）给写入方，两张块表从此在块 3 分道扬镳；块 0~2 继续共享。

**9.3 ★** 两层原因：① 一次查表换来 16 个 token 的整块连续读——寻址开销被 16 倍数据量摊薄；② GPU 以大量并行线程运行，某线程等待寻址时调度器切换到其他就绪线程，访存延迟被计算掩盖。块表本身很小（4096 token 仅 256 项），常驻高速缓存。

**9.4** $8192 \times 2\ \text{B} = 16$ KB/次 × 160 次 ≈ **2.6 MB/步**。看似不大，但 Decode 单步仅 15~70 ms 且同步在关键路径上串行发生——PCIe（~64 GB/s）上 160 次小包同步的延迟叠加不可接受，NVLink（900 GB/s 级 + 低延迟）是 TP 的硬前提。

## 第 10 章 · 吞吐引擎

**10.1** $14\ \text{GB} \div 2\ \text{TB/s} = 7$ ms → **~143 token/s**。手机跑 3B（INT4 ≈ 1.5 GB）：即使 LPDDR 带宽只有几十 GB/s，权重小使单步搬运时间仍在几十 ms 量级——"模型小到带宽账单不疼"，这就是端侧模型的生存逻辑。

**10.2** memory-bound 区间内单步时间由搬权重主导，batch=128 时延迟仅小幅上升（多了激活与 KV 的搬运，通常 <2 倍）；吞吐 ≈ batch ÷ 单步时间，提升接近两个数量级。这条不对称性就是"拼车"的全部利润来源。

**10.3** 长请求自己的 TTFT 变差（prompt 分几步消化完才出首字）；同批其他请求的 TPOT 显著变稳（不再被一个巨型 Prefill 卡住整轮）。这是长请求向全场让利——调度公平性的典型取舍，多数服务场景值得做。

**10.4** 每轮草稿+验证产出约 2.4 + 1 = 3.4 个 token（验证步本身也产出 1 个），而大模型的前向只跑了 1 次多一点——粗略加速比 ≈ 2.4~3.4 倍区间（取决于计费口径）。接受率决定"每次大模型前向摊到几个字"；草稿再快，猜不中就全白干——所以草稿模型与目标模型的**分布对齐度**比草稿速度更关键。

## 附录 D · 思考的解剖（思考题）

**D.1** 思考结束时序列长 500 + 3000 = 3500 token，KV Cache = 3500 × 320 KB ≈ **1.15 GB**（对比：一个普通 4096 上下文请求才 1.34 GB——一次思考就吃掉相当量）。答案首字前的等待 ≈ Prefill（短，数百 ms）+ 3000 个思考 token 的 Decode ≥ 3000 ÷ 14 ≈ **3.6 分钟**（单请求无批处理的下界口径）——推理模型的体感延迟主要是思考长度。

**D.2** 不能扔。因果掩码只保证"历史不依赖未来"，不保证"未来不依赖历史"——恰恰相反，答案段每个新 token 的注意力都要检索全部在前文的 K/V（第 3 章），思考段正是答案的推导依据。扔掉思考段的 K/V，答案段就失去了对草稿的访问——等于让模型"想完就失忆"，输出质量崩回不思考水平甚至更差（分布已被 <think> 模式带偏）。只有当整个请求结束、或后续轮次把"思考摘要"作为新 prompt 重新 Prefill 时，才能安全释放——这正是一些多轮对话产品"不保留历史思考"的实现方式。

---

⬅️ [附录 A · 速查手册](./appendix-a-速查手册.md) ｜ [返回目录](./README.md) ｜ ➡️ [附录 C · 多模态架构一瞥](./appendix-c-多模态.md)
