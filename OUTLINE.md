# V2 写作大纲

> 书名（暂定）：《解剖一次推理：从 API 调用到 Transformer 的每一个矩阵》
> 基准模型：LLaMA-2 70B（d_model=8192，80 层，64 Q 头，8 KV 头，d_ff=28672，V=32000）
> Running example：POST 一次 Chat 请求，messages = [system:"你是一位精通古诗的助手", user:"帮我补全这首诗：白日依山尽，黄河入海"]，经 Chat Template 拼接后模型续写「流」及后文
> 目标读者：1~3 年经验、会矩阵乘法、调用过 LLM API 的软件工程师

---

## 全书叙事主线

一条主线贯穿全部章节：**解剖一次真实的 API 调用**。

```
curl 发出请求 ──► [外壳] HTTP 服务 · Chat Template · 排队 · 调度  ← 第 0 章预览，Part IV 放大
                    │
                    ▼
              [内核] 字符串 → token → 向量 → 80 层 → 分布 → 字   ← Part I~III 逐站放大
                    │
                    ▼
             反分词 · SSE 流式吐字 ──► 屏幕上出现「流」
```

- **第 0 章**用 20 分钟把整条链路走马观花一遍，给读者一张全局地图；
- **Part I~III** 沿数据流逐站放大内核（算法）；
- **Part IV** 回到外壳（工程），回答"这一切在真实 GPU 集群上如何跑得又快又省"；
- 每章开头标注"我们在解剖图上的位置"，每章结尾交代"下一站为什么必须存在"。

伏笔主干线（继承 V1 骨架并强化）：
因果掩码（ch3）→ 历史 K/V 不变（ch8 回收）→ KV Cache 吃显存（ch9 回收）→ 省显存为了大 batch（ch10 回收）。
辅助伏笔：嵌入是上下文无关的（ch1）→ 注意力语境化（ch3 回收）；行序可打乱的悬念（ch1）→ 位置编码（ch2 回收）；上下文窗口的三层天花板（ch2 §2.7 埋"约 65 GB"剧透）→ ch9 §9.1 精确对账；FFN 置零会怎样（ch5 ★题）→ 残差优雅降级（ch6 回收）；只取末行（ch7）→ Decode 每步只算一个 token（ch8 回收）。

---

## 分部与章节

### 序章（1 章）

| 章 | 文件 | 核心论点（一句话） | 预估字数 |
|----|------|-------------------|---------|
| 第 0 章 · 鸟瞰：一次调用的完整解剖图 | `part0-chapter00-鸟瞰.md` | 一次 API 调用 = 工程外壳包着算法内核；先看清整条流水线的 8 个站点，再逐站放大 | 4000~5000 |

### Part I · 请求进门：从字符串到矩阵（2 章）

**Part 导言主题**：模型只认浮点数矩阵；本部分回答"字符串如何获得数字身份、语义坐标与顺序感"。

| 章 | 文件 | 核心论点 | 预估字数 |
|----|------|---------|---------|
| 第 1 章 · 分词与嵌入：语言的数字化两级跳 | `part1-chapter01-分词与嵌入.md` | 分词解决"切多细"（BPE 演示合并全过程），嵌入解决"编号如何携带语义"（可查表的分布式坐标）；此刻的向量上下文无关且行序可互换——留给 ch2/ch3 的双重悬念 | 5000~6500 |
| 第 2 章 · 位置编码：旋转出来的顺序感 | `part1-chapter02-位置编码.md` | 没有位置信息的模型是词袋；RoPE 把绝对位置转成 Q/K 旋转角，点积中自动涌现相对距离——章首用三句话+二维数值例建立"注意力用 Q·K 点积衡量相关"的最小前提（完整推导标注见 ch3，消除 V1 含糊话术）；§2.7 回答"上下文窗口是模型还是推理服务的限制"——三层天花板（量程/训练分布/服务开放成本），工程账本留 ch9 回收；含代码锚点 | 5000~6500 |

### Part II · 核心腹地：一层之内发生了什么（4 章）

**Part 导言主题**：80 层流水线中的一层 = 开会（注意力）+ 查资料（FFN）+ 两根安全支柱（残差/归一化）。

| 章 | 文件 | 核心论点 | 预估字数 |
|----|------|---------|---------|
| 第 3 章 · 注意力：让向量互相塑造 | `part2-chapter03-注意力.md` | 注意力 = 可学习的加权信息汇聚；公式四部件各有存在理由；因果掩码用零参数同时买到训练并行与训推等价（埋最长伏笔）；回收 ch2 的点积前提与 ch1 的语境化悬念；含 NumPy 代码锚点 | 6000~7500 |
| 第 4 章 · 多头与 GQA：64 个问题，8 套答案 | `part2-chapter04-多头与GQA.md` | 多样性来自 Q 而非 K/V——这一洞察让 KV 显存砍到 1/8 而质量几乎无损；先算 MHA 的显存账单再引出 GQA；含代码锚点 | 5000~6500 |
| 第 5 章 · FFN：装进参数里的世界知识 | `part2-chapter05-FFN.md` | 注意力搬运在场信息，FFN 注入不在场知识；SwiGLU = 内容 × 门控；键值存储视角配最小数值例（修复 V1 只有结论）；含代码锚点 | 5000~6500 |
| 第 6 章 · 残差与归一化：深度的两根支柱 | `part2-chapter06-残差与归一化.md` | 80 层不崩塌靠拓扑而非调参：残差把"替换"改成"增量"，Pre-Norm 让主干道永不被截断；组装出完整 Transformer Block 全貌图 | 4500~6000 |

### Part III · 走出模型：从分布到文字流（2 章）

**Part 导言主题**：模型的产出是概率分布而非文字；本部分回答"分布如何变成字、字如何变成流"。

| 章 | 文件 | 核心论点 | 预估字数 |
|----|------|---------|---------|
| 第 7 章 · 输出与采样：分布变成决定 | `part3-chapter07-输出与采样.md` | LM Head 是与全词表同时对表；temperature/top-p/top-k/repetition penalty 是对同一个分布做的四种手术（覆盖真实 API 全部主流参数，修复 V1 遗漏）；含代码锚点 | 5000~6500 |
| 第 8 章 · 自回归与 KV Cache：循环的经济学 | `part3-chapter08-自回归与KVCache.md` | 因果掩码保证历史不可改写 → 缓存合法 → O(n³)降为 O(n²)；Prefill/Decode 分野解释"首字慢、后字匀速"；含代码锚点（带 Cache 的生成循环） | 5000~6500 |

### Part IV · 工程战场：让 GPU 物有所值（2 章）

**Part 导言主题**：回到解剖图的外壳。算法正确只是及格线，本部分回答"同样的模型如何服务 100 倍的用户"。

| 章 | 文件 | 核心论点 | 预估字数 |
|----|------|---------|---------|
| 第 9 章 · 显存战争：分页拯救 KV Cache | `part4-chapter09-显存战争.md` | 显存危机的病根是"连续存放"假设；PagedAttention 复刻 OS 虚拟内存（块池/块表/CoW）；开头补"把 70B 塞进多张卡"——张量并行最小必要知识（修复 V1 遗漏） | 5000~6500 |
| 第 10 章 · 吞吐引擎：带宽、批处理与调度 | `part4-chapter10-吞吐引擎.md` | Decode 的瓶颈是带宽不是算力（AI≈1 vs 平衡点 156）；三味药：Continuous Batching、少搬数据（FlashAttention/融合/量化）、控制/数据平面分离；投机解码入正文收尾（修复 V1 遗漏）；全书主线收官 | 5500~7000 |

### 附录（3 篇）

| 篇 | 文件 | 内容 | 预估字数 |
|----|------|------|---------|
| 附录 A · 速查手册 | `appendix-a-速查手册.md` | 维度全程速查表、参数量账本、显存/延迟账本、术语表、符号约定、延伸阅读总表 | 3000~4000 |
| 附录 B · 练习提示与答案 | `appendix-b-练习答案.md` | 全书练习的提示与答案：验证型给完整解，开放型给判断要点（V2 新增，修复 V1 🔴 问题） | 4000~6000 |
| 附录 C · 多模态架构一瞥 | `appendix-c-多模态.md` | ViT 的 patch embedding（图像如何变 token）；多模态 LLM 接入范式（视觉编码器+投影层→同一条推理流水线）；检查点 A 用户要求新增 | 3000~4500 |
| 附录 D · 思考的解剖 | `appendix-d-思考的解剖.md` | 推理模型的"思考"= 站点 ⑥ 的刻意延长：用序列长度换串行深度（test-time compute），KV Cache 充当工作记忆；CoT→o1/R1 两代来源；工程账单与思考预算旋钮；第 11 轮用户需求新增 | 3500~5000 |

**合计**：正文 11 章 + 附录 4 篇 ≈ 6.6~8.7 万字。

---

## 每章外部引用与知识缺口对照（源自 REFERENCES.md）

| 章 | 计划引用的论文 | 工程对标 | 需补的内部素材缺口 |
|----|----------------|----------|-------------------|
| ch0 | Vaswani 2017；Touvron 2023（参数表） | vLLM V1 引擎分层（API server → EngineCore） | — |
| ch1 | Sennrich 2016（BPE）；Kudo 2018（SentencePiece）；Mikolov 2013（词向量几何） | tiktoken/SentencePiece 演示页 | **BPE 合并过程手推演示**（内部只有思想无过程） |
| ch2 | Su 2021（RoPE）；Vaswani 2017（正弦编码）；Peng 2023（YaRN）；Liu 2024（Lost in the Middle） | HF rotate_half 实现 | 长上下文扩展机制（内部仅一句带过）+ 上下文窗口三层天花板（§2.7） |
| ch3 | Vaswani 2017；Elhage 2021（circuits）；Bahdanau 2015（注意力起源） | PyTorch SDPA（is_causal 语义）；HF modeling_llama | — |
| ch4 | Shazeer 2019（MQA）；Ainslie 2023（GQA 消融）；DeepSeek-V2 2024（MLA） | HF repeat_kv 实现 | **MLA 短节**（内部停在 GQA） |
| ch5 | Shazeer 2020（SwiGLU）；Geva 2021（键值存储）；Meng 2022（ROME）；Mixtral 2024 | HF LlamaMLP | **MoE 短节**（FFN 的下一步）；键值视角最小数值例 |
| ch6 | He 2016（ResNet）；Zhang 2019（RMSNorm）；Xiong 2020（Pre-Norm） | HF LlamaRMSNorm、前向顺序 | — |
| ch7 | Holtzman 2020（top-p）；Fan 2018（top-k）；Press 2017（weight tying） | 主流 API 参数表（OpenAI/vLLM SamplingParams） | **采样参数全景**（repetition/presence/frequency penalty、min-p） |
| ch8 | Pope 2023（两阶段分析）；Chen 2022（inference arithmetic） | vLLM prefill/decode 调度 | — |
| ch9 | Kwon 2023（PagedAttention）；Shoeybi 2019（Megatron TP）；Zheng 2024（RadixAttention） | vLLM 块表/块池/CoW；llama.cpp 量化分级 | **张量并行节**（内部仅隐含 TP=4） |
| ch10 | Williams 2009（Roofline）；Dao 2022（FlashAttention）；Yu 2022（Orca）；Leviathan 2023（投机解码） | vLLM V1 调度循环（2025 重构后事实） | **投机解码节**（含 DeepSeek MTP 一笔）；vLLM V1 演进 |
| 附录 A | 全书论文总表 | — | Mamba/RWKV 作为"O(n²) 的出路"指针 |
| 附录 B | — | — | —（V2 新增） |
| 附录 C | Dosovitskiy 2021（ViT）；Liu 2023（LLaVA） | HF ViT/LLaVA 实现 | 多模态接入范式（检查点 A 新增需求，内部素材完全未覆盖） |
| 附录 D | Wei 2022（CoT）；Kojima 2022；DeepSeek-R1 2025；Snell 2024（test-time compute）；OpenAI o1 System Card 2024 | 各家 API 的 reasoning effort/thinking budget 参数；<think> 标记解析 | 模型思考能力（第 11 轮用户需求，内部素材完全未覆盖） |

## 竞品差异化在大纲中的落点

源自 REFERENCES.md 竞品分析的生态位结论：**"以真实生产尺寸模型为标本、从算法一路讲到 GPU 带宽的推理解剖书"**，具体兑现：

1. **vs Raschka（从零训练玩具模型）**：全书用 70B 真实尺寸而非 124M 玩具尺寸；Part III/IV（KV Cache、显存、调度、带宽）是其完全不覆盖的后半场；
2. **vs Alammar（应用导向）**：Part II 深入每个矩阵形状与设计取舍（I2 标准），回答其跳过的"为什么"；
3. **vs HF 团队书（库 API 驱动）**：不绑定框架版本，只讲多年不变原理 + 纯 NumPy 代码锚点；
4. **vs 教学资源（3B1B/Karpathy 等）**：它们止步于单次前向，V2 用同等教学质量补齐生成循环之后的工程半场（Part III/IV）。

---

## 每章固定结构（全书统一模板）

1. 章首：解剖图位置标记 + 「本章回答的问题」（2~3 个）+ 前置依赖声明
2. 正文：动机 → 最小例子 → 一般形式 → running example 推进 → 工程视角
3. 代码锚点（核心机制章）：≤30 行纯 NumPy，变量名与公式符号对齐
4. 章末：一句话小结表 + 练习（2~4 题，含 1 道 ★ 伏笔题）+ 延伸阅读（原始论文）
5. 结尾一段：交代下一章为什么必须存在（衔接句）

---

## 与 V1 结构的关键差异（每处标注理由）

| # | 差异 | 理由（对应 V1-DIAGNOSIS 问题编号） |
|---|------|----------------------------------|
| 1 | 新增第 0 章全景鸟瞰 | #3：读者需要先有地图再进隧道 |
| 2 | 主线从"一个字的旅程"升级为"一次 API 调用的解剖" | 让 Part IV 从"附赠工程手册"变成主线的必然段落 |
| 3 | 位置编码保持 V1 顺序（ch2，先于注意力） | 检查点 A 用户决策；改用"三句话+二维数值例"前置最小点积前提，消除 V1 的含糊话术（诊断 #7 的新解法） |
| 4 | V2 沿用 V1 的 ch1 分词嵌入 / ch2 位置的分章 | ch1 结尾以"打乱行序"悬念埋位置问题，ch2 回收 |
| 5 | 每个核心机制章新增 NumPy 代码锚点 | #1（🔴）：工程师读者需要"写得出" |
| 6 | 新增附录 B 练习答案 | #2（🔴）：练习必须可自我验证 |
| 7 | 每 Part 有导言段落 | #4：Part 级递进逻辑显式化 |
| 8 | ch9 增张量并行节、ch7 增完整采样参数、ch10 增投机解码 | #6：补齐工程链路遗漏 |
| 9 | 单章字数目标从 V1 的 ~3000 提到 4000~8000 | #8：出版级信息密度 |
| 10 | Running example 换为 Chat API 格式（messages 数组），“白日依山尽，黄河入海”→「流」 | 匹配 2024+ 读者真实体验；内核机制不受影响 |

---

## 写作顺序计划（供后续轮次执行）

| 轮次 | 目标 |
|------|------|
| 第 2 轮 | 规范升级（REFERENCES.md + 标准 X1/X2）+ ch0（确立文风基准） |
| 第 3 轮 | ch1 + 反馈落地（分词嵌入；同步结构决策到全部文件） |
| 第 4 轮 | ch2 + ch3（位置编码与注意力） |
| 第 5 轮 | ch4 + ch5（GQA 与 FFN） |
| 第 6 轮 | ch6 + ch7（残差归一化与输出采样） |
| 第 7 轮 | ch8 + ch9（KV Cache 与显存战争） |
| 第 8 轮 | ch10 + 附录 A |
| 第 9 轮 | 附录 B + 附录 C |
| 第 10 轮起 | 全书一致性通读修订；全维度审计与定向修正，直至 ALL PASS |

每轮实际以审计结果为准，允许在"新章写作"与"质量修正"之间调整优先级。
