# 附录 C · 多模态架构一瞥

> 本书主线只解剖文本推理。这篇附录回答一个自然的追问：**图片是怎么进入同一条流水线的？** 你会发现全书学到的每一个部件——token、嵌入、注意力、KV Cache——在多模态世界里原样复用；变化只发生在"进门"那一步。
>
> **前置依赖**：第 1 章（token 与嵌入）、第 3 章（注意力）、第 8 章（KV Cache）。

---

## C.1 核心问题:图像没有"词表"

回顾第 1 章的两级跳：字符串 → token 编号 → 嵌入向量。文本能这么走，是因为文本天然离散——总能切成词表里的片段。图像是连续的像素海洋，没有现成的"图像词表"可查。

多模态架构的破题思路出人意料地朴素：**跳过"编号"这一级，直接把图像切块变成向量**。既然 Transformer 的核心腹地（第 3~6 章）只认 $(n, d)$ 的浮点矩阵、根本不在乎这些向量当初是查表来的还是算出来的——那就绕过词表，直接造向量。

## C.2 ViT:把图像切成"视觉词"

**Vision Transformer**（ViT, Dosovitskiy et al., 2021）的做法三步走：

1. **切块（patch）**：一张 $224 \times 224$ 的图片，按 $16 \times 16$ 像素切成不重叠的小方块——得到 $14 \times 14 = 196$ 个 patch。
2. **压平 + 线性投影**：每个 patch 有 $16 \times 16 \times 3 = 768$ 个像素值，压成一条向量，乘一个可学习的投影矩阵映射到模型维度 $d$。**这一步在功能上完全对应文本的嵌入查表**——只是"查表"换成了"线性变换"。
3. **加位置信息**：196 个 patch 的空间排布（第几行第几列）用位置编码注入——与第 2 章解决的是同一个问题：注意力是集合运算，顺序/空间关系必须显式补回。

```
224×224×3 图片
   │ 切成 16×16 的块
   ▼
196 个 patch（每个 768 维像素值）
   │ 线性投影到 d 维 + 位置编码
   ▼
(196, d) 矩阵  ←—— 与文本的 (n, d) 完全同构！
   │
   ▼
标准 Transformer 层堆叠（与第 3~6 章的部件完全相同）
```

从第 3 步往后，一切都是你已经学过的东西：多头注意力让 patch 互相看见（天空 patch 从周围 patch 确认"这是晴天不是海面"），FFN 注入视觉知识，残差与归一化护送信号穿过深度。**ViT 的历史意义正在于此：它证明了 Transformer 不是"语言架构"，而是"序列架构"——任何能变成 $(n, d)$ 矩阵的东西都能进这条流水线。**

一个术语差异提醒：ViT 用于图像分类时是**编码器**（无因果掩码，全 patch 互见——图像没有"未来"概念），与本书的解码器（下三角掩码）在掩码策略上不同；但部件本身一模一样。

## C.3 多模态 LLM:两条流水线的汇合

看懂图片还不够，用户想要的是"看图说话"。以 LLaVA（Liu et al., 2023）为代表的主流范式，把 ViT 和语言模型直接焊接：

```
图片 ──► 视觉编码器（预训练 ViT，如 CLIP 的视觉塔）──► (196, d_vit) 视觉特征
                                                          │
                                                    投影层（一两层 MLP）
                                                    d_vit → d_model 对齐维度
                                                          ▼
用户文字 ──► 分词 + 嵌入（第 1 章）──► 文本 token 向量     视觉 token 向量
                                          │                 │
                                          └────── 拼接 ──────┘
                                                   ▼
                                  [视觉 token × 196] [文本 token × n]
                                                   ▼
                                     同一个语言模型（80 层，第 3~6 章）
                                                   ▼
                                     自回归生成回答（第 7~8 章）
```

三个部件各司其职：

- **视觉编码器**：现成的预训练 ViT，负责"看懂"——产出携带语义的视觉特征；
- **投影层**：一两层 MLP，负责"翻译"——把视觉特征空间对齐到语言模型的嵌入空间，让视觉 token 在语言模型眼里"看起来像普通的词向量"（训练时主要就是在教这个翻译官）；
- **语言模型**：不动或微调，把视觉 token 当作一段"看不见的前缀文字"来阅读。

对语言模型而言，一张图 = 196 个"外语词"，投影层是随行翻译。它用与处理「白日依山尽」完全相同的注意力去关注这些视觉 token——用户问"图里的山是什么颜色"，注意力就把问题里的「山」与对应的 patch 向量连起来。

## C.4 全书知识在多模态下的复用清单

| 本书章节 | 在多模态里的对应物 |
|----------|-------------------|
| 第 1 章 嵌入 | patch 线性投影——"造向量"替代"查向量" |
| 第 2 章 位置编码 | patch 的二维空间位置编码（多模态 LLM 中视觉 token 同样参与 RoPE） |
| 第 3~6 章 核心腹地 | 原封不动复用——这正是 Transformer 的通用性红利 |
| 第 7 章 输出采样 | 不变：回答依然是文本 token 的分布 |
| 第 8 章 KV Cache | 视觉 token 同样进缓存——196 个 token 的 K/V 在多轮对话里全程占着显存 |
| 第 9 章 显存战争 | 图片越多、分辨率越高，KV 账单越重——多模态是显存战争的新前线 |

最后一行值得展开一句：一张高分辨率图片动辄消耗几百上千个视觉 token（现代方案会切多个子图），它们的 KV Cache 与文本一视同仁地吃显存——第 9 章的每一条战线（分页、量化、前缀复用）在多模态服务里原样开打，且更激烈。2024 年后的一个活跃方向就是视觉 token 压缩（用更少的 token 表示一张图），本质上与 GQA/MLA 是同一个故事：**在质量与显存之间寻找更好的帕累托点**。

## 延伸阅读

- Dosovitskiy et al., *An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale* (ICLR 2021)——ViT 原论文，标题本身就是本附录的摘要
- Radford et al., *Learning Transferable Visual Models From Natural Language Supervision* (CLIP, 2021)——主流视觉编码器的来源
- Liu et al., *Visual Instruction Tuning* (LLaVA, NeurIPS 2023)——"视觉编码器 + 投影层 + LLM"范式的标杆实现

---

⬅️ [附录 B · 练习提示与答案](./appendix-b-练习答案.md) ｜ [返回目录](./README.md) ｜ ➡️ [附录 D · 思考的解剖](./appendix-d-思考的解剖.md)
