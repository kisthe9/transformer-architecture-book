# 第 3 章 · 注意力:让向量互相塑造

> **Part II · 核心腹地** 从这里开始。这一部分（第 3~6 章）拆解 80 层车间中的一层：先开会交换信息（注意力，第 3~4 章），再各自查资料（FFN，第 5 章），全程有两件保命装置护航（残差与归一化，第 6 章）。
>
> **我们在解剖图上的位置**：站点 ④，80 层车间的第一道工序。
>
> **本章回答的问题**：「海」怎么知道自己是"黄河入海"的海，而不是"海量数据"的海？公式 $\text{softmax}(QK^\top / \sqrt{d_k})\,V$ 里的每个部件为什么长成那样？一个不含任何参数的下三角矩阵，凭什么同时买到"训练可并行"和"训练推理等价"两件大事？
>
> **前置依赖**：第 1 章·分词与嵌入（向量是上下文无关的"平均词义"）、第 2 章·位置编码（$q \cdot k$ 点积衡量相关，且已带位置信息）。

---

## 3.1 问题：十个互不相识的向量

盘点一下手上的资产。$X: (10, 8192)$，每行是一个字的语义坐标；位置机制已就位。但第 1 章诊断过这些向量的病症：**每个向量都是那个字在所有语境下的平均含义**。「海」的向量里咸水、巨量、姓氏各占一份，它对自己身处「黄河入海」这个语境一无所知。

预测下一个字需要的却是语境化之后的表示——"江河的归宿"这一义项必须胜出，才能推出「流」。而语境信息只存在一个地方：**同一句话里的其他字**。「河」就在两格之外，「入」紧贴在旁边。

所以需要一种机制，让每个字从其他字那里收集信息。要求有三条：

1. 收集要**有选择**——「海」该多听「河」「入」的，少听「，」的；
2. 选择标准要**可学习**——什么算相关，不能靠人写规则，得从数据里学；
3. 计算要**可并行**——十个字同时收集，不能像 RNN 一样排队。

同时满足三条的机制，就是**注意力**（attention）。它的全部工作可以压缩成一句话：

> 每个字按"可学习的相关程度"对所有字的信息加权求和，用结果更新自己。

这句话里每个成分怎么实现，是接下来三节的内容。

## 3.2 三重身份：Q、K、V

### 开会的三个角色

注意力给每个字同时分配了三重身份。用第 0 章"开会"的比喻展开：一场十人会议上，每个人都带着三样东西——

| 身份 | 会议类比 | 回答的问题 | 符号 |
|------|---------|-----------|------|
| Query | 你心里的**疑问** | 我想从别人那里了解什么？ | $q$ |
| Key | 你桌牌上的**简介** | 别人凭什么判断该不该问我？ | $k$ |
| Value | 你实际的**发言** | 一旦被问到，我给出什么内容？ | $v$ |

会议规则：每个人拿自己的疑问逐一对照所有人的桌牌简介，匹配度决定听谁的发言听多少，最后把听到的内容按比例汇总，更新自己的认知。

> **类比的边界**：两处失真需要声明。其一，会议里"简介"和"发言"是两样独立准备的东西，而模型中 $k$ 和 $v$ 是从**同一个**输入向量投影出的两个视图，同源不同用；其二，真实会议中发言人知道谁在听，而注意力中 $v$ 的提供方对"谁取走了多少"完全无感——信息流是单向拉取，不是双向交流。

### 为什么必须三个，而不是一个

一个自然的疑问：都是从同一个字的向量派生的，为什么不直接拿原向量互相算相似度、再互相加权？因为"如何提问"、"如何被找到"、"提供什么"是三件**本质不同**的事。「海」想找的是修饰自己的水系词（提问面向），它能被匹配的特征是"地理终点/大容量"（被查面向），它能贡献的内容是完整的语义信息（供给面向）——三个面向没有理由共享同一个向量表达。硬合并成一个，就等于强迫"我找什么"和"我是什么"永远一致，表达力被腰斩。

三重身份由三个**可学习**的投影矩阵生成：

$$
Q = X W_Q, \qquad K = X W_K, \qquad V = X W_V
$$

$W_Q, W_K, W_V$ 是训练出来的参数。这是整个机制里最容易被轻描淡写、却最值得停留的一点：**"什么算相关"本身是学出来的**。没有人告诉模型"发光体与光相关""动词与宾语相关"——这些判断标准全部凝结在三个投影矩阵的数值里，由"预测下一个字"的压力雕刻而成。这也是注意力与传统检索系统（关键词匹配、TF-IDF）的分水岭：后者的相关性定义是人定的，前者是学的。

> **形状备注**：本章以单头视角讲原理，$Q, K, V$ 均为 $(n, d_k) = (10, 128)$；真实模型里 64 个头并行工作、各学一套"相关"的定义，那是第 4 章的主题。RoPE 的旋转（第 2 章）就发生在这三个投影之后：$q$、$k$ 旋转，$v$ 不动。

## 3.3 四步组装那条著名的公式

### 第一步：点积，一次算完所有配对

第 2 章已经建立：相关分数 = $q \cdot k$。十个字两两配对共 100 个分数，全部塞进一次矩阵乘法：

$$
S = Q K^\top, \qquad S: (10, 10)
$$

$S[i][j]$ 读作：第 $i$ 个字的疑问，与第 $j$ 个字的桌牌，匹配度多少。训练好的模型里，「海」那一行会呈现这样的形态（数值为示意）：

```
S 的第 9 行——「海」的疑问 vs 所有字的桌牌:

        白    日    依    山    尽    ，    黄    河    入    海
海 →   0.9   1.1   0.4   1.8   0.7   0.1   4.2   7.6   6.3   2.5
                                            ↑     ↑     ↑
                          「黄」「河」「入」的 k 与「海」的 q 高度匹配
                          （训练学会了：水系动线上的词彼此强相关）
```

### 第二步：除以 √d_k，把分数拉回可用区间

直接用 $S$ 有一个隐蔽的数值炸弹。两个 128 维随机向量的点积是 128 个乘积项之和——**方差随维度线性累加**。若 $q$、$k$ 的各维独立、均值 0、方差 1，则：

$$
\mathrm{Var}(q \cdot k) = \sum_{j=1}^{d_k} \mathrm{Var}(q_j k_j) = d_k = 128
$$

标准差 $\sqrt{128} \approx 11.3$，意味着分数动辄 ±20、±30。这样的分数进入下一步的 softmax 会发生什么？看一个数值实验就明白：

```
softmax([ 2.0,  1.0,  0.5])  =  [0.55, 0.20, 0.12, …]   分布温和，各项都有梯度
softmax([20.0, 10.0,  5.0])  =  [0.9999, 0.000045, …]   最大项独占一切
```

第二种情形叫**饱和**：胜者通吃，其余项的概率被压到接近机器精度，梯度趋近于零——训练信号传不回去，模型学不动。解药是把分数除以标准差，方差归一：

$$
S = \frac{Q K^\top}{\sqrt{d_k}}
$$

分母为什么是平方根而不是 $d_k$ 本身？因为要除的是**标准差**（$\sqrt{\mathrm{Var}}$），不是方差。这个不起眼的 $\sqrt{d_k}$ 是"高维点积 + softmax"这对组合能够训练的前提条件，原始论文特意用一条脚注解释它——值得你在读任何注意力变体时都先确认它还在不在。

### 第三步：softmax，把分数变成预算

缩放后的分数仍然有正有负、总和随意，而"加权求和"需要一组合法的权重——非负、和为 1。逐行做 softmax：

$$
A[i][j] = \frac{\exp(S[i][j])}{\sum_{j'} \exp(S[i][j'])}, \qquad A: (10, 10)
$$

每一行变成一个概率分布，可以理解为这个字手里的**注意力预算**：总额 100%，按指数化后的分数比例分配。指数函数在这里不只是归一化工具，它还自带一个重要特性——**拉大差距**。分数 7.6 与 4.2 相差不到 2 倍，指数化后权重相差 $e^{3.4} \approx 30$ 倍。注意力因此是"锐利"的：它敢于把大部分预算押给最相关的一两个字，而不是雨露均沾。

```
「海」那一行经过缩放与 softmax（示意）:

        白    日    依    山    尽    ，    黄    河    入    海
海 →  0.01  0.01  0.01  0.02  0.01  0.00  0.13  0.48  0.27  0.06
                                            ↑     ↑     ↑
                              88% 的预算投给了「黄河入」三个字
```

### 第四步：按预算取货

最后用预算对所有字的 $v$ 加权求和：

$$
O[i] = \sum_j A[i][j]\, V[j], \qquad \text{矩阵形式 } O = A V, \quad O: (10, 128)
$$

「海」的新向量 ≈ 0.48 × 河的发言 + 0.27 × 入的发言 + 0.13 × 黄的发言 + 0.06 × 自己 + 零头。它不再是词表里那个三分咸水三分巨量的平均「海」，而是**被这句诗塑造过的海**——黄河奔流的终点。第 1 章的第一个悬念（"没有语境"）至此解决。

四步合起来，就是那条你在所有论文里见过的公式：

$$
\text{Attention}(Q, K, V) = \text{softmax}\!\left(\frac{Q K^\top}{\sqrt{d_k}}\right) V
$$

现在你可以逐符号说出它的存在理由：$QK^\top$——可学习的两两相关；$\sqrt{d_k}$——压回不饱和区间；softmax——变预算并锐化；$\times V$——按预算收货。没有一个符号是装饰。

## 3.4 因果掩码:不许偷看后文

### 一条铁律

到目前为止的会议是"全员互看"的：位置 2 的「依」也能听位置 9 的「海」发言。对理解类任务（文本分类、检索）这没问题，但我们的模型以"预测下一个字"为生。想一想训练时发生什么：模型在位置 8（「入」）的任务是预测位置 9 的「海」。如果计算「入」的表示时允许它看到位置 9——**它可以直接抄答案**。训练损失会漂亮得虚假，而推理时后文根本不存在，无处可抄，模型当场原形毕露。

规则必须是：**位置 $i$ 只允许看位置 $\leq i$**。这叫**因果掩码**（causal mask）——"因果"取其"未来不能影响过去"之意。

### 实现：一个零参数的下三角

实现出奇地便宜。构造常量矩阵 $M$：

$$
M[i][j] = \begin{cases} 0 & j \leq i \quad (\text{可见}) \\ -\infty & j > i \quad (\text{未来，屏蔽}) \end{cases}
$$

在 softmax **之前**加到分数上：

$$
A = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}} + M\right)
$$

为什么用"加 $-\infty$"而不是"事后把权重清零"？因为 $e^{-\infty} = 0$：被屏蔽的位置经过 softmax 权重**严格为零**，且分母只对幸存位置求和——剩余权重自动重新归一到总和为 1。清零方案则会破坏"每行和为 1"的预算性质，还得手动补一次归一化（本章练习 3 请你算出两种方案的具体差异）。借用指数函数的极限行为，一步到位。

```
掩码后的注意力预算矩阵（▓ = 恒为 0）:

        白   日   依   山   尽   ，   黄   河   入   海
白     ●   ▓   ▓   ▓   ▓   ▓   ▓   ▓   ▓   ▓      只看自己
日     ●   ●   ▓   ▓   ▓   ▓   ▓   ▓   ▓   ▓
依     ●   ●   ●   ▓   ▓   ▓   ▓   ▓   ▓   ▓
  ⋮                    （下三角逐行展开）
入     ●   ●   ●   ●   ●   ●   ●   ●   ▓   ▓      看前 9 个
海     ●   ●   ●   ●   ●   ●   ●   ●   ●   ●      唯一看到全句的位置
```

### 一笔零参数的双重交易

这个不含任何可学习参数的常量矩阵，同时完成了两件在别的架构里要花大代价才能办到的事。

**买到训练并行。** 训练时整句话一次性输入，掩码让位置 0 只看自己、位置 1 看前两个……位置 9 看全部。于是**一次前向传播 = 同时布置 10 道独立的"预测下一个字"练习**：位置 0 用「白」预测「日」，位置 1 用「白日」预测「依」……10 个位置的损失并行计算、一起回传。RNN 必须逐位递推才能保证"不看未来"，Transformer 用一个矩阵就把顺序约束变成了并行掩码——这是它训练效率碾压前辈的结构性根源。

**买到训练推理等价。** 推理时逐字生成，生成第 $i+1$ 个字的那一刻，物理上只存在前 $i$ 个字；训练时位置 $i$ 因为掩码也恰好只看得到前 $i$ 个字。**每道练习题的作答条件，与真实考场完全一致**——训练学到的策略推理时一字不差地适用，不需要任何"训练模式/推理模式"的切换适配。这个性质看似理所当然，实则是架构设计里最难得的免费午餐之一。

**顺手埋下全书最重要的一颗种子**：掩码保证位置 $i$ 的表示只依赖位置 $\leq i$——这意味着**一个字的 $k$、$v$ 一旦算出，无论后面再生成多少新字，都永远不会改变**。历史是只写一次的。这个此刻看起来平平无奇的推论，是第 8 章 KV Cache 得以成立的全部数学依据；顺着它，还牵出第 9 章的显存战争和第 10 章的吞吐工程。记住这颗种子埋在哪里。

## 3.5 代码锚点:三十行单头因果注意力

本章全部内容合成一段可运行代码。变量名对齐正文符号，形状全部标注：

```python
import numpy as np

def softmax(s):
    e = np.exp(s - s.max(axis=-1, keepdims=True))   # 减最大值防上溢，工程惯例
    return e / e.sum(axis=-1, keepdims=True)

n, d, d_k = 10, 8192, 128
rng = np.random.default_rng(0)
X = rng.normal(size=(n, d)).astype(np.float32)      # 第 1 章的输出（示意用随机值）
W_Q, W_K, W_V = ((rng.normal(size=(d, d_k)) / np.sqrt(d)).astype(np.float32)
                 for _ in range(3))                 # 三个可学习投影（此处随机代替）

Q, K, V = X @ W_Q, X @ W_K, X @ W_V                 # 各 (10, 128)；真实模型此处再做 RoPE
S = Q @ K.T / np.sqrt(d_k)                          # (10, 10) 缩放后的相关分数
M = np.triu(np.full((n, n), -np.inf), k=1)          # 上三角 -inf：因果掩码
A = softmax(S + M)                                  # (10, 10) 每行是注意力预算
O = A @ V                                           # (10, 128) 语境化后的输出

print(A[0].round(3))    # 位置 0 整行只有 A[0][0]=1.0 —— 只能看自己
print(A[9].round(3))    # 位置 9 十个非零权重 —— 唯一看到全句的位置
print(O.shape)          # (10, 128)
```

两行 `print(A[...])` 值得亲手跑一遍：掩码的效果不再是示意图，而是你屏幕上的数字。

## 3.6 我们的诗此刻的状态,与一个新的不满足

走过一层注意力，十个向量互通了有无：「日」吸收了「白」的修饰，「山」染上了「依」「尽」的动势，而位置 9 的「海」——唯一有权看到全句的字——正在把「黄河」「入」「白日依山尽」的全部意象汇入自己的 8192 维坐标。预测「流」所需的语境弹药，正在这个位置集结（为什么偏偏是最后一个位置扛起预测大旗，第 7 章给出正式答案）。

但立刻能发现一个不满足：单头注意力**一次只能执行一种预算分配**。「海」把 88% 的预算投给了水系语义，就再没有余力同时用另一套标准去关注句法结构（我是这个五言句的第几个字？）或韵律位置（我押不押韵？）。一套 $W_Q, W_K, W_V$ 只能定义一种"相关"，而语言里值得问的问题远不止一种。

解决方案听起来朴素：多配几套投影，并行各问各的。但真实模型给出的数字组合很奇特——**64 套问题，只配 8 套答案**。为什么这样反而更好？下一章解开这个省下 87.5% 缓存显存的设计。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 注意力的本质 | 每个字按可学习的相关度对全句信息加权求和，完成语境化 |
| Q/K/V 三重身份 | 疑问 / 桌牌 / 发言——三个面向必须分开参数化，"什么算相关"是学出来的 |
| 四步公式 | 点积算相关 → $\sqrt{d_k}$ 防饱和 → softmax 变预算并锐化 → 乘 $V$ 收货 |
| 缩放的数学 | $\mathrm{Var}(q \cdot k) = d_k$；除以标准差 $\sqrt{d_k}$ 把 softmax 拉出饱和区 |
| 因果掩码 | 下三角 $-\infty$ 常量矩阵，借 $e^{-\infty} = 0$ 融入 softmax，自动重归一 |
| 零参数双重交易 | 一次前向 = $n$ 道并行练习题；训练条件 = 推理条件 |
| 全书最重要的种子 | 掩码 ⟹ 历史字的 $k$/$v$ 永不改变 ⟹ 第 8 章 KV Cache 的全部依据 |

## 练习

1. 手算一个 $n = 3$、$d_k = 2$ 的完整注意力：自设 $Q$、$K$、$V$ 的数值，走完点积 → 缩放 → 掩码 → softmax → 加权求和五步。（完整参考解在附录 B）
2. 证明 3.3 节的方差命题：若 $q_j, k_j$ 相互独立、均值 0、方差 1，则 $\mathrm{Var}(q \cdot k) = d_k$。（提示：独立随机变量乘积的方差 $\mathrm{Var}(q_j k_j) = 1$，再用方差可加性）
3. 把因果掩码从"softmax 前加 $-\infty$"改成"softmax 后把上三角位置清零、不再归一化"。对位置 4 那一行，两种方案的权重各是多少？哪个方案破坏了"预算总和为 1"？
4. ★ 位置 9 的「海」看得到全部十个字，位置 0 的「白」只看得到自己。如果现在就要预测第 11 个字，你会取哪个位置的输出向量交给输出层？说出理由，然后在第 7 章·输出与采样验证你的答案。

## 延伸阅读

- Vaswani et al., *Attention Is All You Need* (NeurIPS 2017)——公式与因果掩码的原始出处；缩放理由见其脚注 4
- Bahdanau et al., *Neural Machine Translation by Jointly Learning to Align and Translate* (ICLR 2015)——注意力思想在 Transformer 之前的起源
- Elhage et al., *A Mathematical Framework for Transformer Circuits* (Anthropic, 2021)——从可解释性视角回答"注意力头到底学会了什么"
- Xiao et al., *Efficient Streaming Language Models with Attention Sinks* (ICLR 2024)——一个反直觉的发现：softmax 迫使模型把多余预算倾倒在开头几个 token 上，删掉它们模型会崩——注意力预算机制的当代注脚

---

⬅️ 上一站：[第 2 章 · 位置编码](./part1-chapter02-位置编码.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 4 章 · 多头与 GQA：64 个问题，8 套答案](./part2-chapter04-多头与GQA.md)
