# 第 6 章 · 残差与归一化:深度的两根支柱

> **我们在解剖图上的位置**：站点 ④ 的收尾——两道工序都已认识，现在看清让它们能堆 80 层的保命装置，并组装出完整的一层。
>
> **本章回答的问题**：把"注意力 + FFN"直接串 80 层，为什么必然崩溃？`x = x + f(x)` 这行小学生都看得懂的代码，凭什么被称为深度学习十年来最重要的一行？归一化为什么要放在子层**之前**而不是之后？
>
> **前置依赖**：第 3 章·注意力、第 5 章·FFN（一层里的两道工序分别是什么）。

---

## 6.1 深度的诅咒

先把危机摆到台面上。假设没有任何保护措施，把 80 层"注意力 + FFN"首尾相接：

$$
x_{80} = f_{80}(f_{79}(\cdots f_2(f_1(x_0))))
$$

每个 $f_l$ 都对输入做一次**彻底改写**——乘几个大矩阵、过非线性。两个灾难在数学上几乎必然发生。

**前向：信息在传话游戏中湮灭。** 第 1 层的输出是对 $x_0$ 的加工品，第 2 层加工"加工品"……到第 80 层，「白」的原始语义还能留下多少？这就是传话游戏：一句话经过 80 个人转述，结尾与开头可以毫无关系。模型没有任何机制保证"有用的东西一定被带到最后"。

**反向：梯度在连乘中消失或爆炸。** 训练靠反向传播：损失对浅层参数的梯度，等于一路上每层导数的**连乘**。80 个数连乘是一台放大器也是一台粉碎机——做个数值实验就懂：假设每层导数的"平均缩放倍率"是 0.98（已经非常接近 1 了），穿过 80 层后：

$$
0.98^{80} \approx 0.198
$$

梯度只剩五分之一；若是 0.9，只剩 0.02%——浅层参数几乎收不到训练信号。反过来若是 1.05，$1.05^{80} \approx 49$ 倍，训练当场发散。想让 80 个数的连乘恰好稳定在 1 附近，等于要求走 80 步钢丝一步不歪。2015 年之前，深度网络普遍卡在十几层，就是被这两个灾难拦住的。

Transformer 的解法是两个部件，各治一个方向：**残差连接**保住信息与梯度的通路，**归一化**管住数值的尺度。

## 6.2 残差连接:把"改写"降级为"批注"

### 一个加号改变一切

残差连接（residual connection，源自 2015 年的 ResNet）的全部内容：

$$
x_{\text{out}} = x + f(x)
$$

层的输出不再**替换**输入，而是**加到**输入上。别被朴素的外表骗了，这个加号从根本上改写了"层"的职责说明书：

> 层的任务不再是"重新表达 $x$"，而是"在 $x$ 的原文上补充一条批注"。

于是整个网络的形态变了：$x_0$ 沿着一条**从不中断的主干道**直达第 80 层，每一层只是站在路边，往主干道上追加自己的增量——注意力追加上下文关联，FFN 追加知识：

```
x₀ ══════╤══════════╤══════════╤═══ ⋯ ═══╤══════► x₈₀     ← 主干道（残差流）
         │          │          │          │
      +Attn₁     +FFN₁      +Attn₂     +FFN₈₀
      （批注）    （批注）    （批注）     （批注）

「白」的原始语义沿主干道完好抵达终点；
80 层贡献的是层层叠加的批注，而非 80 次转述
```

这条主干道在文献里有个正式名字——**残差流**（residual stream）。它是理解现代 Transformer 的关键意象：每一层都从主干道**读取**当前状态、计算自己的增量、再**写回**主干道。第 2 章说过"RoPE 旋转的是每层临时投影出的 $q$/$k$，不是主干道上的 $x$"，指的就是这条流。

### 梯度的免费直通车

反向传播时，加号显出第二重身价。对 $x_{\text{out}} = x + f(x)$ 求导：

$$
\frac{\partial x_{\text{out}}}{\partial x} = 1 + \frac{\partial f}{\partial x}
$$

那个孤零零的 **1** 是全部要害：无论 $f$ 的导数多小，梯度总有一条**系数恒为 1 的旁路**直接流回上一层。80 层的连乘，变成了 80 个"$1 + \text{小量}$"的连乘——不再有粉碎机。梯度消失被**结构性**地解决：不靠精细调参，靠拓扑。

### 优雅降级:第 5 章 ★ 题的答案

现在兑现第 5 章练习 4。把某层 FFN 的 $W_{\text{down}}$ 整体置零，$f(x) = 0$，于是：

$$
x_{\text{out}} = x + 0 = x
$$

这一层退化为**恒等映射**——什么也不做，但也什么都不破坏。信号原样通过，其余 79 层照常工作。对比无残差的架构：置零任何一层，输出全零，整栋楼塌。残差给深度网络买了一份冗余保险：**某层学废了，代价不过是白养一层**。这也解释了为什么大模型可以承受剪枝、量化误差甚至个别层的低质量——主干道兜底。

## 6.3 归一化:每次开工前先校准量纲

### 残差留下的尾巴

残差保住了通路，却引入一个新麻烦：主干道上做的是**不断累加**。80 层 × 每层 2 次批注 = 160 次加法，向量的数值尺度会逐层漂移膨胀。而下游部件对输入尺度很敏感——第 3 章刚讲过，点积分数过大时 softmax 一头扎进饱和区；FFN 的门控同样有舒适区间。放任尺度漂移，等于让每层车间面对量纲不明的原料。

解法：在每个子层的入口设一道**校准工序**，把送进车间的向量拉回标准尺度。

### RMSNorm:最省的够用方案

早期 Transformer 用 LayerNorm：减去均值、除以标准差、再做仿射变换。LLaMA 系用的是它的精简版 **RMSNorm**（Root Mean Square Normalization）：

$$
\text{RMSNorm}(x) = \frac{x}{\sqrt{\dfrac{1}{d}\sum_{j=1}^{d} x_j^2 + \epsilon}} \odot \gamma
$$

只做两件事：除以全向量的**均方根**（把向量的整体尺度归一），再逐维乘上可学习的缩放 $\gamma \in \mathbb{R}^{8192}$。与 LayerNorm 相比，砍掉了"减均值"和偏置项——消融实验（Zhang & Sennrich, 2019）表明减均值对 Transformer 的质量几乎没有贡献，砍掉后计算显著更省（原论文实测提速 7%~64% 不等），质量持平。架构设计里的奥卡姆剃刀：**效果相同时，选算得快的那个**。

两个配角各有戏份：$\epsilon$（约 $10^{-6}$~$10^{-5}$）防止全零向量除零；$\gamma$ 归还模型一个自由度——归一化统一了整体尺度，$\gamma$ 让模型自己学"哪些维度值得放大、哪些该压低"。

手感练习（正式版在章末练习 2）：对 $x = (3, 4)$，均方根为 $\sqrt{(9+16)/2} = \sqrt{12.5} \approx 3.54$，归一后 $\approx (0.85, 1.13)$——长的方向还是长，但整体回到 1 的量级。

### Pre-Norm:校准放在门口,别碰主干道

归一化放哪里，是一个吃过亏才定下来的讲究。现代模型清一色采用 **Pre-Norm**：在进子层**之前**归一化，而且**只归一化送进子层的那份副本，绝不动主干道本身**：

$$
x \leftarrow x + \text{Attn}(\text{RMSNorm}(x)), \qquad x \leftarrow x + \text{FFN}(\text{RMSNorm}(x))
$$

```
      主干道（永不被归一化截断）
x ════════════╤═══════════════════════► x + Attn(·)
              │
        RMSNorm（只校准进车间的副本）
              │
            Attn
```

对照早期的 Post-Norm（先加后归一化，即 $x \leftarrow \text{Norm}(x + f(x))$）：Post-Norm 把归一化骑在主干道上，每层都要"截断"一次残差流——6.2 节那条系数为 1 的梯度直通车，每层都被归一化的导数打一次折。后果是深层模型训练极不稳定，必须配合小心翼翼的学习率预热（warmup）才能压住。Pre-Norm 把归一化挪到旁路上，主干道从第 1 层到第 80 层**没有任何操作挡在中间**，深层训练从此稳健（理论分析见 Xiong et al., 2020）。一句话记忆：**校准原料可以，别校准传送带**。

## 6.4 组装:一层车间的完整图纸

零件到齐，把本书基准模型的一层完整画出来：

$$
\begin{aligned}
x &\leftarrow x + \text{GQA-Attn}\big(\text{RMSNorm}(x)\big) \\
x &\leftarrow x + \text{SwiGLU-FFN}\big(\text{RMSNorm}(x)\big)
\end{aligned}
$$

```
x: (10, 8192)  ────────────────────────────── 主干道
 ║
 ╠══════════════┐
 ║         RMSNorm → GQA 注意力（第 3、4 章）     开会：交换上下文
 ║              │        （RoPE 在其内部，第 2 章）
 ╠══◄═══ + ═════┘
 ║
 ╠══════════════┐
 ║         RMSNorm → SwiGLU FFN（第 5 章）        查资料：注入知识
 ║              │
 ╠══◄═══ + ═════┘
 ▼
x: (10, 8192)  → 送入下一层，如此重复 80 次
```

注意进出形状恒为 $(n, 8192)$。这不是巧合而是**残差加法的硬性约束**——两个加数形状必须相同，任何子层的输出都必须回到 8192 维才能写回主干道。这就是 8192 贯穿全书的深层原因：它是主干道的**车道宽度**，全模型唯一不可协商的接口尺寸。

「白日依山尽，黄河入海」的十个向量就在这条流水线上走完 80 层：浅层整理字形与搭配，中层理解句法与对仗，深层汇聚意象与知识（第 5 章的层间分工）。抵达第 80 层出口时，「海」位置的向量已经装载了预测下一个字所需的一切。

只剩最后一个尾巴。Pre-Norm 只校准进车间的副本，**主干道自己从头到尾没被归一化过**——160 次加法之后，第 80 层输出的尺度是漂移的。把这个向量直接交给输出层合适吗？这是章末 ★ 题，答案藏在下一章的第一节。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 深度的诅咒 | 无保护串联 80 层：前向传话失真，反向连乘粉碎（$0.98^{80} \approx 0.2$） |
| 残差连接 | $x + f(x)$：层从"改写"降级为"批注"；导数中的 $1$ 是梯度免费直通车 |
| 残差流 | 主干道意象：每层读取→算增量→写回，$x_0$ 的信息全程保底 |
| 优雅降级 | 子层输出置零 = 恒等映射；深度获得冗余保险（第 5 章 ★ 题答案） |
| RMSNorm | 只除均方根 + 学 $\gamma$：比 LayerNorm 少算减均值，质量持平速度更快 |
| Pre-Norm | 校准放子层门口、不碰主干道——深层训练稳定的关键，免除 warmup 依赖 |
| 车道宽度 | 残差约束进出恒为 $(n, 8192)$：8192 是全模型唯一不可协商的接口 |

## 练习

1. 若每层导数缩放倍率为 0.95，无残差时梯度穿越 80 层衰减到多少？（$0.95^{80}$）有残差后为什么这个连乘模型不再适用？
2. 对 $x = (1, 2, 2)$ 手算 RMSNorm（取 $\gamma = (1,1,1)$、$\epsilon = 0$），验证输出的均方根恰为 1。
3. Post-Norm 与 Pre-Norm 在"每层梯度旁路是否被归一化打折"上有本质区别。画出两种结构的单层数据流图，标出梯度直通路径分别经过哪些运算。
4. ★ 第 80 层的输出即将送往输出层（LM Head）做最后一次大矩阵乘法。考虑到主干道经历了 160 次未校准的加法，你认为在 LM Head 之前还需要做什么？只需一个词的答案——第 7 章第一节揭晓。

## 延伸阅读

- He et al., *Deep Residual Learning for Image Recognition* (CVPR 2016)——残差连接的起源；"恒等映射兜底"思想的出处
- Zhang & Sennrich, *Root Mean Square Layer Normalization* (NeurIPS 2019)——RMSNorm 原论文与"减均值无用"的消融
- Xiong et al., *On Layer Normalization in the Transformer Architecture* (ICML 2020)——Pre-Norm 训练稳定性的理论分析，warmup 之谜的解答
- Elhage et al., *A Mathematical Framework for Transformer Circuits* (2021)——"残差流"视角的系统阐述

---

⬅️ 上一站：[第 5 章 · FFN](./part2-chapter05-FFN.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 7 章 · 输出与采样：分布变成决定](./part3-chapter07-输出与采样.md)
