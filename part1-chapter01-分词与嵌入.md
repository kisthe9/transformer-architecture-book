# 第 1 章 · 分词与嵌入:语言的数字化两级跳

> **Part I · 请求进门** 从这里开始。这一部分（第 1~2 章）回答一个层次的问题：**模型只认浮点数矩阵，字符串如何获得数字身份、语义坐标与顺序感？** 第 1 章解决前两样，第 2 章解决最后一样。
>
> **我们在解剖图上的位置**：站点 ②（分词）与站点 ③（嵌入）——请求刚被调度器放行，即将进入算法内核的大门。
>
> **本章回答的问题**：为什么说模型从头到尾没见过一个汉字？词表为什么是 3.2 万而不是 30 万或 3000？查表查出来的 8192 个浮点数，凭什么"是"一个字的含义？
>
> **前置依赖**：第 0 章·鸟瞰（八个站点的全局地图）。

---

## 1.1 一个需要先接受的事实

打开任何一个 Transformer 的实现，从第一行到最后一行，你找不到一处代码在处理"文字"。注意力在乘矩阵，FFN 在乘矩阵，归一化在算平方和——**全部部件的输入输出都是浮点数张量**。「白日依山尽」对模型而言并不存在，存在的只是一串数字以及数字之间的几何关系。

所以在进入 80 层车间之前，必须有一道翻译工序把字符串变成数字。这道工序分两级，各自解决一个独立的问题：

| 级 | 名称 | 输入 → 输出 | 解决的问题 |
|----|------|------------|-----------|
| 第一级 | 分词（tokenization） | 字符串 → 整数序列 | 文本切成什么**粒度**？ |
| 第二级 | 嵌入（embedding） | 整数序列 → 浮点矩阵 | 编号如何携带**语义**？ |

分界线值得记牢：第一级的产物是**编号**——它只标识身份，不表达含义，就像图书馆书架上的索书号；含义要到第二级才诞生。很多初学者把两级混为一谈，后面理解"词表外推""嵌入共享"这类话题时就会卡壳。

## 1.2 第一级：分词

### 分词器看到的输入

第 0 章站点 ① 交代过，你提交的 `messages` 数组经 Chat Template 拼接后，变成了一段包含特殊标记的扁平文本。以本书的例子里（Llama 2 格式）：

```
"<s>[INST] <<SYS>>你是一位精通古诗的助手<</SYS>> 帮我补全这首诗：白日依山尽，黄河入海 [/INST]"
```

这段文本就是分词器的输入。其中 `<s>`（BOS，序列开始）、`[INST]`、`<<SYS>>` 等都是**特殊 token**（special tokens）——它们在词表里拥有专属编号（如 LLaMA 的 `<s>` 是编号 1），分词器识别到它们时直接映射为对应编号，不走 BPE 合并流程。它们与普通文字 token 一样参与后续的嵌入、注意力、FFN 计算——模型正是通过这些标记的向量表示“知道”当前处于什么角色、什么轮次。

> **本书的简化约定**：为聚焦内核机制，此后各章的 running example 省略 chat template 前缀，直接以「白日依山尽，黄河入海」这十个字作为 prompt。这不影响任何原理：对模型而言，特殊标记与汉字 token 在流水线上的待遇完全相同。

### 两个天然方案为何都不行

切分粒度有两个显而易见的候选，各有致命伤：

**按单个字符切**。词表小得可爱（覆盖常用中英文几千个符号就够），但两个代价太重：其一，序列长度膨胀——同样的内容占用更多 token，而注意力的计算量随序列长度平方增长（第 3 章会算这笔账）；其二，单字符的语义太稀薄，「依」在「依山」「依赖」「依然」里是三种含义，模型被迫用大量层数去从上下文重建词义。

**按完整词语切**。语义饱满了，但词表失控——中文词汇几十万条且每天有新词诞生，英文加上屈折变化更是无穷无尽。更糟的是**未登录词问题**：词表定死之后，任何没收录的词（新人名、错别字、代码标识符）只能映射到一个万能的「未知」符号，信息当场蒸发。

### 子词：从语料里学出来的折中

现代 LLM 全部采用第三条路——**子词分词**（subword tokenization），最常用的算法叫 **BPE**（Byte Pair Encoding，字节对编码）。它的策略不是人来规定切分规则，而是**让语料的统计规律自己说话**：

> 从最小单元出发，反复把语料中**共现频率最高的相邻单元对**合并成一个新单元，直到词表达到预定大小。

这段话有点抽象，用一份五个词的英文迷你语料手推一轮（外部读者可拿纸笔跟一遍，这是理解 BPE 的最短路径）：

```
语料（词后附频次）:  low ×5   lower ×2   newest ×6   widest ×3   wide ×2

第 0 步——全部拆成字符，统计相邻字符对的出现频次:
    (e,s) 出现在 newest×6 + widest×3   → 9 次   ← 最高频
    (s,t) 出现在 newest×6 + widest×3   → 9 次
    (l,o) 出现在 low×5 + lower×2       → 7 次
    (w,i) 出现在 widest×3 + wide×2     → 5 次
    ...

第 1 步——合并最高频的 (e,s) 为新单元 "es":
    newest → n e w es t      widest → w i d es t

第 2 步——重新统计，(es,t) 现在 9 次最高，合并为 "est":
    newest → n e w est       widest → w i d est

第 3 步——(l,o) 7 次最高，合并为 "lo":
    low → lo w               lower → lo w e r
    ⋮ 如此迭代，直到词表达到预定大小（如 32000）
```

三轮下来规律已经浮现：**高频组合凝固成整体（"est" 这样的高频后缀独立成 token），低频组合保持零散**。训练结束后得到两样产物：一张词表 + 一份按顺序记录的合并规则表。推理时对新文本按同样的规则表贪心合并，就得到确定的切分。

这个设计的收益清单：

1. **词表大小可控**——是超参数，你说 32000 它就停在 32000；
2. **常见词高效**——"the"、高频汉字独占一个 token，不浪费序列长度；
3. **永无未知词**——任何字符串最坏退化为最小单元的拼接，绝不会遇到"未知"。

第 3 点的底气来自 LLaMA 分词器（SentencePiece 实现的 BPE）的**字节回退**（byte fallback）机制：连词表都没收录的罕见字符，直接拆成 UTF-8 字节——256 个字节 token 是词表的保底层，宇宙中任何文本都逃不出它的覆盖。

### 我们的诗被切成了什么

「白日依山尽，黄河入海」经过分词器：

```
"白日依山尽，黄河入海"
      │  按合并规则表切分
      ▼
[白] [日] [依] [山] [尽] [，] [黄] [河] [入] [海]
      │  查词表（片段 → 编号）
      ▼
[2312, 1907, 4023, 855, 3241, 30, 5722, 1385, 2076, 3811]
```

> **教学简化声明**（全书唯一一处系统性简化，此后不再重复解释）：真实的 LLaMA-2 分词器以英文语料为主训练，多数汉字不在 32000 的词表里，会经字节回退被拆成 2~3 个字节 token——「白日依山尽，黄河入海」真实切出来可能是二十多个 token。本书假设**一个汉字恰好一个 token**，让例子可以逐字讲解。这个简化不伤害任何原理：进入模型的永远是"一串整数、长度记为 $n$"，至于每个整数背后是一个汉字还是三分之一个汉字，后续所有数学完全不关心。本章取 $n = 10$。

### 词表大小是一场三方拉锯

为什么 LLaMA-2 停在 32000？这是三股力量的平衡点：

- **词表越大** → 同样文本切出的 token 越少 → 序列越短、生成步数越少（省算力）；
- **词表越大** → 嵌入矩阵和输出层越大（各占 $V \times 8192$ 个参数），且每个罕见 token 分到的训练样本越稀薄（学不好）；
- **多语言覆盖**：以英文为主的 32000 词表对中文很不友好（字节回退导致 token 数翻倍以上）——这正是后来的模型集体扩表的原因：Llama 3 扩到 128K，Qwen2 扩到 152K，中文效率立竿见影地改善。

这场拉锯对你有直接的钱包意义：

| 工程关切 | 与分词的关系 |
|----------|-------------|
| API 账单 | 按 token 计费。同一段意思，在 32000 词表下中文比英文贵 50%~100%；换 128K 词表的模型，差距显著缩小 |
| 上下文窗口 | "128K 上下文"数的是 token。分词效率决定同一个窗口里实际装得下多少文档 |
| 生成速度 | 一步一个 token。被切得越碎的文本，生成同样内容需要越多步 |

选型时拿目标语言的真实语料跑一遍候选模型的分词器、数一数 token，是五分钟就能做完却常被跳过的尽职调查。

## 1.3 第二级:嵌入

### 全模型最简单的一步

十个整数现在要变成向量。执行这一步的是**嵌入矩阵** $E$，形状 $(V, d) = (32000, 8192)$，约 2.6 亿个参数。而它的用法简单到反高潮——**不做任何计算，只按行号取行**：

$$
X[i] = E[\,\text{id}_i\,], \qquad X: (n, d) = (10, 8192)
$$

编号 2312（「白」）就取 $E$ 的第 2312 行——8192 个浮点数；十个编号取十行，摞起来就是矩阵 $X$。第 0 章解剖图里"进入 80 层车间的原材料"，指的就是它。

```
E: (32000, 8192)                          X: (10, 8192)
┌────────────────────────┐
│ 第 0 行     …           │    取行        ┌──────────────┐
│   ⋮                    │  ─────────►    │ E[2312] 「白」│
│ 第 2312 行  ◄── 白      │  [2312,1907,   │ E[1907] 「日」│
│   ⋮                    │   4023, …]     │    ⋮          │
│ 第 31999 行 …           │                │ E[3811] 「海」│
└────────────────────────┘                └──────────────┘
```

用代码写出来同样朴素（本书代码锚点约定：纯 NumPy、变量名对齐正文符号、复制即可运行）：

```python
import numpy as np

V, d = 32000, 8192
rng = np.random.default_rng(0)
E = rng.normal(size=(V, d)).astype(np.float32)   # 真实模型中 E 是训练得到的

token_ids = [2312, 1907, 4023, 855, 3241, 30, 5722, 1385, 2076, 3811]
X = E[token_ids]                                  # 嵌入 = 花式索引，零浮点运算

print(X.shape)        # (10, 8192)
print(X[0][:4])       # 「白」向量的前 4 维——一串看似随机的浮点数
```

### 凭什么这 8192 个数"是"白的含义

查出来的一行浮点数，何以承载「白」的语义？答案分两半。

**这些数是被训练逼出来的坐标。** $E$ 初始时确实是随机数（如上面代码所示）。预训练的目标只有一个——把"预测下一个 token"做准。要做准，模型必须利用一个统计事实：**用法相似的词出现在相似的上下文里**。「白」「素」「皎」经常可以互换出现，把它们的向量安排得彼此靠近，模型就能用一套共享的下游权重处理它们，预测损失随之下降。亿万次梯度更新之后，词表在 8192 维空间里自发排布成一张语义地图：距离编码相似度，某些方向甚至编码可复用的关系（性别方向、时态方向——著名的 king − man + woman ≈ queen 就是这种几何性的实证）。

**含义在方向里，不在坐标轴里。** 一个常见误解需要当场拆掉：这 8192 个维度不是 8192 个"属性栏"，不存在"第 305 维 = 颜色，第 4477 维 = 名词性"。真实情况是每个语义特征弥散在成百上千个维度的组合上，每个维度也同时参与表达无数特征——术语叫**分布式表示**（distributed representation）。可以把一个词向量理解为高维空间中的一个**方向**：单独看任何一个坐标分量毫无意义，两个方向之间的夹角与距离才携带信息。这也是为什么后面所有章节比较向量时用的都是点积（第 2、3 章）而不是逐维解读。

> **类比与它的边界**：把嵌入空间想成一张"语义城市地图"——近义词是邻居，主题相近的词在同一个街区——这个类比在"距离≈相似度"这一点上成立。但它有两处失效：其一，地图是二维的，而 8192 维空间里"邻居"可以多到违反平面直觉（一个点可以同时紧邻几千个点）；其二，地图上的位置是唯一的，而一个多义词的向量其实是它所有义项的**叠加混合**——这个缺陷马上就要登场。

### 此刻的向量还有两个"没有"

矩阵 $X$ 已经拿到，但对照最终目标（预测「流」），它还缺两样东西。这两个缺口分别是接下来两章的全部动机，值得郑重写下：

**没有语境。** 查表是无条件的：「海」在「黄河入海」和「海量数据」里取出的是**同一行** $E[3811]$。此刻的「海」向量是它所有用法的平均混合体——三分咸水、三分巨量、几分姓氏。让「海」在这句诗里收敛成"江河归宿的海"，需要它与「黄」「河」「入」交换信息——那是注意力的工作，第 3 章·注意力揭晓。

**没有顺序。** 矩阵的行之间没有任何"先后"的概念。把 $X$ 的十行随意打乱——「海入河黄，尽山依日白」——得到的矩阵包含完全相同的十个向量，只是排列不同。而后续的注意力计算恰恰是一种对行序不敏感的运算（第 2 章开头会用一个两行的小实验证明这一点）。语言的意思几乎全押在顺序上（「日白」不是「白日」），顺序信息却在数字化的过程中丢失了。把"你是第几个字"重新写进计算，是第 2 章·位置编码的使命。

一句话总结此刻的状态：**十个孤独的、不知道自己站在哪里的"平均词义"，正排队等待加工。**

## 1.4 工程视角：嵌入层的另外两个身份

在推理工程里，这张 $(32000, 8192)$ 的表还会以两个身份再次出现，先在这里挂号：

1. **它有一个镜像兄弟**。第 7 章会遇到 LM Head——一张 $(8192, 32000)$ 的矩阵，负责把最终向量翻译回词表分数。它恰好是嵌入的形状转置，一些模型（GPT-2 等）干脆让两者共享同一份参数（weight tying），省 2.6 亿参数；LLaMA-2 为了质量选择各存一份。进门查表、出门对表，一进一出的对称性是理解输出层的捷径。
2. **它是显存账本的固定条目**。2.6 亿参数 × 2 字节 ≈ 0.5 GB，在 140 GB 的权重总账里只占约 0.4%——嵌入表看着吓人，其实是小头。真正的大头在哪，第 5 章（FFN 占约八成）和第 9 章（KV Cache 动态增长）各有一笔震撼的账。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 两级翻译 | 分词管粒度（字符串→编号），嵌入管语义（编号→向量）；编号本身无含义 |
| BPE | 高频相邻对反复合并；词表大小可控 + 常见词高效 + 字节回退保证永无未知词 |
| 词表拉锯 | 大词表省序列长度、贵参数且稀释训练；32000 是英文中心的取舍，新模型普遍扩表 |
| 嵌入 = 取行 | $E: (32000, 8192)$，零计算查表；坐标由"预测下一个词"的训练压力自发塑造 |
| 分布式表示 | 含义在方向与夹角里，单个维度不可解读 |
| 两个"没有" | 没有语境（第 3 章解决）、没有顺序（第 2 章解决）——本书最重要的两个悬念 |

## 练习

1. 用 1.2 节的迷你语料继续往下推两步合并：第 4 步和第 5 步分别合并哪个单元对？合并后 "lower" 被切成什么样？（完整推演见附录 B）
2. 打开任意在线分词器演示页（如 tiktoken 或 SentencePiece 的演示），输入同一段中英对照文本，记录两者的 token 数之比；再换一个大词表模型（如 Llama 3 系）重复一次，验证 1.2 节"扩表改善中文效率"的说法。
3. 嵌入矩阵 $E$ 的参数量精确是多少？在 FP16 下占多少显存？占 70B 总参数的比例是多少？（对账答案见附录 A 的参数量账本）
4. ★ 设计一个思想实验：把 $X$ 的第 0 行（「白」）与第 9 行（「海」）整体对调后送入模型。如果模型**没有**任何位置机制，最终"预测下一个字"的结果会变吗？为什么？带着你的答案进入第 2 章·位置编码——它的第一节就是这个实验的正式版。

## 延伸阅读

- Sennrich et al., *Neural Machine Translation of Rare Words with Subword Units* (ACL 2016)——BPE 进入 NLP 的原点，1.2 节手推的算法出处
- Kudo & Richardson, *SentencePiece: A simple and language independent subword tokenizer* (EMNLP 2018)——LLaMA 系分词器的实现基础，字节回退机制在此
- Mikolov et al., *Efficient Estimation of Word Representations in Vector Space* (2013)——词向量几何结构（king − man + woman ≈ queen）的经典实证
- Touvron et al., *LLaMA 2* (2023) §2——本书基准模型词表与分词配置的一手来源

---

⬅️ 上一站：[第 0 章 · 鸟瞰](./part0-chapter00-鸟瞰.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 2 章 · 位置编码：旋转出来的顺序感](./part1-chapter02-位置编码.md)
