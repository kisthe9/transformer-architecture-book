# 附录 D · 思考的解剖:推理模型如何"想"

> 2024 年之后，你调用的模型多了一个新举动：回答难题前先"想一会儿"——界面上转着圈，账单里多出一笔 reasoning tokens。这篇附录用全书已有的语言解剖这件事，你会得到一个可能反直觉的结论：**"思考"没有给解剖图增加任何新器官——它只是站点 ⑥ 的刻意延长。**
>
> **前置依赖**：第 7 章（模型的产出是分布）、第 8 章（自回归循环与 KV Cache）、第 10 章（Decode 的成本结构）。

---

## D.1 先看现象:思考的字面形态

给一个推理模型出道数学题，把它的原始输出打开看，形态大致是：

```
<think>
设这批货物共 x 件。第一天卖出 x/3……
等等，题目说的是"剩余的一半"，我重新列式……
所以 x = 84。验算：84/3 = 28……成立。
</think>
这批货物共有 84 件。
```

两段之间隔着一对特殊标记（如 `<think>`…`</think>`，不同厂商叫法不同）。标记之前的几百上千个 token 是**思考轨迹**——打草稿、试错、回头检查；标记之后才是给用户看的答案。有的产品把思考过程折叠或隐藏，但计费表不说谎：那些看不见的 token 一个都没少收钱。

现在用全书的眼光提问：这些思考 token 在流水线上有什么特殊待遇吗？

**没有。一点都没有。** 它们与「流」「。」「欲」完全同等：从同一张 32000 行的词表里采样出来（第 7 章），逐个走完同样的 80 层（第 3~6 章），逐个追加进同样的 KV Cache（第 8 章），逐个占用同样的 Decode 步（第 10 章）。`<think>` 标记本身也只是词表里的普通 token。**推理模型没有"思考电路"这个新硬件——思考是用旧机器跑出来的新行为。**

## D.2 为什么多生成一段话,答案就会变好

这才是真正值得解剖的问题。机器没变，多打一页草稿凭什么提高正确率？两个互相咬合的解释，全部建立在本书已有的概念上。

**解释一：串行深度是花钱买不到的，但可以用长度换。** 回忆第 6 章：无论问题多难，一个 token 的前向计算深度**恒为 80 层**——这是架构写死的。一道需要十步推导的数学题，硬要在"一次 80 层"里从题面直接映射到答案，等于要求流水线一步登天。而思考轨迹把这道题改写成了另一个任务：每一步只需要在 80 层里完成"从前文推出**下一小步**"——这恰好是模型被训练了万亿次的本行（预测下一个 token）。生成 500 个思考 token，等于把 80 层的流水线**串行复用了 500 次**：草稿上的每一行中间结论，都通过 KV Cache 变成后续步骤可检索的"已知条件"（第 8 章——KV Cache 在这里扮演了**工作记忆**的角色）。序列长度成了第三个可伸缩的算力维度，学界称之为 **test-time compute**（测试时计算）：参数不变、训练不变，只靠推理时多花计算换更好的答案——2024 年的多项研究表明，这条曲线在很多任务上比继续堆参数更划算。

**解释二：中间步骤给了分布"纠错的机会"。** 第 7 章说过模型每步产出的是分布。直接答题时，一步押错满盘皆输；有思考轨迹时，模型可以在草稿里生成"等等，这里不对"——这句话本身也是采样出来的普通 token，但它一旦进入上下文，后续所有步骤的条件分布就被它改写了（第 3 章：注意力让每个新 token 都看得到这句自我否定）。试错、回溯、验算，全部是**用生成来实现的控制流**。

一句话总结：**思考 = 把"一次前向解决"改写成"多次前向接力"，用序列长度购买有效计算深度，用 KV Cache 充当草稿纸。**

## D.3 能力从哪来:从"求它想"到"它自己想"

思考行为的来源经历了两代（训练细节超出本书范围，只交代脉络）：

**第一代：提示出来的思考（2022~）。** Chain-of-Thought 提示——在问题后面加一句"让我们一步一步地想"，或给几个带推导过程的示例——就能让足够大的模型自发写出推理链，准确率跳涨（Wei et al., 2022）。这个发现的深意：**分步推理的能力早已潜伏在预训练权重里**（毕竟训练语料里满是人类写的推导过程），提示只是把它唤醒。

**第二代：训练进去的思考（2024~）。** OpenAI o1 与 DeepSeek-R1 把"想"从可选项变成本能：用强化学习在**可验证的任务**（数学有标准答案、代码能跑测试）上训练——答案对了整条轨迹得到奖励，答错了不奖励。模型在这种压力下**自发**演化出越来越长的思考、自我检查甚至推翻重来的行为（R1 论文记录了训练中思考长度自发增长的曲线，这是 2025 年最令人印象深刻的实验图之一）。R1 完整公开了思考轨迹与训练配方，把这一代能力带进了开源世界。

对本书读者的意义：第一代思考是**你在 prompt 里索取的**，第二代是**模型出厂自带的**——但两者在推理流水线上的形态一模一样：更长的站点 ⑥。

## D.4 工程账单:思考不是免费的

"用长度换深度"这笔交易的另一面，恰好把本书 Part III/IV 的每一本账都翻出来重算一遍：

| 账目 | 本书出处 | 思考带来的变化 |
|------|---------|---------------|
| Decode 步数 | 第 8 章 | 一道难题思考 2000 token = 多跑 2000 个 Decode 步；按 14 token/s 的单请求上限（第 10 章），光思考就要两分多钟 |
| KV Cache | 第 8/9 章 | 2000 个思考 token × 320 KB = **多吃 0.66 GB 显存**，且在整个回答期间不释放——思考型负载让第 9 章的显存战争全面升级 |
| 计费 | 第 1 章工程视角 | 思考 token 按**输出价**计费（输出 = Decode，是两阶段中贵的那个，第 8 章解释了为什么）——"看不见的字最贵" |
| 用户体感 | 第 8 章 TTFT | 答案的"首字"要等整段思考结束才出现——推理模型的体感延迟主要是思考长度，而非 Prefill |
| 调度 | 第 10 章 | 思考请求是超长 Decode 序列，与短请求混批时挤占块池——Continuous Batching 的调度公平性面临新考题 |

于是厂商给了你一个新旋钮：**思考预算**（各家 API 里的 reasoning effort / thinking budget 类参数）——本质是限制或引导 `<think>` 段的长度。它是继 temperature（质量-多样性）之后的又一个权衡旋钮：**准确率-延迟-账单**三角，简单任务调低白省钱，难题调高买正确率。选型与调参时，把"思考长度"当作和"上下文长度"同级的一等公民来做容量规划——这是 2024 年后推理工程新增的必修课。

## D.5 复用清单:全书概念在"思考"下的对应

| 本书概念 | 在思考机制里的角色 |
|----------|-------------------|
| 第 7 章 · 每步产出分布 | "等等，不对"是采样出来的普通 token——自我纠错是生成行为 |
| 第 8 章 · 自回归循环 | 思考 = 循环的刻意延长；`</think>` 之后循环继续产出答案 |
| 第 8 章 · KV Cache | 思考轨迹的 K/V 全程驻留——模型的"工作记忆"物理上就是它 |
| 第 6 章 · 固定 80 层 | 单步深度写死 → 用序列长度换串行深度（test-time compute 的架构根源） |
| 第 9/10 章 · 显存与调度 | 思考型负载 = 超长 Decode + 大缓存，两大战役的新战场 |

边界声明：本附录不展开第二代能力的训练方法（奖励设计、策略优化算法）——那是一本训练之书的领地；本书只负责让你看清**思考在推理流水线上的真身**。

## 思考题（答案见附录 B）

1. 某请求 prompt 500 token、思考 3000 token、答案 200 token。分别估算：思考结束时的 KV Cache 总量（GQA-8, FP16）；若按 14 token/s 的单请求 Decode 上限，用户看到答案首字前大约等了多久？
2. 有人建议：思考 token 反正用户看不见，为什么不在 `</think>` 出现时立刻把思考段的 KV Cache 释放掉，省下显存？用第 3 章因果掩码和第 8 章缓存合法性的语言分析：答案段生成期间能不能扔掉思考段的 K/V？扔了会发生什么？

## 延伸阅读

- Wei et al., *Chain-of-Thought Prompting Elicits Reasoning in Large Language Models* (NeurIPS 2022)——第一代思考的起点
- Kojima et al., *Large Language Models are Zero-Shot Reasoners* (NeurIPS 2022)——"一句话唤醒思考"（Let's think step by step）的出处
- DeepSeek-AI, *DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning* (2025)——第二代思考的公开配方；思考长度自发增长曲线在此
- Snell et al., *Scaling LLM Test-Time Compute Optimally Can Be More Effective than Scaling Model Parameters* (2024)——"长度换深度"这笔交易的定量研究
- OpenAI, *o1 System Card* (2024)——reasoning tokens 的产品化形态与计费口径的一手来源

---

⬅️ [附录 C · 多模态架构一瞥](./appendix-c-多模态.md) ｜ [返回目录](./README.md)
