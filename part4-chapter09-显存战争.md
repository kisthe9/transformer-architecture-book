# 第 9 章 · 显存战争:分页拯救 KV Cache

> **Part IV · 工程战场** 从这里开始。这一部分（第 9~10 章）回到解剖图的外壳：算法正确只是及格线，真正的考题是**同样的硬件如何服务多几十倍的用户**。第 9 章打显存的仗，第 10 章打带宽和调度的仗。
>
> **我们在解剖图上的位置**：站点 ⑦（显存与调度）的前半场。
>
> **本章回答的问题**：140 GB 的模型怎么塞进 80 GB 的卡？显存总量明明够，为什么并发一高就 OOM？操作系统五十年前发明的虚拟内存分页，怎么在 2023 年拯救了 LLM 推理？
>
> **前置依赖**：第 8 章·自回归与 KV Cache（1.34 GB/请求的动态缓存从何而来）、第 4 章·多头与 GQA（KV 头数为 8）。

---

## 9.1 开战之前:先把 70B 塞进机器

有一个问题从第 0 章拖到现在必须正面回答：权重 140 GB，单卡最大 80 GB，**模型根本放不进一张卡**。这不是优化问题，是生存问题。

答案是**张量并行**（Tensor Parallelism, TP；方案定型于 Megatron-LM）：把每一个大矩阵**切开**分给多张卡，每张卡只算自己那一片，靠高速互联（NVLink）拼合结果。以 TP = 4 为例：

- **注意力按头切**：64 个 Q 头、8 组 KV 头，每卡分得 16 个 Q 头、2 组 KV 头。各头本来就互相独立（第 4 章），切分天然无痛——这也是"KV 头数取 8"暗合工程的地方：8 能被常见的 TP 度（2/4/8）整除，每卡分得整数组。
- **FFN 按中间维切**：28672 个知识槽位每卡分 7168 个。槽位之间本来就互相独立（第 5 章），同样天然可切。
- **代价**：每层需要两次跨卡的求和同步（AllReduce）——注意力出口一次、FFN 出口一次。80 层就是 160 次通信，这是 NVLink 带宽存在的意义。

TP = 4 之后，每卡摊到 **~35 GB 权重**。一张 80 GB 的卡的资产负债表终于可以画出来了：

```
单卡 80 GB（TP=4 视角）:
├─ 模型权重分片（静态，服务期间纹丝不动）        ~35 GB
├─ KV Cache 块池（动态，本章的战场）            ~40 GB
└─ 激活值 + 通信缓冲 + CUDA 开销                ~5 GB
```

权重是一次付清的房贷，真正的战争在中间那 40 GB：每个请求进来要分一块，**长度不可预知、随生成不断变长、随时可能结束**。如何为这样的"动态租户"分配空间——这是一道纯粹的内存管理题，而 2023 年之前所有推理框架的答案都不及格。

开战之前，先兑现第 2 章 §2.7 的剧透。那一节说：一个吃满 200K token 的请求，光 KV 缓存就需要约 65 GB。账本公式第 4 章已经给过，现在可以精确结清：GQA-8、FP16 下每 token 每层 $2 \times 8 \times 128 \times 2$ B = 4 KB，乘 80 层 = 320 KB/token，再乘 200K——

$$
320\ \text{KB} \times 200{,}000 \approx 65\ \text{GB}
$$

**比上面账本里整个 40 GB 块池还大 60%。** 一个长上下文请求的缓存就装不进这张卡；若是没有 GQA 的 MHA 时代，同样的 200K 要 ~524 GB，纯属天方夜谭。这就是上下文窗口"第三层天花板"（第 2 章 §2.7）的具体形状：长上下文服务要么上更多卡、要么把缓存压小（§9.5 的量化战线），要么干脆对超长请求拒载。本章的战役主要围绕大量"短租"租户展开，但请记住鲸鱼租户真实存在——它解释了为什么许多服务把窗口开得远比模型量程小。

## 9.2 朴素分配的三宗罪

传统做法直白：请求到达时，按**可能的最大长度**（如 max_seq_len = 2048）一次性预留一整块**连续**显存。三个恶果接踵而至。

**罪一：内部碎片——为"可能性"付全款。** 请求实际只生成了 60 个 token 就结束，预留的 2048 个坑位里 1988 个从未使用——但显存已被占走，其他请求用不了。相当于酒店要求每位客人按"最长可能住一年"付押金。

**罪二：外部碎片——空闲却拼不齐。** 各请求预留的块大小不一、结束时间不一，释放后显存布满大小参差的空洞。总空闲量明明够新请求用，却找不出一块**连续**的够大区域——分配失败，请求排队，GPU 干等。任何写过内存分配器的工程师都会对这个剧情会心一笑。

**罪三：无法共享。** 100 个请求共用同一段 system prompt，它们前缀部分的 KV **逐比特相同**（第 8 章：K/V 只由前文决定），却被各存了 100 份——99 份纯浪费。

vLLM 团队在真实负载下实测：传统方式的 KV 显存**有效利用率只有 20%~40%**（Kwon et al., SOSP 2023）。一张卡一大半的显存在"被占用地闲置"。三宗罪追到根上是同一条隐含假设：

> **每个请求的 KV Cache 必须在显存中连续存放。**

## 9.3 五十年前就有的答案

"程序申请连续内存 → 碎片化 → 利用率崩塌"——这个剧本计算机史上一字不差地上演过。1960~70 年代的操作系统面对完全相同的困局，给出的答案沿用至今：**虚拟内存分页**。把物理内存切成等大的页，程序看到的是连续的虚拟地址，页表负责把虚拟页映射到散落各处的物理页帧——"连续"成为一种由映射制造的幻觉。

**PagedAttention**（vLLM, 2023）做的事，就是把这套机制原样搬进 KV Cache 管理。对照表可以直接抄 OS 教科书：

| 操作系统 | PagedAttention |
|----------|----------------|
| 进程 | 一个请求 |
| 虚拟页 | 逻辑 KV 块 |
| 物理页帧 | 物理 KV 块 |
| 页表 | 块表（block table） |
| 缺页时按需分配 | 写满一块再领下一块 |
| fork 的写时复制 | 共享前缀的 CoW |

## 9.4 分页三步曲

### 第一步:显存切块

引擎启动时，把那 ~40 GB 的 KV 区整体切成**等大的物理块**，组成全局块池。vLLM 默认每块存 **16 个 token** 的 K/V（GQA-8 下即 16 × 4 KB × 每层，块按层组织）。块大小是折中的产物：块太小则块表膨胀、寻址开销上升；块太大则内部碎片卷土重来（最后一块平均半空）。

### 第二步:块表映射

每个请求持有一张**块表**，记录"我的逻辑块序列对应哪些物理块"。我们那首诗的请求生成到 24 个 token 时：

```
逻辑视角（请求以为自己是连续的）:
   [白日依山尽，黄河入海流。欲穷千][里目，更上一层楼。<EOS>…]
          逻辑块 0（16 token）           逻辑块 1

块表:   逻辑块 0 → 物理块 B203
        逻辑块 1 → 物理块 B7        ← 物理上相距十万八千里

块池:   [B0][B1]…[B7●]……………[B203●]…
              ↑ 诗的后半          ↑ 诗的前半
```

**逻辑连续、物理分散**，三宗罪应声而解：

- **内部碎片**：按需领块，写满一块才领下一块——浪费上限 < 16 个 token 的坑位。实测有效利用率从 20~40% 跃升至 **90% 以上**；
- **外部碎片**：所有块等大，任何空闲块都能满足任何请求——外部碎片**在定义上消失**；
- **共享**：见第三步。

### 第三步：写时复制的前缀共享

100 个请求共用同一段 system prompt，其 KV 物理上只存**一份**，100 张块表指向同一批物理块（引用计数 = 100）。只有当某个请求要在共享块的空位上追加自己的新 token 时，才复制这一块给它专用——**写时复制**（Copy-on-Write），与 OS 的 fork 语义逐字对应。

顺着这个机制再往前一步就是**前缀缓存**（prefix caching）：跨请求自动识别相同前缀、直接复用现成的 KV 块。回收第 8.6 节的悬念：多轮对话中，第 $k$ 轮的 prompt 前 $k-1$ 轮与上一轮完全相同，其 KV 逐比特相同（第 8.2 节的缓存合法性保证）——前缀缓存让第 $k$ 轮的 Prefill 只需处理新增的几十个 token，而不是重新啃全部历史：

```
无缓存:  第 5 轮 Prefill 70 token → TTFT ∝ 70² = 4900
有缓存:  前 60 token 的 KV 命中缓存 → 只 Prefill 新增 10 token → TTFT ∝ 10² = 100
                                                                  约 50 倍改善
```

长 system prompt 场景（几千 token 的指令）同样受益巨大：所有请求的 system prompt 部分只需 Prefill 一次，此后近零开销。vLLM V1 已把前缀缓存做成默认能力，SGLang 的 RadixAttention 则用基数树管理所有请求的前缀 KV，自动最大化共享。

### 代价与回报

天下没有免费的映射。注意力内核必须学会**按块表跳跃取数**——K/V 不再连续，标准内核直接失效，vLLM 为此定制了 CUDA 内核：按块 gather、配合在线 softmax 逐块累积注意力结果。每次取数多一层查表寻址，但一次查表换来的是一整块 16 个 token 的连续读取，均摊后开销很小（章末练习 3）。

回报是**吞吐量提升 2~4 倍**：省出来的显存直接转化为同一张卡上更多的并发请求。至于"更多并发"为什么恰恰是喂饱 GPU 的关键——这个钩子留给第 10 章，它是两章之间真正的接力棒。

## 9.5 战役未完:把缓存变得更小的三条战线

PagedAttention 解决了"怎么放"，围绕"放得更少"的军备竞赛仍在推进。三条战线彼此独立、**可以叠乘**：

| 战线 | 思路 | 效果量级 |
|------|------|---------|
| **KV 量化** | 缓存用 FP8 甚至 INT4 存放，计算时还原；K 和 V 的数值分布不同，常用不对称策略分别定标 | 再省 50%~75% |
| **前缀复用** | RadixAttention（SGLang）用基数树管理所有请求的前缀 KV，自动最大化共享 | 共享场景 TTFT 大幅下降 |
| **架构级压缩** | MLA（第 4 章 4.6 节）：训练时就把 KV 压成低秩潜向量，缓存的从"8 组 K/V"变成"1 个潜向量" | 相关工作报告 90% 以上压缩 |

这里可以兑现第 4 章练习 4 的伏笔了：MLA 为什么要对 RoPE 做**解耦设计**？因为 RoPE 的旋转角依赖位置（第 2 章），若把 K 压缩进潜向量再解压，"先压缩后旋转"与"先旋转后压缩"并不等价——位置信息会在低秩压缩中被搅浑。DeepSeek 的解法是把 K 拆成两段：一小段专门携带 RoPE 位置信息、不压缩，其余大段无位置信息、放心压缩——各行其道，互不污染。

把几条战线叠起来看战果：GQA-8（÷8，第 4 章）× FP8 量化（÷2）已把 KV Cache 压到 MHA-FP16 的 **1/16**；换 MLA 还能更低。显存战争每推进一寸，都在同样的硬件上兑换成更高的并发和更长的上下文——而并发，正是下一章故事的主角。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 张量并行 | 按头切注意力、按槽位切 FFN，每层两次 AllReduce；TP=4 后每卡 ~35 GB 权重 |
| 战场划分 | 权重是静态房贷，KV Cache 是动态租户——难题全在后者的 40 GB |
| 三宗罪 | 内部碎片（为可能性付全款）、外部碎片（空闲拼不齐）、无法共享 |
| 病根 | "KV 必须连续存放"这条隐含假设 |
| 分页解法 | 等大块池 + 块表映射 + CoW 共享——OS 虚拟内存的完整复刻 |
| 战果 | 利用率 20~40% → 90%+，吞吐 2~4 倍；代价是内核按块表跳跃取数 |
| 三条战线 | KV 量化 × 前缀复用 × MLA 架构压缩，与 GQA 正交叠乘 |

## 练习

1. block_size = 16 时，一个生成了 100 个 token 的请求占几个物理块？最后一块浪费几个坑位？最坏情况下浪费多少？
2. 两个 beam search 分支共享前 50 个 token、在第 51 个分叉。以 block_size = 16 画出分叉前后两张块表的变化，指出 CoW 在哪一块上被触发、触发时复制了多少数据。
3. ★ 块表查询发生在注意力内核的取数热路径上，为什么实测开销很小？（提示：一次查表换来 16 个 token 的连续读取；再想想 GPU 上"计算掩盖访存延迟"的惯用手法）（第 10 章的 SRAM/HBM 层级图会让这个答案更立体）
4. 9.1 节说 TP=4 时每层两次 AllReduce、80 层共 160 次。若每次同步 (n, 8192) 的 FP16 激活、n=1（Decode 单 token），单步同步的数据总量是多少 MB？这解释了为什么 TP 依赖 NVLink 而不能走 PCIe。

## 延伸阅读

- Kwon et al., *Efficient Memory Management for Large Language Model Serving with PagedAttention* (SOSP 2023)——vLLM 原论文；20~40% 利用率实测与分页方案的完整论证
- Shoeybi et al., *Megatron-LM: Training Multi-Billion Parameter Language Models Using Model Parallelism* (2019)——张量并行的列切/行切定式
- Zheng et al., *SGLang: Efficient Execution of Structured Language Model Programs* (2024)——RadixAttention 前缀树共享
- DeepSeek-AI, *DeepSeek-V2* (2024)——MLA 与 RoPE 解耦设计的一手细节

---

⬅️ 上一站：[第 8 章 · 自回归与 KV Cache](./part3-chapter08-自回归与KVCache.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 10 章 · 吞吐引擎：带宽、批处理与调度](./part4-chapter10-吞吐引擎.md)
