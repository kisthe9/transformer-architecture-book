# 第 5 章 · FFN:装进参数里的世界知识

> **我们在解剖图上的位置**：站点 ④ 内部——一层车间的第二道工序。开完会（注意力），各自回工位查资料。
>
> **本章回答的问题**："黄河入海流是名句"这个知识不在 prompt 的十个字里，模型从哪里取出来的？FFN 占了全模型约八成参数，这些参数到底以什么形式"存"知识？门控（SwiGLU）比朴素的两层网络好在哪？
>
> **前置依赖**：第 3 章·注意力（信息搬运的能力边界）、第 4 章·多头与 GQA（一层中注意力工序的完整形态）。

---

## 5.1 注意力搬不来的东西

先给注意力的能力画一条精确的边界。它做的事是**搬运与混合**：把「河」「入」的信息按预算搬进「海」的向量。搬运有一个不言自明的前提——**货物必须已经在场**。十个向量无论怎样加权组合，产出的都是这十个向量张成的空间里的东西。

现在问关键问题：模型凭什么会想到「流」？

「白日依山尽，黄河入海」这十个字里没有「流」。让「流」胜出的依据是一条**世界知识**："这是王之涣《登鹳雀楼》的前九个半字，第十个字是流"——外加一层更通用的知识："五言绝句第二句要收尾、要押韵"。这些知识不在上下文里，它们来自模型训练时读过的海量文本，**以参数的形式凝固在网络里**。

存放和调取这些参数化知识的部件，就是每层注意力之后紧跟的**前馈网络**（FFN, Feed-Forward Network）。延续会议类比，一层车间的完整节奏是：

| | 注意力（开会） | FFN（回工位查资料） |
|---|---------------|---------------------|
| 信息来源 | 上下文——其他字的向量 | 参数——训练凝固的知识 |
| 交互范围 | token **之间**流动 | 每个 token **独立**处理 |
| 回答的问题 | "在场的人都说了什么？" | "关于这个话题，档案里存了什么？" |

"每个 token 独立"值得加重语气：FFN 对序列的每一行执行**完全相同**的变换，行与行之间零交互。「海」查「海」的资料，「山」查「山」的资料，互不知情。全模型只有注意力这一处发生 token 间的信息流动——这个分工的纯粹性，是理解 Transformer 架构的支点之一。

## 5.2 结构:升维、筛选、降维

### 先看两层的骨架

最初版本的 FFN（Vaswani, 2017）是三明治结构——两个线性层夹一个非线性激活：

$$
\text{FFN}(x) = \sigma(x W_1)\, W_2
$$

灵魂在维度轨迹上：

$$
8192 \ \xrightarrow{\ W_1\ }\ 28672 \ \xrightarrow{\ W_2\ }\ 8192
$$

先升到 3.5 倍宽，过激活函数，再降回原宽。为什么要先"变胖"？两个互相独立又互相印证的理由：

1. **非线性需要施展空间。** 逐元素的激活函数在原始维度里能切出的决策面很有限；先把输入投到高维，简单的逐元素非线性就能表达复杂的特征组合——与 SVM 把数据升维后用线性面切分是同一个思想。
2. **知识需要独立的储位。** 下一节会看到，中间层的 28672 个维度可以各自视为一个可寻址的"知识槽位"。宽度直接决定这一层能存多少条独立的模式-响应对。

### 现代版:SwiGLU 的门控

LLaMA 系用的是升级版 **SwiGLU**，把单入口改成双支路：

$$
\text{FFN}(x) = \Big[\underbrace{\text{SiLU}(x W_{\text{gate}})}_{\text{门控支路}} \odot \underbrace{(x W_{\text{up}})}_{\text{内容支路}}\Big]\, W_{\text{down}}
$$

$$
W_{\text{gate}},\ W_{\text{up}}: (8192, 28672), \qquad W_{\text{down}}: (28672, 8192)
$$

两条支路同时看输入：内容支路 $x W_{\text{up}}$ 负责"取什么货"，门控支路 $\text{SiLU}(x W_{\text{gate}})$ 负责"每个槽位的阀门开多大"，逐元素相乘 $\odot$ 合成"货量 × 阀门开度"。其中 $\text{SiLU}(z) = z \cdot \text{sigmoid}(z)$ 是一个平滑版的开关：正区间近似直通，负区间柔和地压向零而非硬切断。

对比朴素版（ReLU 单支路），差别在于**阀门的决定方式**：ReLU 只看本槽位自己的输出正负，一刀切；SwiGLU 的阀门由一整套独立参数 $W_{\text{gate}}$ 看着完整输入来调节，开合更细腻、可学习空间更大。消融实验（Shazeer, 2020）显示门控变体稳定优于单支路——代价是矩阵从两个变三个，为了参数预算持平，中间宽度从惯例的 4 倍下调；LLaMA-2 70B 取 3.5 倍，即 $d_{ff} = 28672$。

## 5.3 键值存储:知识以什么形式存在

### 换一副眼镜看这三个矩阵

FFN 的公式平平无奇，但可解释性研究（Geva et al., 2021）提供了一副眼镜，戴上之后同一条公式会显出完全不同的结构——**FFN 是一个拥有 28672 个条目的键值存储**（key-value memory）：

- $W_{\text{gate}}$（连同 $W_{\text{up}}$）的每一**列**是一把"钥匙"：它与输入向量做点积，检测输入中是否存在某种特定模式；
- $W_{\text{down}}$ 的每一**行**是对应的"值"：一旦模式被检测到，就把这行内容按检测强度加进输出。

把公式按这副眼镜重写——记第 $j$ 个槽位的激活度为 $a_j(x)$（门控 × 内容在第 $j$ 维的乘积），则：

$$
\text{FFN}(x) = \sum_{j=1}^{28672} a_j(x) \cdot W_{\text{down}}[j]
$$

**输出 = 所有被激活槽位的值向量的加权和。** 检测（钥匙）与响应（值）在训练中被同时雕刻成对。

### 一个可以手算的最小实例

空口说"键值存储"容易过目即忘，用一个 $d = 2$、$d_{ff} = 3$ 的微缩 FFN 手算一遍（为看清结构，激活简化为 ReLU、去掉门控）：

$$
W_1 = \begin{pmatrix} 1 & 0 & -1 \\ 0 & 1 & 1 \end{pmatrix}, \qquad
W_2 = \begin{pmatrix} 2 & 0 \\ 0 & 2 \\ 3 & 3 \end{pmatrix}
$$

$W_1$ 的三列是三把钥匙：钥匙 A 检测"第一维为正"，钥匙 B 检测"第二维为正"，钥匙 C 检测"第二维压过第一维"。$W_2$ 的三行是三个值：$(2,0)$、$(0,2)$、$(3,3)$。

送入 $x = (1, 2)$：

$$
x W_1 = (1,\ 2,\ 1) \ \xrightarrow{\text{ReLU}}\ a = (1,\ 2,\ 1)
$$

三把钥匙全部命中，输出是三个值的加权和：

$$
\text{FFN}(x) = 1 \cdot (2,0) + 2 \cdot (0,2) + 1 \cdot (3,3) = (5,\ 7)
$$

换 $x = (2, -1)$：$x W_1 = (2, -1, -3) \xrightarrow{\text{ReLU}} (2, 0, 0)$——只有钥匙 A 命中，输出 $= 2 \cdot (2,0) = (4, 0)$。**不同的输入唤醒不同的槽位子集，输出由被唤醒者的值拼合而成**——这就是键值存储的全部机制，真实模型只是把 3 个槽位换成 28672 个、把 ReLU 换成带门控的 SwiGLU。

### 回到我们的诗

「海」的向量走到 FFN 门口时，已被注意力注入"黄河 + 入 + 上句尽字收束"的复合语境。它与 28672 把钥匙逐一对表：

```
「海」的语境化向量 vs 本层 28672 把钥匙（示意）:

  某槽位 α「名句续写」模式:    强命中 → 注入值向量 ≈ "流"的语义前兆
  某槽位 β「五言句尾+平声韵」: 中命中 → 注入"收尾字应押 iou 韵"的偏好
  某槽位 γ「水流方向」:        强命中 → 注入"江河东流入海"的意象
  其余两万多把钥匙:            未命中 → 阀门关闭，零贡献

  输出 ≈ a_α·值_α + a_β·值_β + a_γ·值_γ
```

（诚实声明：槽位编号与语义标签是教学示意。真实模型中单个槽位往往是多义混叠的，几个不相干的模式可能挤在同一维——但"钥匙检测 → 值注入"的机制本身被 Geva 等人的实验反复证实，后续的模型编辑工作更是直接利用了它。）

### 这副眼镜顺带解释的三件事

**为什么 FFN 吃掉了八成参数。** 每层三个矩阵共 $3 \times 8192 \times 28672 \approx 7.05$ 亿参数，80 层合计约 564 亿——占 70B 的八成。对照注意力每层约 1.5 亿（第 4 章）。知识库是模型最大的部件，这个比例分配本身就是宣言：**大模型的"大"，大在知识存储**。

**为什么"编辑模型记忆"改的是 FFN。** 既然事实以"钥匙-值"对的形式驻留，定点改写就成为可能：ROME 一类工作先定位存放某条事实的槽位，再改写对应的值向量——手术后模型换答案，其余能力几乎无损。这是键值视角从"解释"走向"操作"的直接证据。

**为什么深浅层有分工。** 逐层探测发现：浅层槽位多检测表层模式（词形、搭配、标点节奏），深层槽位多存语义关联与事实知识。80 层车间是一条从字面到含义的渐进流水线——这也回答了"为什么层数是几十上百而不是三五层"的一半（另一半在第 6 章）。

## 5.4 代码锚点:SwiGLU 与键值分解

```python
import numpy as np

def silu(z): return z / (1.0 + np.exp(-z))

# 演示用缩小 8 倍：真实值 d=8192, d_ff=28672（比例同为 3.5×，仅为省内存）
d, d_ff = 1024, 3584
rng = np.random.default_rng(0)
W_gate = (rng.normal(size=(d, d_ff)) / np.sqrt(d)).astype(np.float32)
W_up   = (rng.normal(size=(d, d_ff)) / np.sqrt(d)).astype(np.float32)
W_down = (rng.normal(size=(d_ff, d)) / np.sqrt(d_ff)).astype(np.float32)

x = rng.normal(size=(d,)).astype(np.float32)      # 单个 token —— FFN 逐 token 独立
a = silu(x @ W_gate) * (x @ W_up)                 # (3584,) 每个槽位的激活度 a_j
y = a @ W_down                                    # (1024,) 输出

# 键值视角的数值验证：输出 == 各槽位激活度加权的 W_down 行向量之和
y_kv = np.einsum('j,jd->d', a, W_down)
print("键值分解成立:", np.allclose(y, y_kv, atol=1e-4))

# 真实尺寸的参数账（纯算术，不占内存）
per_layer = 3 * 8192 * 28672
print(f"单层 FFN 参数: {per_layer/1e6:.0f}M；80 层合计: {per_layer*80/1e9:.1f}B")
```

## 5.5 前沿短节:当知识库大到装不下(MoE)

FFN = 知识库这个视角，还能解释 2024 年以来最重要的架构潮流之一。

知识库越大模型越博学，但稠密 FFN 有个费钱的特性：**每个 token 都要过全部 28672 个槽位**——哪怕 99% 的槽位对这个 token 阀门全关，乘法一个不少。想把知识库再扩大十倍？算力账单同步扩大十倍。

**混合专家**（MoE, Mixture of Experts）的破局思路：把一个巨型 FFN 拆成多个中等大小的"专家"FFN，前面加一个轻量的**路由器**，每个 token 只被派发给最相关的少数几个专家。Mixtral 8×7B 是教科书案例：每层 8 个专家，每个 token 只激活其中 2 个——参数总量近 47B，每次前向真正参与计算的只有约 13B。DeepSeek-V3 把这条路走到 671B 总参数、每 token 只激活 37B。

一句话总结 MoE 的交易：**用"参数总量"换"知识容量"，同时让"激活参数量"决定算力成本**——藏书可以无限扩馆，每次借阅只跑两个书架。代价也直白：全部专家都得住在显存里（容量问题没消失，只是从算力转嫁给了显存），且路由器的负载均衡是出了名的训练难点。本书主线是稠密模型，MoE 到此为止；想深入的读者见延伸阅读。

## 5.6 一层车间的两道工序到齐了

「白日依山尽，黄河入海」的十个向量，至此在一层之内完成了完整的一开一合：注意力让它们互通有无（「海」知道了自己身处水系动线的末端），FFN 给每个字独立注入参数深处的知识（「海」的向量里泛起了「流」的前兆——名句、韵脚、水流意象三路汇合）。

先别急着庆祝。刚才的叙述悄悄回避了一个工程死穴：这样的"注意力 + FFN"要**串行堆叠 80 次**。每一层都对向量做一次彻底改写，信号连过 80 道改写还能保真吗？训练时梯度反向穿越 80 层还剩多少？2015 年之前，整个深度学习界就是被这个问题卡在十几层上不去的。下一章交代让 80 层成为可能的两根支柱——它们加起来不到五行公式，却是整栋大楼的地基。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 能力分工 | 注意力搬运在场信息（token 间），FFN 注入不在场知识（token 独立） |
| 结构 | $8192 \to 28672 \to 8192$：升维给非线性和知识槽位腾空间 |
| SwiGLU | 内容支路 × 门控支路；阀门由独立参数看全输入调节，优于 ReLU 一刀切 |
| 键值存储 | 钥匙（$W_{\text{gate}}$ 列）检测模式，值（$W_{\text{down}}$ 行）注入内容；$\text{FFN}(x)=\sum_j a_j W_{\text{down}}[j]$ |
| 参数版图 | 单层 ~7 亿、80 层 ~564 亿，占 70B 八成——模型的"大"大在知识库 |
| MoE | 拆库 + 路由：参数容量与算力解耦；Mixtral 8×7B 激活 2/8，DeepSeek-V3 激活 37B/671B |

## 练习

1. 精确计算单层 SwiGLU FFN 的参数量，验证 5.3 节"约 7.05 亿"；再算 80 层总量占 70B 的百分比。（对账见附录 A）
2. 画出 $\text{SiLU}(z) = z \cdot \text{sigmoid}(z)$ 与 ReLU 在 $z \in [-4, 4]$ 的曲线。SiLU 在 $z < 0$ 区域不完全归零意味着什么？（提示：从"阀门能不能透一点气"和"梯度能不能通过"两个角度想）
3. 用 5.3 节的微缩 FFN（$W_1$、$W_2$ 照抄）计算输入 $x = (-1, 1)$ 的输出，指出哪些钥匙命中、输出由哪些值合成。
4. ★ 把某一层 FFN 的 $W_{\text{down}}$ 整体置零，这一层还剩下什么功能？模型会当场崩溃吗？先写下你的推断——第 6 章·残差与归一化的"优雅降级"一节会给出一个可能让你意外的答案。

## 延伸阅读

- Geva et al., *Transformer Feed-Forward Layers Are Key-Value Memories* (EMNLP 2021)——键值视角的实证源头，5.3 节那副"眼镜"的出处
- Shazeer, *GLU Variants Improve Transformer* (2020)——SwiGLU 的提出与门控变体消融
- Meng et al., *Locating and Editing Factual Associations in GPT* (NeurIPS 2022)——ROME：定位并改写 FFN 中的单条事实
- Jiang et al., *Mixtral of Experts* (2024)——8 专家 top-2 路由的开源标杆；DeepSeek-AI, *DeepSeek-V3 Technical Report* (2024)——MoE 规模化的当前上限

---

⬅️ 上一站：[第 4 章 · 多头与 GQA](./part2-chapter04-多头与GQA.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 6 章 · 残差与归一化：深度的两根支柱](./part2-chapter06-残差与归一化.md)
