# 第 4 章 · 多头与 GQA:64 个问题,8 套答案

> **我们在解剖图上的位置**：站点 ④ 内部——注意力工序的完整体。第 3 章讲的是"一个头"的原理，本章把它放大到真实模型的规模。
>
> **本章回答的问题**：一个注意力头明明能工作，为什么要 64 个？64 个 Q 头为什么只配 8 组 K/V——这刀砍在哪里才既省下 87.5% 的缓存显存、又几乎不伤质量？
>
> **前置依赖**：第 3 章·注意力（单头机制的完整流程、Q/K/V 三重身份）。

---

## 4.1 一份预算的天花板

第 3 章结尾的不满足，现在具体化。回看「海」的注意力预算：88% 投给了「黄」「河」「入」——这是一种**语义视角**的分配："找出与我构成事件的水系词"。但同一时刻，「海」还有别的问题值得问，而每个问题需要的预算分配完全不同：

- **句法视角**：我是"入"的宾语吗？（预算应集中在「入」）
- **韵律视角**：我是五言下句的第四字，句子还差一个字收尾？（预算应关注「，」和句内位置）
- **篇章视角**：上句"白日依山尽"的意象与我有什么呼应？（预算应铺向前五个字）

问题在于：一套 $W_Q, W_K, W_V$ 只能定义**一种**"什么算相关"，一次 softmax 只能给出**一份**预算。想同时问多个问题，单头在结构上就做不到——softmax 归一化决定了预算总额恒为 100%，押注了水系语义，就顾不上句法结构。

解决思路不需要天才：**多配几套投影，并行各问各的**。真正值得琢磨的是接下来的两个数字——为什么是 64 套，以及为什么后来只保留了 8 套答案。

## 4.2 多头:把 8192 维切成 64 个视角

### 切分,而不是复制

多头注意力（MHA, Multi-Head Attention）容易被误解成"把注意力模块复制 64 份"。真实设计精巧得多：**总维度不变，切开分工**。

$$
d_{\text{model}} = h \times d_k \quad\Longrightarrow\quad 8192 = 64 \times 128
$$

投影矩阵 $W_Q: (8192, 8192)$ 的输出被重新解读为 64 段、每段 128 维——第 $i$ 段就是第 $i$ 个头的 $q$ 向量。每个头在自己的 128 维子空间里，独立走完第 3 章的全部五步（点积 → 缩放 → 掩码 → softmax → 加权求和），互不干扰：

```
X: (10, 8192)
  │  乘 W_Q，输出按 64 段重新解读（reshape）
  ▼
Q: (10, 64, 128)
                   64 个头并行，各自独立跑一遍第 3 章:
                     头 0:  在子空间 0 分一份预算   → (10, 128)
                     头 1:  在子空间 1 分另一份预算 → (10, 128)
                       ⋮
                     头 63: 在子空间 63 分第 64 份  → (10, 128)
  │  64 份输出按位拼接
  ▼
concat: (10, 64 × 128) = (10, 8192)
  │  乘 W_O: (8192, 8192)   ← 跨头融合
  ▼
输出: (10, 8192)
```

尾部那个 $W_O$ 不是仪式性的：64 个头各自为战之后，每个头只见过自己那 128 维里的世界。$W_O$ 把 64 份"局部见解"线性混合成统一表示，让句法头发现的结构能修正语义头收集的内容。拿掉它，多头就退化成 64 份互相隔离的报告的机械装订。

### 128 维的头,会不会太瘦?

有必要直面一个疑虑：一个 128 维的头，表达力显然不如一个独占 8192 维的巨头，这算不算降级？

单看一个头，确实是。128 维大约只够编码一种相对单纯的关联模式。但这恰是设计意图——**用 64 次独立的机会换取多样性，而不是把所有筹码压在一个视角上**。训练结束后检查真实模型，会发现头与头自发分化出了可辨认的分工：有的头几乎只看紧邻的前一个词，有的头专职追踪引号和括号的配对，有的头负责把代词连向先行词（可解释性研究已经给这些头起了名字，见延伸阅读的 Elhage et al.）。64 种单纯模式的组合，覆盖面远超一个"全能"巨头。

$d_k = 128$ 这个数字本身也值得记一笔：从 GPT-3 到 LLaMA 再到更新的开源模型，每头维度几乎钉死在 128 附近——它是"单头够用"与"头数够多"之间多年实践沉淀的平衡点。

## 4.3 账单:MHA 在推理时的隐性成本

训练视角下 MHA 无可挑剔，账单要到推理时才寄到。

这里需要预支一个第 8 章才正式展开的事实（此处只需接受结论）：**推理逐字生成时，历史 token 的 $k$、$v$ 会被缓存下来反复使用**——第 3 章证明过它们永不改变，缓存是合法且必要的。这份缓存叫 KV Cache，它住在显存里。

MHA 下这份缓存有多大？每个 token 在**每一层**要存 64 个头的 $k$ 和 $v$：

$$
\underbrace{2}_{k \text{ 和 } v} \times \underbrace{64}_{\text{头数}} \times \underbrace{128}_{d_k} \times \underbrace{2\ \text{B}}_{\text{FP16}} = 32\ \text{KB / token / 层}
$$

乘上 80 层、一个 4096 token 的上下文：

$$
32\ \text{KB} \times 80 \times 4096 \approx 10.7\ \text{GB} \quad (\text{单个请求！})
$$

一个请求 10.7 GB。一张 80 GB 的 A100，扣除权重分片后能同时伺候的请求数是个位数；50 个并发就需要 500 多 GB——**比 70B 模型权重本身（140 GB）还大三倍多**。请记下这个反直觉的结论：推理显存的第一大户往往不是那 700 亿参数，而是 KV Cache。这笔账单逼着架构师回头审视：64 份 K/V，真的每份都必要吗？

## 4.4 关键洞察:多样性住在 Q 里,不住在 K/V 里

回到会议类比（第 3 章）。64 个头相当于 64 位与会者，每人带着不同的疑问（$q$）。疑问必须各不相同——这正是多头的全部价值。但换个角度想：**会场需要为每位提问者准备一套独立的桌牌和发言记录吗？**

不需要。一套桌牌 + 一套发言记录，配上 64 种不同的疑问，照样产出 64 种不同的收获。数学上看得更干脆——同一组 $K, V$ 配不同的 $Q_i$：

$$
O_i = \text{softmax}\!\left(\frac{Q_i K^\top}{\sqrt{d_k}}\right) V
$$

只要 $Q_i$ 不同，注意力分布就不同，加权出的 $O_i$ 就不同。**输出的多样性由 Q 驱动；K 和 V 只是被检索的对象。** 检索对象被 64 个人共用，不损失任何一个人的提问自由。

再从信息容量的角度补一刀。$K$ 回答的是"这个字能从哪些角度被匹配到"，$V$ 回答的是"它能供给什么内容"。一个字可被匹配的角度——句法角色、语义类别、位置特征、指代关系、主题归属——是有限的几类；8 组 × 128 维 = 1024 维的空间足以张开这些主要方向。64 份 K/V 里，后 56 份提供的边际信息趋近于零，纯属为对称性付费。

## 4.5 GQA:分组共享的落地

### 从两个极端到中间解

砍 K/V 头数的想法有一条演化线：

| 方案 | Q 头 | KV 组 | 每 token 缓存 | 质量（GQA 论文消融） |
|------|------|-------|--------------|---------------------|
| **MHA** | 64 | 64 | 1×（基准 32 KB/层） | 基准 |
| **MQA** | 64 | 1 | 1/64 | 可感知的下降，且训练欠稳 |
| **GQA-8** | 64 | 8 | **1/8**（4 KB/层） | 与 MHA 几乎持平 |

MQA（Shazeer, 2019）是激进的先行者：所有 Q 头共享唯一一组 K/V，缓存省到极限，但基准测试掉分肉眼可见。GQA（Ainslie et al., 2023）取中间值：**把 64 个 Q 头分成 8 组，组内 8 个头共享一组 K/V**：

```
Q 头:   q0 … q7 │ q8 … q15 │ … │ q56 … q63
              ▼          ▼              ▼
K/V 组:    (k0,v0)    (k1,v1)   …    (k7,v7)        ← 缓存里只有这 8 组
             共享        共享            共享
```

GQA 论文的消融曲线给出了经验答案：KV 组数从 64 砍到 8，平均基准分几乎不动；从 8 再往 1 砍，下降开始显现且模型越大越明显。**8 是"质量无损"与"缓存大省"的拐点**——这与 4.4 节的容量直觉互相印证。此后 LLaMA-2/3 70B、Qwen 系列、Mistral 不约而同选择了 $g = 8$，GQA-8 成为一代事实标准。（一个工程上的顺水人情：8 恰好对上多卡张量并行的常见切分度，每张卡分得整数组 K/V——第 9 章展开。）

### 账目变化

投影矩阵随之瘦身。$W_K$、$W_V$ 从 $(8192, 8192)$ 缩到 $(8192, 1024)$（$1024 = 8 \times 128$）：

| 矩阵 | MHA 形状 | GQA-8 形状 | 每层参数 |
|------|----------|-----------|---------|
| $W_Q$ | (8192, 8192) | 不变 | 67.1 M |
| $W_K$ | (8192, 8192) | **(8192, 1024)** | 67.1 M → 8.4 M |
| $W_V$ | (8192, 8192) | **(8192, 1024)** | 67.1 M → 8.4 M |
| $W_O$ | (8192, 8192) | 不变 | 67.1 M |

每层省约 1.17 亿参数（80 层共省 ~94 亿），但这只是小头；大头是 KV Cache 直接除以 8——单请求 4096 上下文从 10.7 GB 降到 **1.34 GB**。同一张卡的并发容量翻了八倍的量级，这是 GQA 真正的商业价值。

### 实现:一次都不用复制

概念图里"8 组 K/V 广播给 64 个 Q 头"听起来要做 8 → 64 的复制，实现上一个字节都不复制：注意力内核只需让同组的 8 个 Q 头**读同一块 K/V 显存地址**。Hugging Face 源码里那个 `repeat_kv` 函数做的也只是逻辑上的形状扩展（expand），物理内存零拷贝。省下的是真金白银，开销是零——架构优化里罕见的无对价交易。

### 代码锚点:GQA 的分组检索

```python
import numpy as np

n, d, h, g, d_k = 10, 8192, 64, 8, 128
rng = np.random.default_rng(0)
X = rng.normal(size=(n, d)).astype(np.float32)
W_Q = (rng.normal(size=(d, h * d_k)) / np.sqrt(d)).astype(np.float32)
W_K = (rng.normal(size=(d, g * d_k)) / np.sqrt(d)).astype(np.float32)   # 只有 8 组
W_V = (rng.normal(size=(d, g * d_k)) / np.sqrt(d)).astype(np.float32)

Q = (X @ W_Q).reshape(n, h, d_k)          # (10, 64, 128)
K = (X @ W_K).reshape(n, g, d_k)          # (10, 8, 128)  ← 推理时缓存的就是它
V = (X @ W_V).reshape(n, g, d_k)

def softmax(s):
    e = np.exp(s - s.max(-1, keepdims=True)); return e / e.sum(-1, keepdims=True)

M = np.triu(np.full((n, n), -np.inf), 1)
O = np.empty((n, h, d_k), dtype=np.float32)
for i in range(h):
    kv = i // (h // g)                    # 第 i 个 Q 头 → 第 i//8 组 K/V，纯索引零拷贝
    S = Q[:, i] @ K[:, kv].T / np.sqrt(d_k)
    O[:, i] = softmax(S + M) @ V[:, kv]

print(O.reshape(n, -1).shape)             # (10, 8192)，接下来乘 W_O 融合（此处从略）
print(K.nbytes // n, "B/token —— K 的缓存量（FP32 演示值，FP16 减半）")
```

## 4.6 更进一步:KV 压缩的下一站(MLA)

GQA 把 64 份答案合并成 8 份，自然的追问是：8 份还能再压吗？2024 年 DeepSeek 给出的答案是**多头潜在注意力**（MLA, Multi-head Latent Attention）：不再缓存 K 和 V 本身，而是训练时就把它们**联合压缩成一个低秩潜向量**——缓存里只存这个瘦小的潜向量，用时经一个小投影"解压"回各头的 K/V。

效果：KV 缓存可压到 MHA 的十分之一以下（相关工作报告 90% 以上的压缩率），而 DeepSeek-V2/V3 的实践表明质量不降反稳。代价是每步多一次解压投影，以及与 RoPE 的兼容需要专门的解耦设计（位置相关的部分单独留一小段不压缩）。

把三代方案排成一条线，你会看到同一个问题被越问越深：

```
MHA:    每个头一套 K/V        —— "谁都别共享"
GQA:    每 8 个头一套 K/V     —— "答案可以共享"        ← 本书基准
MLA:    所有头共享一个潜向量   —— "答案还可以压缩存放"
```

驱动这条演化线的压力始终只有一个：4.3 节那张 KV Cache 账单。它还会在第 8、9 章两次回来敲门。

## 4.7 我们的诗此刻的状态

「白日依山尽，黄河入海」在真实模型的每一层里，实际发生的是：64 个头分成 8 组，带着 64 种问题检索 8 套共享的桌牌与发言；64 份 128 维的收获拼回 8192 维，经 $W_O$ 融合。「海」的向量此刻已经是多视角的复合体——语义上是黄河的归宿，句法上是"入"的宾语，韵律上是等待收尾的第十字。

但要预测出「流」，光靠这十个字内部的信息仍然不够。"白日依山尽，黄河入海流"是千年名句，**这个知识不在 prompt 的任何一个字里**——它躺在模型的参数深处。注意力是搬运工，搬的是在场的信息；下一章打开存放不在场知识的仓库：占全模型八成参数的 FFN。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 单头天花板 | 一套投影只能定义一种"相关"，一次 softmax 只有一份预算 |
| 多头 = 切分 | $8192 = 64 \times 128$，各头在子空间独立跑注意力，$W_O$ 跨头融合 |
| 多样性哲学 | 64 个单纯视角的组合胜过一个全能巨头；$d_k=128$ 是多年沉淀的平衡点 |
| MHA 账单 | 缓存 32 KB/token/层 → 单请求 10.7 GB：显存大户是 KV Cache 而非权重 |
| 核心洞察 | 输出多样性由 Q 驱动，K/V 是共享的被检索对象 |
| GQA-8 | 8 个 Q 头共享一组 K/V：缓存 ÷8 至 1.34 GB/请求，质量与 MHA 几乎持平 |
| 演化线 | MHA → GQA → MLA：KV 从"不共享"到"共享"再到"压缩存放" |

## 练习

1. 计算 GQA-8 下单层注意力四个投影矩阵的总参数量，并与 MHA 对比：省了百分之多少？（对账见附录 A）
2. 若本书基准模型改用 $g = 16$，单请求（4096 token、FP16）的 KV Cache 是多少 GB？改用 MQA（$g=1$）呢？
3. 4.5 节说 GQA 的实现"零拷贝"。设想反面：如果真的把 8 组 K/V 物理复制成 64 份再计算，显存占用和带宽消耗各增加几倍？（这道题的答案解释了为什么 `repeat_kv` 必须是逻辑扩展）
4. ★ MLA 把 K/V 压成潜向量缓存，代价之一是"与 RoPE 的兼容需要解耦设计"。结合第 2 章——RoPE 旋转的是每个头的 $k$——想一想：为什么"先压缩再解压"的 K 无法直接套用按位置旋转？（第 9 章讨论 KV 压缩全景时回收此题）

## 延伸阅读

- Shazeer, *Fast Transformer Decoding: One Write-Head is All You Need* (2019)——MQA 起点，KV 共享思想的源头
- Ainslie et al., *GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints* (EMNLP 2023)——GQA 原论文；消融实验含"从 MHA 检查点低成本改造"的实用配方
- DeepSeek-AI, *DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model* (2024)——MLA 的提出与大规模验证
- Elhage et al., *A Mathematical Framework for Transformer Circuits* (2021)——"头的分工"从传说到证据

---

⬅️ 上一站：[第 3 章 · 注意力](./part2-chapter03-注意力.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 5 章 · FFN：装进参数里的世界知识](./part2-chapter05-FFN.md)
