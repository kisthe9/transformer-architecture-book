# V1 诊断报告

> 诊断对象：`book/` 全部 12 个文件（README + 10 章 + 附录，约 1650 行）。
> 诊断依据：`book-v2/QUALITY-STANDARD.md` 的 20 条标准。
> 结论用于指导 V2 的独立创作——继承结构智慧，解决遗留问题。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📖 V1 诊断分析
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 👍 V1 做得好的地方（V2 应继承其"设计思想"，但重新表达）

1. **单一 running example 贯穿全书**。一句诗 + 一组真实模型尺寸（LLaMA-2 70B）从第 1 章用到第 10 章，读者永远不需要切换心智上下文。这是 V1 最大的结构资产，V2 必须继承这一设计原则（但换用自己的例子与叙述）。
2. **"动词开头的章名 + 本章回答的问题"的章首设计**。每章以 2~3 个具体问题开篇，读者带着问题进入正文，教学效率高。V2 继承此机制。
3. **伏笔—回收网络**。因果掩码（ch3）→ KV Cache 合法性（ch8）→ 显存战争（ch9）→ 大 batch（ch10）这条因果链是全书最精彩的骨架；练习题中的 ★ 题作为下一章伏笔也是巧思。V2 保留并强化该机制。
4. **量纲驱动的讲解**。所有讲解落到具体形状（(10, 8192)、(8192, 28672)…）和具体账单（10.7 GB、70 ms…），拒绝悬空抽象。V2 继承。
5. **诚实声明简化**。中文分词粒度的简化在使用处显式声明，不欺骗读者。V2 继承此规范并推广到所有简化处。
6. **章末小结表 + 延伸阅读给原始论文**。支持快速回顾与溯源。V2 继承。

## 👎 V1 的核心问题（V2 必须解决）

1. **全书零代码**（违反 E1，🔴）。十章讲完注意力、RoPE、采样、KV Cache，没有一行可运行代码。目标读者是工程师，"读得懂但写不出"是这类书最常见的失败模式。
   → **V2 方案**：核心机制章节各配一段 ≤30 行、纯 NumPy、变量名与公式符号对齐的代码锚点；全书代码可串成一个 ~200 行的玩具推理器。
2. **练习题无任何答案或提示**（违反 P2，🔴）。V1 每章 2~4 道练习设计得好，但全书没有答案附录——练习变成了摆设，读者无法自我验证。
   → **V2 方案**：新增附录 B《练习提示与答案》，验证型题给完整解，开放型题给提示与判断要点。
3. **缺少全景开篇**（违反 S2/P1，🟡）。V1 的读者从第 1 章直接跳进分词细节，全书地图只在 README 里以一张 ASCII 图存在。读者在前三章缺少"我为什么在学这个"的全局参照。
   → **V2 方案**：新增第 0 章"鸟瞰"——用一次完整 API 调用把全链路走马观花过一遍（每站一段话），后续各章逐站放大。
4. **Part 级结构有名无实**（违反 S2，🟡）。V1 的四个部分只是目录里的四个标题，没有 part opener 说明各部分回答什么层次的问题。
   → **V2 方案**：每个 Part 有独立导言段（并入该 Part 首章开头或独立短文件），明确本部分的问题层次与前后衔接。
5. **类比未声明边界**（违反 P3，🟡）。图书馆检索类比（Q/K/V）在"V 不是静态存储而是投影结果"这一点上会误导；"高速公路装货"类比在"残差流会被 norm 缩放"处失效——V1 均未声明类比边界。
   → **V2 方案**：每个核心类比引入时说明成立点，并在失效处显式画界。
6. **工程链路有遗漏**（违反 C1，🟡）。张量并行只在 ch9 以"TP=4"一笔带过，但 70B 单卡放不下是读者必然遇到的第一个现实问题；采样一章缺 repetition penalty 等真实 API 参数；引擎一章未提 speculative decoding 正文（只在延伸阅读）。
   → **V2 方案**：Part IV 补一节"把 70B 塞进多张卡"（张量并行的最小必要知识）；采样章覆盖读者在 API 文档里能看到的全部主流参数；投机解码作为吞吐章的收尾展望正式入正文。
7. **RoPE 章的位置造成叙事扭曲**（🟢）。V1 把 RoPE 放在注意力之前（ch2），但 RoPE 实际作用于每层注意力内部的 Q/K——V1 只能靠"逻辑上可以理解为"的补丁话术弥合。这不是错误，但叙事上是一处硬伤。
   → **V2 方案（检查点 A 用户决策后更新）**：保持 V1 的章序（位置编码在前，作为 ch2），但用更干净的方式化解前向引用：ch2 章首用三句话 + 一个二维数值例建立"注意力用 Q·K 点积衡量相关性"这一最小前提（并明确标注完整推导在 ch3），从而把 V1 的含糊话术替换为显式、可验证的最小铺垫。
8. **信息密度天花板偏低**（🟢）。V1 单章仅 120~200 行（约 2500~4000 字），部分主题浅尝辄止：BPE 只给了一句话思想没有演示合并过程；softmax 饱和只有结论没有数值示例；FFN 键值视角没有展示最小数值例。
   → **V2 方案**：单章目标 4000~8000 字，每个"只有结论"的地方补最小可验证示例。

## 🔀 V2 的结构决策

**1. 是否沿用 V1 的章节结构？——骨架沿用，三处调整（检查点 A 后修订）。**
- 沿用"四部分 + 数据流顺序"的骨架：它与推理链路同构，是正确的组织方式，没有理由推翻。
- 调整一：**新增第 0 章（全景鸟瞰）**，解决"只见树木"问题（诊断 #3）。
- 调整二：**位置编码保持 V1 顺序（ch2，先于注意力）**——检查点 A 用户决策；诊断 #7 改用"最小点积前提前置"方案化解（见上）。
- 调整三：**新增附录 B（练习提示与答案）**（诊断 #2）与**附录 C（多模态架构一瞥）**（检查点 A 用户要求）。

**2. 叙事主线是否改变？——升级：从"一个字的旅程"到"一次 API 调用的解剖"。**
- V1 主线是"prompt 如何续写出下一个字"，覆盖 ch1–8 很顺，但 ch9–10（显存、调度、引擎）与"字的旅程"关系松散，读起来像附赠的工程手册。
- V2 主线改为：**解剖一次真实的 API 调用**——从 `curl` 发出请求，到 SSE 流式吐字结束。字的旅程（算法）是这次调用的内核，显存/调度/引擎（工程）是这次调用的外壳——两者被同一条主线自然统摄，Part IV 不再是"附录感"章节。
- 新 running example：`「白日依山尽，黄河入海」→「流」`（同为 10 字五言诗场景，与 V1 例子区分，保持"一字一 token"教学简化并声明）。

**3. 读者定位是否与 V1 一致？——一致，表述更可操作。**
- V1 的定位（1~3 年经验工程师、会矩阵乘法、调过 API）是准确的，V2 沿用。
- V2 额外给出"自测三题"（读者用 30 秒判断本书是否适合自己），把画像从描述变成可自查的门槛。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## V1 问题 → V2 改进对照表（收官时逐项核销）

| # | V1 问题 | 严重度 | V2 改进承诺 | 落点 |
|---|---------|--------|------------|------|
| 1 | 零代码 | 🔴 | 每个核心机制配 NumPy 代码锚点 | ch2/3/4/5/7/8 |
| 2 | 练习无答案 | 🔴 | 附录 B 练习提示与答案 | appendix-b |
| 3 | 缺全景开篇 | 🟡 | 第 0 章鸟瞰全链路 | ch0 |
| 4 | Part 有名无实 | 🟡 | 每部分有导言 | 各 Part 首章 |
| 5 | 类比无边界 | 🟡 | 类比引入即声明成立点与失效点 | 全书 |
| 6 | 工程链路遗漏 | 🟡 | 补张量并行、完整采样参数、投机解码 | ch7/9/10 |
| 7 | RoPE 前向引用 | 🟢 | 保持 V1 章序（用户决策），改用最小点积前提前置方案消除含糊话术 | ch2 章首三句话+二维数值例 |
| 8 | 密度天花板低 | 🟢 | 单章 4000~8000 字，结论处补最小示例 | 全书 |
