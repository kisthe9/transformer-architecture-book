# 第 10 章 · 吞吐引擎:带宽、批处理与调度

> **我们在解剖图上的位置**：站点 ①（接入调度）+ 站点 ⑦ 后半场 + 站点 ⑧（流式返回）——外壳的全部剩余部分，也是全书的收官。
>
> **本章回答的问题**：GPU 标称每秒 312 万亿次运算，为什么单请求的 Decode 连它的 1% 都用不到？vLLM 凭什么比自己手写的推理脚本快一个数量级？"一步一字"的铁律真的不可打破吗？
>
> **前置依赖**：第 8 章·自回归与 KV Cache（Decode 是带宽瓶颈）、第 9 章·显存战争（块表与省下的显存）。

---

## 10.1 开场白:先把第 8 章的 ★ 题算完

第 8 章练习 4 的答案值得工整地写在黑板上，因为它是本章一切结论的地基。

Decode 每生成一个字，GPU 必须把**全部权重**（140 GB）从显存（HBM）读进计算单元，外加读一遍 KV Cache。以 A100 的 HBM 带宽 2 TB/s 计：

$$
\text{单步时间下界} \geq \frac{140\ \text{GB}}{2\ \text{TB/s}} = 70\ \text{ms}
\quad\Longrightarrow\quad
\text{单请求速度上限} \approx 14\ \text{token/s}
$$

请注意这个推导里**根本没出现算力**。A100 的 FP16 算力高达 312 TFLOPS，而生成一个 token 的前向计算量约 $2 \times 70\text{B} = 140$ GFLOPs——满算力只需 **0.45 ms** 就能算完。对比一下：**搬数据 70 ms，算数据 0.45 ms**。Decode 阶段的 GPU，99% 以上的时间里算术单元在围观搬运工干活。

衡量这类失衡的标准工具叫**算术强度**（Arithmetic Intensity，出自 Roofline 模型）：每从显存搬 1 字节，能做几次浮点运算：

$$
\text{AI} = \frac{\text{FLOPs}}{\text{Bytes}}, \qquad
\text{A100 的收支平衡点} = \frac{312\ \text{TFLOPS}}{2\ \text{TB/s}} = 156
$$

AI < 156：带宽瓶颈（memory-bound），加算力无用；AI > 156：算力瓶颈（compute-bound），带宽有富余。单请求 Decode 每搬 2 字节（一个 FP16 权重）只做约 2 次运算——**AI ≈ 1**，离平衡点差两个数量级。这一个数字就是全部病情，接下来的三味药全部对着它开。

## 10.2 药方一:拼车——Continuous Batching

### 一次搬运,服务多人

AI ≈ 1 的根源：搬一遍 140 GB 权重，只服务了 1 个 token 的计算。但权重对所有请求是**同一份**——如果这一步同时处理 64 个请求的 Decode 呢？权重还是搬一遍，计算却翻 64 倍，AI 直接乘 64：

$$
\text{batch} = 64:\ \text{AI} \approx 64 \qquad \text{batch} = 256:\ \text{AI} \approx 256 > 156\ ✓
$$

batch 冲过平衡点，Decode 从带宽瓶颈翻转为算力瓶颈——GPU 终于吃饱。**"拼大 batch"是推理吞吐的第一性原理。** 现在可以把第 9 章结尾的接力棒接稳了：显存战争拼命省下的每一 GB，都是为了让同一张卡装下更多请求的 KV Cache——**省显存的终极目的是凑大 batch**。两章是同一场战役的上下半场。

### 从"锁死的批"到"每步重组"

朴素批处理把若干请求捆成一批同进同出——短请求早写完了也得干等最长的那个，座位大量空转。现代引擎改用 **Continuous Batching**（思想源自 Orca 的 iteration-level scheduling）：调度粒度细化到**每一个生成步**——每产出一轮 token 就重组一次批次，完成的请求立即下车、排队的请求立即补位，GPU 的车永远满员：

```
step t:   [请求A(decode) 请求B(decode) 请求C(prefill块) 请求D(decode)]
step t+1: [请求A(decode) 请求E(prefill块) 请求C(decode) 请求D(decode)]
                              ↑ B 写完下车，E 无缝上车
```

一个必须回答的疑问：几十个请求的 token 混在同一批里过同一套权重，怎么保证互不串数据？答案第 9 章已经造好——**每个请求的注意力只沿自己的块表取自己的 KV 块**。分页机制一石二鸟：既解决碎片，又天然提供了多租户隔离。

顺带补一个现代细节：Prefill 与 Decode 混批时，一个几千 token 的长 prompt 会把整批拖慢（其他请求的字间间隔突然抖动）。主流做法是 **Chunked Prefill**——把长 prompt 切成小块分几步消化，与其他请求的 Decode 混跑，TTFT 略增，换来全场 TPOT 平稳。

## 10.3 药方二:少搬——把中间结果留在片上

GPU 内部还有一层比 HBM 快一个数量级的存储：每个计算单元自带的 **SRAM**（A100 约 19 TB/s，但每单元只有约 192 KB）。第二味药的统一思想：**能在 SRAM 里干完的活，绝不把中间结果写回 HBM**。

```
                容量           带宽
  SRAM       ~20 MB 总量    ~19 TB/s     ← 快但小：中间结果的理想住处
  HBM         80 GB          2 TB/s      ← 大但慢：每次往返都是 70ms 账单的一部分
```

三个代表性成员：

- **FlashAttention**：标准注意力要把 $(n, n)$ 分数矩阵完整写进 HBM、再读回来做 softmax——长序列下这是 GB 级的往返。FlashAttention 把 Q/K/V 切成小瓦片，在 SRAM 里边算边做在线 softmax，那个 $(n, n)$ 矩阵**从头到尾没有完整存在过**。HBM 流量降一个数量级，且结果与标准算法**数学上精确相同**（不是近似）。
- **算子融合**：RMSNorm、投影、RoPE 原本是各自独立的内核，每个都要"读 HBM → 算 → 写 HBM"走一遭。融合成一个内核后，中间结果全程留在片上，HBM 往返从多次压到一次。
- **量化**：权重与 KV 用 FP8/INT4 存放（第 9 章已见其省容量的一面），在带宽视角下还有第二重收益——同样的字节数装下双倍的参数，**带宽瓶颈下等价于带宽翻倍**。这就是 GPTQ/AWQ/GGUF 这些权重量化方案在推理侧真正的价值主张：省的不只是显存，更是每一步的搬运时间。

三味药合治一个病：**做小分母（Bytes），抬高 AI**。

## 10.4 药方三:分工——控制平面与数据平面

最后一块拼图：谁在指挥这一切？推理引擎（vLLM、SGLang、TensorRT-LLM……）的内部是一次经典的**控制平面 / 数据平面**分离：

| | 控制平面（CPU） | 数据平面（GPU） |
|---|----------------|----------------|
| 职责 | 理解"请求"：排队、组批、管块表、判停、流式推送 | 只认张量：矩阵乘、注意力、采样 |
| 擅长 | 分支逻辑、状态机、I/O | 大规模并行算术 |
| 每步耗时 | 应 < 1 ms | 15~70 ms |

GPU 不知道“用户”“对话”“max_tokens”为何物——它收到的只是拼好的 token 批、位置数组和块表；一切业务语义在 CPU 侧翻译完毕。引擎主循环每步三拍：

```
while 还有未完成的请求:
    ① 调度（CPU）:  重组批次；显存吃紧时抢占低优先级请求（回收其块）
    ② 执行（GPU）:  一次前向 = 批内每个请求各产出 1 个 token
    ③ 后处理（CPU）: 反分词 → 停止判定 → 统计 → SSE 推送 —— 站点 ⑧
```

第③拍值得展开，因为它是“模型产出”变成“用户看到的文字”的最后一公里：

```
GPU 产出的 token id (e.g. 5872)
    │
    ▼  反分词 (detokenize)
    │  查词表: 5872 → UTF-8 字节序列
    │  若字节不完整（BPE 拆字场景）→ 缓冲，等下一个 token 凑齐
    │  过滤特殊 token（BOS/EOS/角色标记不输出）
    ▼
    停止判定: 是 EOS? 达到 max_tokens? stop 字符串后缀匹配?
    │
    ▼  统计: prompt_tokens / completion_tokens / 累计延迟
    │
    ▼  封装 SSE 帧并推送:
    data: {"choices":[{"delta":{"content":"流"},"finish_reason":null}]}
```

反分词中的 UTF-8 缓冲解释了一个常见的流式输出现象：偶尔会看到“半个乱码字一闪而过”——那是因为一个汉字被 BPE 拆成了两三个字节 token，第一个 token 只贡献了 UTF-8 的前两字节，必须等下一个 token 补齐才能组装出完整字符。引擎通常先推送一个占位符，凑齐后替换为正确字符。

### 请求的一生:状态机

从用户视角看，一个请求从提交到结束会经历以下状态：

```
waiting ─────► running ─────► finished
    │              │
    │              ├─► preempted（显存不足时被抢占，KV 块回收）
    │              │        │
    │              │        └─► 重新排队 → 再次 running
    │              │
    └─► rejected（显存无法容纳其最大长度，直接拒绝）
```

**抢占**（preemption）是 Continuous Batching 的另一面：当显存块池耗尽、新请求无法分配块时，调度器会挑选低优先级请求，回收其 KV 块（或 swap 到 CPU 内存），把显存让给更紧迫的请求。被抢占的请求重新排队，等待显存释放后继续生成。用户侧的体感是“某个请求突然卡了几秒”——它被请下车了。

这个分工不是纸上谈兵：当 GPU 单步被算子优化压到十几毫秒后，CPU 侧那"区区几毫秒"的调度开销反而成了新瓶颈——vLLM 在 2025 年的 V1 重构中重写调度器与进程架构，核心动机之一正是把控制平面开销压到近零。**优化是一场打地鼠：压下一个瓶颈，下一个瓶颈就抬头。**

另有一次性成本值得知道：服务启动时要把权重从磁盘经内存装进各卡 HBM（几十秒到几分钟），此后权重常驻、全体请求共享。这就是模型服务"启动慢、服务快"的原因。

## 10.5 番外药方:一步两字——投机解码

三味药都在优化"每步"的效率，但"一步只出一个字"这条铁律本身可以撼动吗？

**投机解码**（Speculative Decoding）的答案漂亮而狡猾：铁律不破，但可以**让别人先猜**。用一个小得多的"草稿模型"（比如 1B）快速连猜 $k$ 个字，再让 70B 大模型**一次前向并行验证**这 $k$ 个猜测——验证是 Prefill 型的并行计算（第 8 章：多 token 并行是大矩阵运算，AI 高），代价与生成一个字相近。猜对的部分全部收下，猜错处截断重来。数学上通过接受-拒绝采样保证：**输出分布与大模型独自生成严格相同**——又一个"精确等价、只省时间"的工程奇迹（同类还有 KV Cache 与 FlashAttention，你可以开始收集了）。

草稿命中率越高收益越大，简单文本上常见 2~3 倍加速。DeepSeek-V3 的 Multi-Token Prediction 则把"草稿员"直接内置成模型自带的预测头，连独立小模型都省了——这条战线在 2024 年之后仍在快速演化。

## 10.6 主线收官:再看一次那条 curl

十一章之前你发出的那条请求，现在可以用全书的语言完整复盘了：

> 你在聊天界面打下一句诗，客户端把 messages 打包成 HTTP 请求；服务端按 Chat Template 拼接成扁平文本（第 0 章站点 ①），prompt 被切成 10 个整数、查表成 $(10, 8192)$（第 1 章）；在 80 层车间里，RoPE 把顺序旋进 Q/K（第 2 章），注意力让「海」认出黄河（第 3 章），64 个问题共享 8 套答案（第 4 章），FFN 从五百亿参数里唤醒“名句该续流”（第 5 章），残差与归一化护送信号走完全程（第 6 章）；末位向量与 32000 个候选对表、温度与截断之后掷出「流」（第 7 章）；「流」喂回循环，KV Cache 免除重算，Prefill 与 Decode 各司其职（第 8 章）；块表让缓存塞满而不碎，前缀缓存让多轮对话不再重新啃历史（第 9 章）；大 batch 抬高算术强度、FlashAttention 少搬字节、CPU/GPU 各守其位、投机解码偷跑两步（本章）；最后，token 经反分词变回文字、封装成 SSE 帧逐字推回你的屏幕——半秒之内，《登鹳雀楼》逐字流回你的终端。
>
> 从算法到硬件，每一环都在回答同一个问题的不同侧面：**如何又快又省地预测下一个字。**

下次看到屏幕上逐字蹦出的回答，愿你眼前浮现的是这整条流水线——以及它每一处"为什么非这样设计不可"的理由。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 核心算术 | Decode 搬 140 GB / 算 0.45 ms：AI ≈ 1，距平衡点 156 差两个数量级 |
| 药方一 | Continuous Batching：大 batch 抬 AI 过平衡点；每步重组批次，块表保证隔离 |
| 药方二 | FlashAttention / 算子融合 / 量化：中间结果留 SRAM，做小 Bytes |
| 药方三 | 控制平面（CPU 管业务）/ 数据平面（GPU 管张量）；vLLM V1 重构为压调度开销 |
| 后处理 | 反分词（UTF-8 缓冲）→ 停止判定 → 统计 → SSE 帧推送；请求状态机含抢占与恢复 |
| 番外 | 投机解码：小模型猜、大模型并行验，分布严格等价，2~3 倍提速 |
| 全局闭环 | 省显存 → 多并发 → 大 batch → 高 AI → 高吞吐——第 9、10 章是同一场战役 |

## 练习

1. 用 10.1 节的方法估算 7B 模型（FP16，~14 GB 权重）在 2 TB/s 带宽下的单请求 Decode 上限（token/s）。再想想：为什么手机上跑 3B 模型反而"感觉不慢"？
2. batch 从 1 增到 128（仍在 memory-bound 区间内），单步延迟大约变化多少？总吞吐大约变化多少？（提示：搬权重的时间与 batch 无关）
3. Chunked Prefill 把长 prompt 切块、与其他请求的 Decode 混批。分析：对该长请求自己的 TTFT、对同批其他请求的 TPOT 各有什么影响？这是谁向谁让利？
4. 投机解码中，草稿模型猜 $k=4$ 个字、平均接受 2.4 个。粗略估算加速比（忽略草稿模型自身开销），并说明为什么"接受率"比"草稿速度"更关键。

## 延伸阅读

- Williams et al., *Roofline: An Insightful Visual Performance Model for Multicore Architectures* (CACM 2009)——算术强度框架的出处
- Yu et al., *Orca: A Distributed Serving System for Transformer-Based Generative Models* (OSDI 2022)——Continuous Batching 的首创
- Dao et al., *FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness* (NeurIPS 2022)——IO 感知内核的里程碑；FlashAttention-3 (2024) 为 Hopper 架构特化版
- Leviathan et al., *Fast Inference from Transformers via Speculative Decoding* (ICML 2023)——投机解码与接受-拒绝采样的等价性证明
- vLLM Blog, *vLLM V1: A Major Upgrade to vLLM's Core Architecture* (2025)——控制平面开销之战的一手工程记录

---

⬅️ 上一站：[第 9 章 · 显存战争](./part4-chapter09-显存战争.md) ｜ [返回目录](./README.md) ｜ ➡️ [附录 A · 速查手册](./appendix-a-速查手册.md)
