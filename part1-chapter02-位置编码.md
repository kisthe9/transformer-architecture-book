# 第 2 章 · 位置编码:旋转出来的顺序感

> **我们在解剖图上的位置**：仍在进入 80 层车间的门口。第 1 章交付了矩阵 $X: (10, 8192)$，但它缺一样东西——顺序。
>
> **本章回答的问题**：为什么说没有位置机制的 Transformer 是个"词袋"？为什么现代模型不再把位置信息"加"进向量，而是把向量"转"一个角度？「旋转」这个几何操作，凭什么能让模型感知相对距离？
>
> **前置依赖**：第 1 章·分词与嵌入（矩阵 $X$ 的来历，以及"没有顺序"这个悬念）。

---

## 2.1 本章需要的一个最小前提

位置信息最终要影响的是注意力的计算，所以本章无法完全绕开注意力。但你不需要提前学完第 3 章——只需要接受三句话（第 3 章会把它们从头推导一遍，并解释每个符号为何如此设计）：

1. 注意力要为每一对字算一个**相关分数**：第 $i$ 个字与第 $j$ 个字有多相关。
2. 算法是：每个字先被投影成两个向量——提问用的 $q$（query）和被查用的 $k$（key）；分数就是两者的**点积** $q_i \cdot k_j$。
3. 点积衡量方向一致性：两个向量指向越接近，点积越大，相关分数越高。

一个二维数值例，30 秒建立手感：

```
q = (1, 0)        k₁ = (1, 0)      → q·k₁ = 1×1 + 0×0 = 1     方向相同，最相关
                  k₂ = (0.7, 0.7)  → q·k₂ = 0.7               夹角 45°，中等
                  k₃ = (0, 1)      → q·k₃ = 0                 垂直，不相关
                  k₄ = (-1, 0)     → q·k₄ = -1                反向，负相关
```

记住这一条就够了：**注意力的相关性 = q 与 k 的点积**。本章要做的事，就是想办法让这个点积能反映出"这两个字隔多远"。

## 2.2 打乱的诗：一个必须先看清的灾难

第 1 章末尾埋下的悬念，现在正式引爆。

矩阵 $X$ 的十行分别是「白」「日」「依」「山」「尽」「，」「黄」「河」「入」「海」的语义向量。现在把行序彻底打乱，比如变成「海入河黄，尽山依日白」。这个新矩阵包含完全相同的十个向量，只是排列不同。

如果直接把两个矩阵分别送进注意力，输出会有区别吗？

**答案是：每个字得到的输出向量一模一样，只是跟着它的行一起换了位置。**

原因藏在 2.1 节那三句话里。注意力对每个字做两件事：（a）与所有字两两算点积；（b）按分数加权求和。点积集合与求和都不在意"谁排在第几行"——它们是**集合运算**，不是序列运算。术语叫**置换等变性**（permutation equivariance）：输入行序做任何置换，输出只跟着做同样的置换，内容分毫不变。

用一个两行的最小实验就能验证（这也是第 1 章练习 4 的正式答案）：

```python
import numpy as np

def attn(X, Wq, Wk, Wv):                       # 极简注意力：无位置、无掩码、无缩放
    Q, K, V = X @ Wq, X @ Wk, X @ Wv
    S = Q @ K.T
    A = np.exp(S - S.max(axis=1, keepdims=True))
    A /= A.sum(axis=1, keepdims=True)          # 逐行 softmax
    return A @ V

rng = np.random.default_rng(0)
X = rng.normal(size=(3, 4))                    # 3 个 token，4 维
Wq, Wk, Wv = (rng.normal(size=(4, 4)) for _ in range(3))

perm = [2, 0, 1]                               # 把行序打乱
out_a = attn(X, Wq, Wk, Wv)[perm]              # 先算注意力，再打乱输出
out_b = attn(X[perm], Wq, Wk, Wv)              # 先打乱输入，再算注意力
print(np.allclose(out_a, out_b))               # True —— 置换等变性
```

对语言来说这是灾难性的。「白日依山尽」和「尽山依日白」会得到完全相同的表示；「猫追狗」与「狗追猫」在模型眼里毫无区别。语言的意义有相当大的一部分就编码在顺序里，而顺序在第 1 章那次查表中已经丢失了——嵌入表只按 token 编号取行，根本不知道这个 token 出现在第几位。

RNN 时代不存在这个问题：它逐词读取，顺序天然编码在处理次序里。Transformer 为了让整句话能并行计算而放弃了逐词处理，代价就是**位置信息必须显式补回来**。怎么补，就是本章的全部内容。

## 2.3 第一代方案：把位置加进向量

2017 年的原始 Transformer 给出的答案直截了当：为每个位置造一个 8192 维的"位置向量" $P[i]$，直接加到词向量上。

$$
X'[i] = X[i] + P[i]
$$

位置向量用一组不同频率的正弦、余弦函数生成——低频维度变化缓慢，高频维度变化剧烈，不同位置得到不同的波形组合，就像给每个座位贴上一个独一无二的条纹图案。

这个方案能用，也确实撑起了 Transformer 的第一个时代。但随着模型变大、上下文变长，两个短板越来越刺眼：

**短板一：它编码的是绝对位置，而语言更关心相对关系。** 「形容词修饰紧随其后的名词」这条规律，与这两个词出现在第 3、4 位还是第 103、104 位无关——真正重要的是"相邻"。绝对编码把"第 3 位"和"第 4 位"两个标签交给模型，让它自己从两个绝对值里"减出"距离 1，这是一份本可以避免的额外作业。

**短板二：加法把位置和语义搅在了同一个空间里。** 词向量所在的 8192 维空间原本只表达语义，硬加一个位置向量进去，两种信息就叠加纠缠。训练时见过的长度范围内还好，一旦外推到更长的位置，位置分量的取值进入陌生区域，语义也跟着被污染，输出质量急剧退化。

## 2.4 第二代方案：把位置转成角度（RoPE）

现代主流模型（LLaMA、Qwen、Mistral 等）改用**旋转位置编码**（RoPE, Rotary Position Embedding）。它的思路发生了根本转向，一句话概括：

> 不往向量里**加**东西，而是把向量**旋转**一个与位置成正比的角度。

### 从二维讲起

先看最简单的情形：一个二维向量绕原点旋转角度 $\theta$，用旋转矩阵表示为

$$
R(\theta) = \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}
$$

RoPE 规定：**位于位置 $m$ 的向量，旋转 $m\theta$**——第 0 个字不转，第 1 个字转 $\theta$，第 5 个字转 $5\theta$。位置越靠后，转得越多，像钟表指针一格一格地走。

注意旋转的两个关键性质：它**不改变向量长度**（正交变换），只改变方向。这正是它优于加法的第一层原因——语义信息主要编码在方向的相对关系里，旋转是一次"整体扭转"，不会像加法那样把某几个维度的数值撑大或压小。

### 魔法发生在点积里

为什么"旋转"比"加法"高明？关键性质只需一行推导。设两个二维向量 $q$、$k$ 分别位于位置 $m$ 和 $n$，各自旋转后做点积（点积正是 2.1 节说的相关分数）：

$$
\langle R(m\theta)\, q,\ R(n\theta)\, k \rangle
= q^\top R(m\theta)^\top R(n\theta)\, k
= q^\top R\big((n-m)\theta\big)\, k
= \langle q,\ R\big((n-m)\theta\big)\, k \rangle
$$

中间那一步用了旋转矩阵的两条性质：$R(\alpha)^\top = R(-\alpha)$，以及 $R(-\alpha)R(\beta) = R(\beta - \alpha)$。结论令人愉快：

> **每个向量各自按绝对位置旋转，但点积的结果只依赖于位置之差 $n - m$。**

两个绝对角度在点积中自动相消，只留下角度差。模型不需要学任何"从两个绝对位置减出相对距离"的技巧——**相对位置由几何结构免费提供**。而实施上依然是每个向量独立旋转（不需要知道对方在哪），完全可以并行。绝对方式实施、相对方式生效，这是 RoPE 最漂亮的一笔。

用数值验证一下（顺便作为本章的代码锚点第一部分）：

```python
import numpy as np

def rot2d(v, angle):
    c, s = np.cos(angle), np.sin(angle)
    return np.array([c * v[0] - s * v[1], s * v[0] + c * v[1]])

q, k = np.array([1.0, 0.3]), np.array([0.6, -0.8])
theta, m, n = 0.4, 2, 5

lhs = rot2d(q, m * theta) @ rot2d(k, n * theta)   # 各自按绝对位置旋转后点积
rhs = q @ rot2d(k, (n - m) * theta)               # 只用相对距离旋转一次
print(lhs, rhs, np.isclose(lhs, rhs))             # 两者相等 → 点积只看距离差
```

### 从二维到 128 维

真实向量不是二维的。RoPE 的处理是：把向量的维度**两两配对**，每一对当成一个独立的二维平面各转各的，并且**每一对使用不同的旋转频率**：

$$
\theta_j = 10000^{-2j/d_k}, \qquad j = 0, 1, \ldots, \tfrac{d_k}{2} - 1
$$

在本书基准模型中，RoPE 作用于每个注意力头的 128 维向量（$d_k = 128$），即 64 个旋转平面，每个平面一个频率：

- $j$ 小的平面：$\theta_j$ 接近 1，转得快——对**近距离**差异敏感，能分辨"隔 1 个字"和"隔 2 个字"；
- $j$ 大的平面：$\theta_j$ 小到 $10^{-4}$ 量级，转得极慢——在几千个位置的跨度上才转过明显角度，负责表达**远距离**的粗粒度信息。

> **类比及其边界**：这像一个多转盘里程表——快盘记个位、慢盘记万位，读数组合起来能唯一表示很大的范围。类比成立在"多个不同速率的周期量组合可以无歧义编码大范围距离"这一点上。但两处别当真：里程表的每一位是十进制离散跳变，而旋转角是连续的；里程表各位之间有严格进位关系，而 RoPE 各平面完全独立、互不进位。

如果所有平面用同一个频率会怎样？那么所有平面的角度差都相同，距离信息就退化成一个单一的角度——而角度以 $2\pi$ 为周期，距离 $d$ 与 $d + 2\pi/\theta$ 会给出完全相同的点积贡献，远近混淆。多频率的设计正是为了消除这种周期性歧义（这也是本章练习 2）。

### 它到底作用在哪一步

一个关键的实施细节，也是 V1 那类叙述容易含糊的地方，这里明确交代：

**RoPE 不作用在第 1 章那个嵌入矩阵 $X$ 上，而是作用在每一层注意力内部——投影出 $q$、$k$ 之后、计算点积之前。**

```
每一层注意力内部的顺序：
   x ──投影──► q, k, v            （每头 128 维）
              │  │
              ▼  ▼
         RoPE 旋转 q 和 k          ← 位置在这里注入
              │  │
              ▼  ▼
        点积 q·k → 相关分数 → 加权 v
                                   ↑ v 从不旋转
```

两个"为什么"值得说透：

**为什么在每一层都做，而不是入口做一次？** 因为 RoPE 旋转的是 $q$ 和 $k$，而这两个向量是每层各自投影出来的临时产物，不是沿着主干道传递的那个 $x$（第 6 章会讲清"主干道"的含义）。每层都要重新投影，自然每层都要重新旋转。

**为什么只转 $q$ 和 $k$，不转 $v$？** 回到设计初衷：位置信息应该影响"谁和谁相关"（这由 $q \cdot k$ 决定），而不该篡改"相关之后取来什么内容"（这由 $v$ 提供）。如果把 $v$ 也旋转，取回的内容会随位置发生系统性偏转，等于让语义内容染上位置的颜色——正是第一代加法方案的毛病。RoPE 干净地把位置限制在"分数"这一侧（本章练习 3 请你想清楚这件事的后果）。

### 完整的 RoPE 代码锚点

把上面所有内容合成一段可运行代码。变量名与公式对齐：`d_k`、`theta`、`m` 分别对应 $d_k$、$\theta_j$、位置 $m$。

```python
import numpy as np

def rope(x, base=10000.0):
    """x: (n, d_k) —— 一个注意力头的 q 或 k。返回旋转后的同形状数组。"""
    n, d_k = x.shape
    j = np.arange(d_k // 2)
    theta = base ** (-2.0 * j / d_k)              # 每个平面的频率 θ_j
    m = np.arange(n)[:, None]                     # 位置 m
    ang = m * theta[None, :]                      # (n, d_k/2) 每个位置每个平面的角度
    x_even, x_odd = x[:, 0::2], x[:, 1::2]        # 维度两两配对（奇偶交错式）
    out = np.empty_like(x)
    out[:, 0::2] = x_even * np.cos(ang) - x_odd * np.sin(ang)
    out[:, 1::2] = x_even * np.sin(ang) + x_odd * np.cos(ang)
    return out

q = np.random.default_rng(0).normal(size=(10, 128))   # 10 个 token，d_k = 128
q_rot = rope(q)
print(q_rot.shape)                                     # (10, 128)
print(np.allclose(np.linalg.norm(q, axis=1), np.linalg.norm(q_rot, axis=1)))  # True：长度不变
```

> **与生产实现的差异说明**：Hugging Face 的 LLaMA 实现（`rotate_half`）把向量按**前半/后半**分组配对，而不是这里的奇偶交错配对。两者是同一个操作在不同维度排列下的写法——只要模型权重按相同约定训练，数学结果完全等价。本书采用交错写法是为了与公式 $\theta_j$ 的下标一一对应；你在读源码时看到前后半分组，不必怀疑自己理解错了。

## 2.5 我们的诗此刻的状态

回到「白日依山尽，黄河入海」。经过本章，模型获得了分辨顺序的能力：

```
位置 0: 白   →  q/k 旋转 0·θ⃗      （不转）
位置 1: 日   →  q/k 旋转 1·θ⃗
位置 2: 依   →  q/k 旋转 2·θ⃗
  ⋮
位置 9: 海   →  q/k 旋转 9·θ⃗

于是：「日」(位置1) 看「白」(位置0) 的位置贡献，
      与「河」(位置7) 看「黄」(位置6) 的位置贡献完全相同——都是距离 1。
```

最后这一点是相对位置编码真正的价值：**位置模式可以跨位置复用**。模型学到的"关注紧邻的前一个字"这条规律，只需要学一次，就在所有位置生效，不必为每个绝对位置各学一遍。这既省参数，也省数据。

同时，第 1 章那个"行序可以随意打乱"的漏洞被堵上了：打乱行序会改变每个字的旋转角度，从而改变所有点积分数——「白日」和「日白」终于是两回事了。

## 2.6 工程视角：长上下文是怎么"扩"出来的

RoPE 的相对性带来一个非常实用的推论：既然点积只看角度差，那么理论上只要**角度差落在训练时见过的范围内**，模式就依然有效。这条推论催生了一整条工程线——把训练时只见过 4K 上下文的模型，扩展到 32K、128K 甚至更长。

主流做法有三类，本质都是对频率表 $\theta_j$ 动手术：

| 路线 | 做法 | 直觉 |
|------|------|------|
| **位置插值**（Position Interpolation） | 把位置索引整体压缩，如 $m \to m/4$ | 把 16K 的位置"折叠"回 4K 的角度范围——模型见过的区间 |
| **NTK-aware / YaRN** | 不均匀缩放：高频平面几乎不动，低频平面大幅拉伸 | 近距离分辨率是模型的立身之本，别动；要扩的是远距离量程 |
| **抬高 base** | 直接把 $10000$ 换成更大的数（Llama 3 用 500000） | 所有平面整体转慢 → 同样的角度范围覆盖更长的距离 |

三条路线的共同前提，正是 2.4 节那个"点积只依赖角度差"的性质——如果当年用的是绝对位置加法方案，这类低成本改造根本无从下手。**一项设计的优越性，往往体现在它给后人留下了多少改造空间**，RoPE 是这句话的教科书案例。

顺便回答一个常见困惑：为什么厂商宣称的"支持 128K"往往在实测中不如短上下文表现？因为频率手术只保证位置编码不失效，不保证模型真的学会了利用那么远的信息——注意力分数在长距离上普遍衰减，检索能力需要额外的长文本训练来兑现。看到"支持 N K 上下文"的宣传时，把它读作"位置编码在 N K 内不崩"，而不是"在 N K 内同等好用"。

至此，进门前的准备全部完成：$X: (10, 8192)$ 携带语义，旋转机制随时待命注入位置。下一章我们正式踏进 80 层车间的第一道工序，把 2.1 节那三句话从头推导一遍——并回答第 1 章留下的另一个悬念：「海」如何知道自己是"入海"的海。

---

## 本章小结

| 要点 | 一句话 |
|------|--------|
| 为什么需要位置 | 注意力是集合运算（置换等变），顺序信息在嵌入查表时已丢失，必须显式补回 |
| 第一代方案 | 正弦位置向量直接相加：编码绝对位置、污染语义空间、外推差 |
| RoPE 核心 | 位置 $m$ 的向量旋转 $m\theta$；旋转不改长度，点积中绝对角度自动相消为角度差 |
| 相对性的价值 | 位置模式跨位置复用，"看紧邻前一字"只需学一次 |
| 多频率设计 | 维度两两配对、各配不同频率；单频率会因 $2\pi$ 周期造成远近歧义 |
| 作用位置 | 每层注意力内部、投影之后点积之前，只转 $q$ 和 $k$，不转 $v$ |
| 工程红利 | 长上下文扩展（插值 / YaRN / 抬 base）都是对频率表动手术，靠的就是相对性 |

## 练习

1. 手算验证 2.4 节的关键性质：取 $q = (1, 0)$、$k = (0, 1)$、$\theta = 30°$、$m = 2$、$n = 5$，分别算出等式两边的点积，确认相等。
2. 假设所有维度对都用同一个频率 $\theta = 0.1$。找出两个不同的位置距离 $d_1 \neq d_2$，使它们给点积带来的贡献完全相同。（提示：$2\pi/\theta$）
3. ★ RoPE 只旋转 $q$ 和 $k$。假如把 $v$ 也按同样规则旋转，注意力的输出会发生什么性质的变化？特别地，想一想"同一个字在不同位置出现时，它贡献给别人的内容是否应该不同"。带着你的答案读第 3 章·注意力的加权求和一节。
4. 把 2.4 节的 `rope` 函数应用到两个相同的向量上，分别放在位置 3 和位置 7，然后计算它们与位置 5 的某个 $k$ 的点积。验证：两个点积的差异只来自距离（2 和 -2），而与"3"和"7"这两个绝对数值无关。

## 延伸阅读

- Su et al., *RoFormer: Enhanced Transformer with Rotary Position Embedding* (2021)——RoPE 原论文，2.4 节的频率公式与相对性证明出处
- Vaswani et al., *Attention Is All You Need* (NeurIPS 2017) §3.5——第一代正弦位置编码的原始设计
- Chen et al., *Extending Context Window of LLMs via Position Interpolation* (2023)；Peng et al., *YaRN* (2023)——2.6 节两条频率手术路线的原始论文
- Press et al., *Train Short, Test Long: Attention with Linear Biases (ALiBi)* (ICLR 2022)——另一条技术路线：直接给注意力分数加距离惩罚，与 RoPE 形成对照

---

⬅️ 上一站：[第 1 章 · 分词与嵌入](./part1-chapter01-分词与嵌入.md) ｜ [返回目录](./README.md) ｜ ➡️ 下一站：[第 3 章 · 注意力：让向量互相塑造](./part2-chapter03-注意力.md)
